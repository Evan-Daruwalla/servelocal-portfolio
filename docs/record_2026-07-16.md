# Record — 2026-07-16

## 2026-07-16 ~13:40 CST — privacy page: stock security hedge removed (humanizer pass)

**WHAT.** One-line copy edit in `frontend/app/privacy/page.tsx`: the stock
sentence "No system is perfectly secure, but we follow industry practices to
protect your information." trimmed to "No system is perfectly secure." The
"industry practices" clause was vague boilerplate; the same paragraph already
names the actual practices (Argon2, HTTPS).

**WHY.** Surfaced by an AI-texture review (the-humanizer skill) Evan ran on the
policy text — the one flagged line that survived into v2. Two other flags from
that review were already moot here: the pasted text was v1's policy (frozen
reference, untouched), and v2's minors section already documents proactive
guardian consent (M5).

**VERIFIED.** Backend pytest 211 passed; frontend lint + build clean (after an
`rm -rf .next` to clear a stale routes-manifest ENOENT); browser-verified on a
dev server at :3005 (port 3000 is serving v1 via Docker) — old hedge absent,
new line renders, zero console errors.

**NOT COMMITTED — concurrent session.** The working tree holds 10+ frontend
files modified by a parallel session, including a full in-progress rewrite of
this same privacy page (DRAFT-pending-legal-review banner, guardian-rights
section, Stripe/Resend disclosures) that carries this edit inside it.
Committing the file would sweep that session's work. Left in the working tree
for whichever session commits the privacy-page batch.

**OPEN (BLOCKED-ON-EVAN).** The privacy page has a literal
`[CONTACT EMAIL — Evan]` placeholder in two mailto links (guardian-rights +
changes/contact sections) — needs Evan to pick the real address (v1 used
privacy@servelocal.org) before launch.

## 2026-07-16 ~23:42 CST — Strix pentest runbook added (docs/strix-staging-pentest-runbook.md)

**WHAT.** New doc: an execution-ready runbook for running Strix (open-source
autonomous-pentest CLI, `strix-agent` v1.1.0) against the LOCAL `docker compose`
staging stack at M11-launch time. No app code touched — docs only.

**WHY.** ServeLocal ships publicly at M11 holding minors' PII; a dynamic
exploit-validation pass against staging is coverage `/audit` + security-review
(static) don't give. Evan vetted Strix (skill-vet) — it's a standalone tool, not
a skill — and asked for the exact recipe.

**HOW (grounding discipline).** Built via a verify-workflow: two workers swept
Strix's real interface (the actual CLI source `strix/interface/main.py` + README +
PyPI), one drafted, two adversarially verified. Every flag/env/output path is
source-quoted or tagged [CONFIRM]; nothing invented. Corrections the verify caught:
the install line is `pip install strix-agent` (not the undocumented `pipx`/pin),
flags are grounded to `main` so `strix --help` is the authority, curl|bash
installer rejected. Safety additions: command-based pre-run gate, email/outbound
neutralization (Strix fires real PoCs → real guardian emails), kill switch, DB
snapshot/restore, LOCAL-only (never Railway — AUP), uncapped `--max-budget-usd`
default. Compose-grounded fact: web:3000 + api:8000 publish on 0.0.0.0 (sandbox
can reach via host.docker.internal), Postgres loopback-only.

**BLOCKED-ON-EVAN.** Runbook is not runnable until: LLM-provider usage-policy
permits offensive-agent use, and the API-account-ownership tension is resolved
(providers require 18+; Evan is 17). Flagged in the doc, not worked around.

**NOT verified by execution** (deliberately): this is a future-use runbook; Strix
was not installed or run this session. The DoD's browser/pytest checks don't apply
to a standalone-tool doc with no ServeLocal runtime surface.

---

## 2026-07-20 ~14:06 CDT — taste-skill design pass on marketing surfaces (Lucide icons, truthful claims)

**WHAT.** Evan installed `leonxlnx/taste-skill` (vetted via /skill-vet first: MIT, injection-scan
clean, pinned commit `98565e6`, cherry-picked ONLY the flagship skill of 13 — manifest at
`../.claude/skills/taste-skill/VETTED.md`) and asked for a design-improvement pass. Ran the
skill's Redesign-Preserve protocol: audit first, brand tokens locked (Fraunces/green/gold stay —
its serif ban yields to "brand already names it"), scope limited by its §13 to marketing-class
surfaces: landing, /for-organizations, /register. Dashboards/product UI untouched.

**Changes.** (1) Emoji-as-UI-icons replaced with Lucide (already a dep via shadcn — zero new
packages; skill's §3.C allows Lucide when the project already depends on it): 14 icons on
landing (hero mini-cards, how-it-works, bento), 4 on for-organizations, 2 register role-picker.
One family, strokeWidth 1.75, brand green (amber in gold-tinted cells); DB-driven category
emoji (discover avatars) is data, not decoration — left alone. (2) **False trust claim
removed**: for-organizations claimed "Domain-email verification plus admin review" — neither
exists in the v2 backend (grep-verified: no org verification/admin-review flow). Same class as
the 9c64ece org-trust fix; this instance had slipped through. Rewritten to public reviews
(true). (3) Stats bar "100% Easy setup" (meaningless percentage, skill §9.D) → "Guardian
consent / Required for minors" (true: require_consent gates apply/check-in/message). (4) CTA
intent unified: pledge "Get Started Free" → "Sign Up Free" (matches nav); hero secondary now
links /for-organizations (page exists; previously went straight to /register). (5) Leftover
em-dash placeholder `{oppCount ?? "—"}` → "…". (6) Bento diversity (§4.7): awards +
reviews-reputation cells get brand gold-pale tint (`.bento-card.gold`, additive CSS only).

**Verify.** Dev-server DOM checks on all 3 pages: 14/4/2 lucide svgs, zero emoji, zero em
dashes, gold tint + icon colors computed correct, role icons centered; zero console errors;
`npm run lint` clean; `npm run build` clean (dev server stopped first — the .next clobber
gotcha). **Honest limitation:** Browser-pane screenshot capture was broken this session (all
capture paths timed out; DOM tools fine), so visual verification was computed-style/DOM-based,
not eyeball-based. Backend was down during checks (Docker Desktop wouldn't start) — landing
count correctly shows its fallback; change touches no API paths.

---

## 2026-07-20 ~14:28 CDT — icon pass extended to product UI (opus-worker sweep, ~100 swaps)

**WHAT.** Evan approved extending the taste-skill icon pass beyond marketing surfaces. Full
frontend emoji inventory (106 hits, 13 files) split into replace-targets (pictographic emoji)
vs keep-glyphs (typographic: check/x/stars/hearts/plus/infinity — brand-consistent, kept). New
shared `components/v1/category-icon.tsx` replaces the duplicated per-page CAT_EMOJI/CAT_BG maps
(9 categories -> Lucide icon + pastel bg + matching darker fg). One Opus worker executed the
mechanical sweep from an exact per-file spec (dashboard + org sidebar tabs restructured to
{id,label,Icon}, empty states 40px, meta rows 13px, format pips 12px, award tiles 24px,
leaderboard medals -> per-rank-colored Medal, nav bell, donate hearts, pricing star/grad-cap);
orchestrator reviewed. Worker made 4 flagged extrapolations (identical elements the per-file
bullets missed; all used the spec's own mappings) - accepted; one reviewer patch (org sidebar
logout arrow -> LogOut icon). Hand-rolled search-bar SVG deliberately left (v1's own, fill-based
CSS; zero visible gain to swap).

**Verify.** Lint clean (worker + re-run after patch), `npm run build` clean (dev server stopped
first), DOM checks: discover error-state TriangleAlert + MapPin zip label, pricing gold Star +
GraduationCap, donate Heart button - zero emoji, zero console errors on all driven pages.
**Honest limitations:** (1) screenshot capture still broken this session (DOM/computed-style
verification only); (2) Docker Desktop would not start, so authenticated screens (dashboard,
applicants tabs) could not be driven live - their changed paths are the same mechanical pattern,
tsc/build-verified; drive them once the stack is up. Backend untouched; the 215-pass pytest run
from ~14:05 stands.

---

## 2026-07-20 ~15:42 CDT — landing hero primary CTA: "Browse Opportunities" -> "Sign Up Today"

**WHAT.** Evan: change the landing hero's primary button from "Browse Opportunities" to
"Sign Up Today". Changed text AND href (`/discover` -> `/register`) — a "sign up" label
pointing at the browse page would be a broken CTA. Secondary button ("For Organizations" ->
/for-organizations) unchanged. One-line JSX edit in `frontend/app/page.tsx`; no CSS/logic touch.

**FLAGGED (open decision).** This reintroduces the exact CTA-label inconsistency the 2026-07-20
taste-skill pass had just resolved: nav + pledge say "Sign Up Free", hero now says "Sign Up
Today" — three signup-intent CTAs, two labels (taste-skill §4.5 one-label-per-intent). Made
Evan's exact wording as instructed; raised the unify-vs-keep question back to him rather than
silently overriding. Not yet resolved.

**Verify.** DOM: primary = "Sign Up Today" -> /register, secondary unchanged, zero console
errors. (Screenshot capture still broken this session; verified structurally. Docker still down —
irrelevant, no API path touched.) Not committed pending the label decision.

**Resolution (~15:46 CDT).** Evan chose option 1: unify all signup-intent CTAs to "Sign Up
Today". Nav (`components/v1/v1-shell.tsx`, shows on every v1 page) and pledge (`page.tsx`)
"Sign Up Free" -> "Sign Up Today". All three /register CTAs now read identically (DOM-confirmed:
nav/hero/pledge = "Sign Up Today"). CTA-label consistency restored. Lint + build clean; committed
and pushed.

---

## 2026-07-20 ~15:51 CDT — reverted the landing CTA change (back to original)

**WHAT.** Evan: "actually revert the change." Undid the entire CTA episode (commit `6e0eab2`) at
Evan's direction. Hero primary back to "Browse Opportunities" -> /discover; nav + pledge back to
"Sign Up Free". Reverted by hand-editing the two code files (not `git revert`, which would also
strip the two record entries inside 6e0eab2 and break the append-only trail). Confirmed clean:
`git diff 032b21e` on both frontend files is empty (byte-identical to the pre-CTA state).

**Verify.** DOM: hero = "Browse Opportunities"/discover, nav + pledge = "Sign Up Free", zero
console errors. Lint + build clean. (Screenshot capture still broken this session; Docker still
down — no API path touched.) Committed + pushed so the private remote no longer carries the
reverted change. The 6e0eab2 record entries above stay as the honest trail of the round-trip.

---

## 2026-07-20 ~15:56 CDT — signup CTA label "Sign Up Free" -> "Sign Up"; public mirror synced

**WHAT.** Evan: drop "free" from the signup buttons. Nav (`components/v1/v1-shell.tsx`) and
pledge (`page.tsx`) "Sign Up Free" -> "Sign Up". Hero primary stays "Browse Opportunities"
(unchanged from the revert). The "Free forever for students" hero tag and the students-free
messaging elsewhere are untouched - this was only the two button labels.

**Verify.** DOM: nav + pledge = "Sign Up", zero console errors; lint + build clean. Committed +
pushed to private, then `scripts/sync_portfolio.py --commit --push` refreshed the public mirror
(stale by the icon-pass + revert commits). Screenshot capture still broken this session (DOM
verification); Docker down (no API path touched).

---

## 2026-07-22 ~18:14 CDT — PRD gains a GOAL block (skeleton sync)

**WHAT.** The /project-memory PRD skeleton (`templates.md` §3) gained a required **GOAL** block
today (2026-07-22, Evan's proposal) — one paragraph at the very top, after the standing-document
line and before SCOPE GUARD, stating what will exist when done + for whom + the single sentence a
fresh session reads to stay on track (not vision, not process). The live ServeLocal PRD predates
it (written 2026-07-07), so it was out of sync. Added a filled-in GOAL to
`servelocal-v2/PRD_ROADMAP.md` in that exact slot: publicly launched student-volunteering platform
on FastAPI+Next, verified hours -> exportable portfolio, org rosters, under-18 guardian-consent
gate, free forever for students (funded by org Pro + donations); north-star sentence = ship to
real users at a public URL (M11) with M5 consent as the non-negotiable gate.

**HOW/NOTES.** Purely additive — no existing planning history touched (per the PRD append-only
rule). Kept the GOAL undated to match the skeleton's treatment (date lives here + in the commit).
Deliberately did NOT add a SCOPE GUARD (optional in the skeleton; inventing scope limits is a
separate decision) and did NOT touch Trading's PRD (also predates the skeleton; flagged to Evan).
No code touched; no tests/build relevant. Committed to the private repo; PRD is not in the public
portfolio mirror manifest, so nothing to sync there.

---

## 2026-08-05 ~12:46 CDT — full cold audit (two passes) + P1/P2 fix batch

**WHAT.** Ran `/audit` twice. Pass 1 (baseline tooling) and a two-agent second
round: a **live-stack** auditor that owned the containers (M8 data-at-rest,
live-endpoint M6, G1/G2 executed) and a **static** auditor barred from Docker
(frontend surface, M2 call-site contracts, M1 deep, M3 exit-gates, M4, M7, G3/G4).
Docker came back up mid-session, which is what made M8/M6-live reachable at all —
they were `not swept` in pass 1 and are now genuinely swept.

Evan approved "fix everything through P2 in one pass". Landed in four batches.

**The crit — M5 guardian consent did not work end to end.** `ConsentBanner` was
the only caller of the only path that sends the guardian email, and it was never
mounted anywhere (`grep` for `ConsentBanner` returned exactly one hit: its own
definition, confirmed with two tools). `register` recorded `pending` and sent
nothing — `auth.py:114-115` said so outright. So every minor registered into a
permanent gate no guardian was ever told about, while three UI surfaces claimed an
email had been sent. 215 green tests missed it because they all call the API
directly. Fixed at the chokepoint: extracted `services/consent_invite.py` and fire
it server-side at the end of `register`, so the launch gate no longer depends on
any UI being mounted. Mounted the banner too, restyled from shadcn/Tailwind to v1
`.consent-zone` (the dashboard lives in the `.v1` editorial shell). The three "we
emailed your guardian" strings became TRUE, so no copy change was needed.

**Other fixes.** Production config guard now fails CLOSED (only explicit dev/test
names skip it) + `RESEND_API_KEY`/`TURNSTILE_SECRET_KEY`/32-byte `SECRET_KEY`
checks; `ge=1` on four unbounded settings; `role:"admin"` no longer
self-registerable; self-report dedupe + approved-application filter + strict
numeric types; `require_consent` on auto-log/self-report/appeal/exclude-date;
idempotent re-verify; `IntegrityError`→409 on concurrent apply; `/health` 503 when
the DB is down; `consent_blocks` fails closed on null dob; consent audit rows now
name the guardian (`actor_id=None`, `target`, IP/UA meta) instead of the student;
audit rows on both hours-minting paths and on Stripe webhook signature failure;
rate-limit key sweep; `verify_password` abnormal-failure logging; pytest
warnings-as-errors.

**HOW / two corrections to the auditors.** Their proposed F1 fix — a partial
unique index on `(opportunity_id, user_id) WHERE occurrence_date IS NULL` — was
NOT implemented: it would permanently block a second self-report for the same
opportunity, breaking recurring commitments. Deduped on the still-unreviewed row
instead. Their "the unique constraint is a 100% no-op" was also overstated:
check-in (`opportunities.py:399`) and auto-log (`hours.py:65-73`) both set
`occurrence_date` and both already dedupe; self-reports are null *by design*. The
live auditor also called the dev Postgres "the production DB" — it isn't; the site
is unlaunched.

**Verified by feeding each fix its own trigger, not by reading patches.**
8 concurrent `POST /hours`: was 8x201 -> 11.0 verified hours + an unearned NHS
award; now 201 + 7x409, one row, 3.0 hours. Revoked minor `POST /hours`: was 201,
now 403 `GUARDIAN_CONSENT_REQUIRED`. 8 concurrent apply: was `[201,409x6,500]`, now
no 500. Consent token minted at registration confirmed in the live DB — the three
probe minors created *before* the fix show `has_token = f`, the one after shows
`t`. Banner verified in-browser (correct v1 computed styles, resend round-trip,
self-hides for `not_required`, zero console errors). `249 passed` (from 215, +34),
now under warnings-as-errors — the auditor found `-W error` had collapsed
collection to 0 tests. Lint + `tsc --noEmit` + build clean.

**Two mistakes of mine, both caught by running things.** (1) My first
`ENVIRONMENT` fix only normalized case/whitespace, so `prod` still booted on the
dev `SECRET_KEY`; the trigger script caught it and it became a fail-closed
allowlist. (2) I had the invite service charge the resend throttle, which 429'd a
student's first retry — exactly when a guardian email lands in spam. Seven tests
failed; I reverted the design choice rather than editing the tests around it.

**HONEST OPEN ITEMS (not fixed).**
- **Age floor drift, needs Evan's decision.** PRD says no-under-13;
  `core/consent.py:11` ships `MINIMUM_AGE = 12` and both legal drafts say 12. Code
  and legal agree with each other and disagree with the PRD. Not changed silently.
- **Org account deletion is unreachable, needs a decision.** No endpoint can
  deactivate an opportunity (`Opportunity.active`: 4 reads, 0 writes), so
  `delete_me` 409s telling orgs to do something the API doesn't expose. The fix is
  a NEW endpoint, not a repair.
- **Consent `declined`/`revoked` are still absorbing states.** The audit wants
  re-request allowed; v1's ADR-0010 reportedly treats declined as terminal. That
  conflict was NOT resolved — flagged for Evan rather than guessed.
- **`/health` semantics conflict.** `test_health.py` previously documented 200-on-
  DB-down as intended (liveness). Changed to 503 because `railway.json` wires it as
  `healthcheckPath`, which routes on status code. Documented in the test file.
- **Not reached in the P2 sweep:** unbounded `list_reviews`/`list_messages`,
  org-notification-on-pending-work, ruff wired into CI, JWT lifetime/httpOnly
  migration, remaining `send_email` return values, notification `link` constraint.
- **Recurrence DST/midnight/month-boundary logic never live-exercised** — every
  seeded opportunity is `one_time`.
- **Suite still runs on in-memory SQLite, never Postgres** — which is exactly why
  the NULL-unique semantics behind the hours bug went uncaught. Not fixed (sized
  large by the auditor).
- **Probe data left in the dev DB on purpose** (~15 accounts on
  `@servelocal-audit-probe.org` plus child rows) — not deleted without Evan's say.
- Screenshot capture still unavailable (pane not compositing); browser
  verification was DOM + computed-style.

---

## 2026-08-05 ~22:06 CDT — audit follow-through: mirror, leftovers, Postgres CI

**WHAT.** Evan: "1 then 2 then 3" — sync the public mirror, finish the audit
leftovers, then close the SQLite-only test gap.

**1 — Public mirror synced** (`scripts/sync_portfolio.py --commit --push`, `c5c5eeb`,
121 files, secret guard clean). Verified by reading the mirror rather than trusting the
exit code: HANDOFF carries the 2026-08-05 callout, PRD shows the corrected
`no-under-12 policy`, and II.22 is present in both the record `.md` and the regenerated
`.html`. **Correction to something I said earlier:** `PRD_ROADMAP.md` IS in the mirror
manifest — I had claimed it wasn't. Nothing sensitive in it, but it is public.

**2 — Leftovers.** `send_email`'s return was discarded at every remaining call site.
The sharpest was the guardian manage/revoke link (`consent.py`): minted once, only its
hash stored, no resend route — so a dropped send destroyed the guardian's only kill
switch permanently and silently. Now ERROR-logged plus an audit row. Password reset:
the caller always sees the same generic response (no enumeration, deliberate), so a
silent mail failure was indistinguishable from a typo'd address; now logged + audited.
Notifications: fire-and-forget by design, but a total outage looked exactly like normal
operation; now WARNING-logged. Also: orgs are finally told when an application needs
approval — apply emitted no notification at all, so an org with `requires_approval=True`
could sit on a queue it never knew existed. Only the `pending` case notifies.

**E501 cleared, not deferred.** Earlier today it was ignored in writing with a dated
reason; `ruff format` then cleared 235 of the 242 and the remaining 7 were long string
literals no formatter can split, wrapped by hand. `ruff format --check` added to CI so
it cannot drift back.

**3 — The suite now runs on Postgres too.** `conftest.py` honors `TEST_DATABASE_URL`
(default stays in-memory SQLite so the suite runs anywhere), and CI gained a
`backend-postgres` job with a `postgres:16` service running the identical suite. This is
the gap that let the hours bug live: `uq_hours_opp_user_date` is inert for self-reported
rows because Postgres treats NULLs as distinct in a unique index, and no SQLite-only run
can observe that. **258 passed on real Postgres in 56s**, 258 on SQLite in 33s.

**HOW / a wrong first cut.** My first Postgres fixture rebuilt the schema per test —
DROP/CREATE plus ~30 `CREATE TABLE`s, 258 times. It blew past a 600-second timeout and
I killed it. Replaced with a session-scoped schema plus a single `TRUNCATE ... CASCADE`
between tests, which is what got it to 56s.

**HONEST NOTE — what this did NOT prove.** Running on Postgres did not retroactively
catch the hours bug, because no test asserted the constraint's behavior; the regression
test written earlier today is what actually covers it. The Postgres job closes the
*class* of gap going forward, it did not find anything today. Saying otherwise would
overstate it.

**Verify.** `ruff check` clean, `ruff format --check` clean (97 files), 258 pytest green
on BOTH engines under warnings-as-errors, `npm run lint`/`tsc --noEmit`/`build` clean,
CI YAML parsed and asserted to contain all three jobs plus the postgres service.
Applied Alembic revisions confirmed untouched (only `alembic/env.py`, which is config,
not a revision).

**STILL OPEN.** Recurrence DST/midnight/month-boundary logic never live-exercised (all
seeded opportunities are `one_time`); `httpOnly`-cookie migration for the JWT (sized
large, not done); no support contact behind the "contact support" decline copy
(BLOCKED-ON-EVAN); screenshot capture still unavailable in this environment.

---

## 2026-08-06 ~13:47 CDT — support contact wired (address still blocked); recurrence clock edges

**WHAT.** Evan: "1 and 2" — give the consent decline dead-end a real exit, and
exercise the recurrence edge cases the audit never reached.

**1 — Support contact.** The address itself is BLOCKED-ON-EVAN and was NOT invented.
What existed: the literal string `[CONTACT EMAIL — Evan]` hardcoded in **six**
`mailto:` links across `privacy/page.tsx` and `terms/page.tsx`, plus consent copy
telling declined students to "contact support" with no channel behind it. Deploying
as-is would have shown real users a mailto pointing at a bracketed placeholder.

Centralized to one value: `SUPPORT_EMAIL` (backend) / `NEXT_PUBLIC_SUPPORT_EMAIL`
(frontend, build-time), a `lib/support.ts` + `<SupportEmail/>` component, and
`_with_support()` in `consent.py` that appends a contact ONLY when one is configured.
Wired through `Dockerfile` ARG/ENV, `docker-compose.yml` build args, and `.env.example`.
**Production now refuses to boot without it** (`check_production_config`), which turns
a silent placeholder leak into a hard gate — the same treatment `RESEND_API_KEY` and
`TURNSTILE_SECRET_KEY` got yesterday.

Verified against the prerendered static HTML (Docker had dropped again, so no browser):
unset → placeholder renders as **plain text with zero `mailto:` links** (was 6 dead
ones); set → 8 real `mailto:` links and no placeholder. Rebuilt clean afterward so no
test address stayed baked in.

**2 — Recurrence clock edges (audit G2, previously never swept).** Every seeded
opportunity in the project is `one_time`, so `occurrences.py` had never been driven
across a DST shift. Added 8 tests: weekly spacing across US spring-forward and
fall-back (asserting exactly 7-day deltas), the ambiguous 01:30 fall-back hour in both
folds, a 23:30-local start anchoring to the NEXT UTC date (and NOT the local one —
the exact bug v1 had), monthly December→January year rollover, a monthly series
anchored on leap day 2024-02-29 clamping to 2025-02-28 without duplicates, and the
`_MAX_OCCURRENCES` cap actually firing on a 30-year window.

**RESULT: all 8 passed first time.** The engine is correct across every one of these.
That is a real finding — an assumption inverted and found guarded — not a fix. Nothing
in `occurrences.py` changed.

**HOW / the new ruff gate caught my own code.** `B905` — `zip(got, got[1:])` without an
explicit `strict=`. The lists differ by one by construction, so `strict=False`. Worth
noting the gate wired yesterday earned its keep within a day.

**Verify.** `ruff check` + `ruff format --check` clean, **267 pytest green** (from 258)
under warnings-as-errors, `npm run lint` / `tsc --noEmit` / `build` clean.

**HONEST OPEN ITEMS.** The support ADDRESS is still BLOCKED-ON-EVAN — the mechanism is
built, the value is not set, and production will refuse to start until it is. Docker
went down partway through, so the browser pass fell back to inspecting prerendered
HTML; that verifies exactly what a user receives for these static pages but did not
exercise the consent banner live.

---

## 2026-08-06 ~13:55 CDT — PRD continuation: M11.1(a) closed out, deploy runbooks de-drifted

**WHAT.** Evan: continue the PRD until a blocker or completion. Rollup
(`milestone-track`, canary 16/16): **17/23 done (73.9%), 6 open — all M11 plus one
M12 line.** Read every M11 task; each is BLOCKED-ON-EVAN at its key step (host
account, domain purchase, prod secrets, Resend key, Turnstile site key, Sentry
account, legal sign-off, real users for soft launch). The single genuinely
model-doable item was **M11.1(a) — the token-storage ADR**.

**ADR 0001 already existed** (2026-07-16, decision: keep localStorage-JWT, don't
switch to cookie sessions) but its five Follow-ups were all marked "NOT implemented
here" — and three of them had since landed. Verified each against SOURCE, not
HANDOFF: TTL 7d→24h (`config.py`, yesterday's audit); `POST /auth/logout` bumps
`token_version` (`auth.py:174`) and `get_current_user` compares it (`deps.py:31`);
401 interceptor at `api.ts:65`. Appended a dated follow-up-status table.

**This materially shrinks the risk the ADR signed off on.** Its decision paragraph
accepts shipping "a 7-day token, no server-side logout" — both are now fixed. Status
stays **Proposed**; Evan's sign-off is BLOCKED-ON-EVAN. So the *implementation* half
of the PRD criterion is satisfied and the *sign-off* half is not — the checkbox stays
unticked. Not ticking it is the point.

**Deploy runbooks were actively wrong, and would have bitten Evan on first deploy.**
Yesterday's audit made `RESEND_API_KEY`, `TURNSTILE_SECRET_KEY` and (today)
`SUPPORT_EMAIL` hard boot blockers, and made the guard fail CLOSED. Neither
`DEPLOY.md` nor `DEPLOY_RAILWAY.md` knew:
- `SUPPORT_EMAIL` / `NEXT_PUBLIC_SUPPORT_EMAIL` were absent entirely — deploy would
  crash-loop with no runbook entry explaining why.
- The boot-guard table still claimed `ENVIRONMENT != "production"` skips the guard.
  That is now backwards: it is skipped only for an explicit dev/test name, so `prod`
  and typos are guarded. Rewrote the table with all six conditions.
- The 32-byte `SECRET_KEY` floor was undocumented.

**Also added a warning that did not exist anywhere: run the api on ONE replica.**
Three protections are in-memory per-process — the per-IP rate limiter
(`core/rate_limit.py`), the check-in failure throttle (`services/checkin.py:19`), and
the guardian consent-resend throttle (`core/consent.py:17`) — and **nothing in the
code enforces the single-process assumption; it is a convention, not a guard.**
Scaling Railway past one replica multiplies every limit by the replica count,
silently, on a platform holding minors' data. That is an UNENFORCEABLE contract in
the audit's sense, and the runbook is the only place it can currently be caught.

**Verify.** `ruff check` + `ruff format --check` clean, 267 pytest green under
warnings-as-errors, lint + `tsc --noEmit` clean. No code changed in this entry —
docs only, all claims grepped against source.

**BLOCKER REACHED — stopping here.** Every remaining PRD item needs Evan: a host
account + domain purchase (M11.3), Resend production key + verified domain (M11.4),
Turnstile site key (M11.1b), Sentry account (M11.5), legal review by an adult
(M11.2), the support address, ADR 0001 sign-off, the billing decision (ADR 0002
default: free tier until Evan turns 18), and real users for the soft launch (M11.6).
None of these can be prepared further without faking them.

---

## 2026-08-06 ~18:41 CDT — second cold audit; the previous fixes were partly wrong

**WHAT.** Ran `/audit` again with two cold agents on disjoint scopes (backend
internals; frontend + deps + spec), both weighted toward `b1d7e71..f9006e0` — the
previous day's fix batch, i.e. the highest-churn, least-reviewed code in the repo.
Docker was DOWN again, so **M8 and live-endpoint M6 were NOT swept** and G1 produced
constructed triggers only. That is thinner than the 2026-08-05 pass in exactly the
dimension that found the hours bug, and is stated as such rather than papered over.

**The headline is that yesterday's fixes were incomplete, and the audit caught it.**

1. **The hours dedupe I added was narrowed, not closed.** I keyed it on the hours
   VALUE so a "genuinely different entry" could pass. That made it trivially
   bypassable: resubmitting `3.01` instead of `3.0` minted a fresh row every time —
   reproduced at **11 pending rows for one 3-hour shift → 33.55 verified hours and an
   unearned NHS award**. Worse, **my own test encoded the bypass**
   (`test_self_report_allows_a_genuinely_different_entry`), which is why the suite
   stayed green. Dropped the value clause; replaced the test with one that probes
   3.01/2/0.5/3 and asserts 409 on every one, plus a second test proving a real
   second session is still allowed *once the first is reviewed*.
2. **`set_active`, the endpoint I added yesterday, made a pre-existing hole
   reachable.** `redeem_checkin` never checked `opp.active`, and check-in mints
   `status="verified"` hours directly — so a student holding a code could credit
   themselves for an event the org had cancelled. `apply` gated on this correctly;
   check-in never did.
3. **My `ENVIRONMENT` fail-closed change only covered values you SET.** The default
   was still `"development"`, which is in the lenient set, so *omitting* the variable
   skipped the entire boot guard and booted on the dev `SECRET_KEY`. Default is now
   `production`; dev/CI/compose all set it explicitly. And my `f9006e0` commit had
   deleted the one doc row that warned about this.
4. **`admin_reopen_consent` (mine, yesterday) had no `db.commit()`** — the status
   change, audit row and notification persisted only as a side effect of the invite
   service's commit. Latent, but it was the only one of 18 such routes without one.

**CRIT found independently of my changes: admin escalation.** `is_platform_admin`
granted admin to any account whose email appeared in `ADMIN_EMAILS`, and registration
seeded `is_admin` from the same list — with **no email verification anywhere**. So
whoever registered a listed address FIRST became a platform admin with the audit log
(every user's security events, minors included) and consent-reopen. The runbook told
you to set `ADMIN_EMAILS` at Step 4, before any account existed — precisely that
window; and account deletion rewrites the email to a tombstone, which would free a
listed address for anyone to claim. **The auditor's proposed fix was insufficient** —
it only removed the signup seeding, leaving the request-time email grant intact. Both
paths closed: admin is the `is_admin` column only, promotion is explicit SQL against
an existing account, documented in a new `DEPLOY_RAILWAY.md` §Granting platform admin.

**Also fixed.** The M5 gate's error rendered to gated minors as a raw JSON blob
(`{"code":"GUARDIAN_CONSENT_REQUIRED",…}`) because `api.ts` stringified dict details —
now unwrapped, with pydantic 422 arrays handled too. The public leaderboard published
a **revoked** minor's first name, last initial and hours with no gate and no opt-out
(the reachable path is consent → hours → revoke, not "unconsented minor earns hours",
which is impossible since apply and auto-log are both gated). Rejected applicants kept
thread write access and could post a public 1-star review. Three token-hash columns
were unindexed on unauthenticated, unrate-limited GETs (migration 0024). Three
in-memory throttles never evicted, one keyed by attacker-supplied email. Stripe
webhook redelivery duplicated audit rows. Re-approval silently rotated the guardian's
manage token, dead-linking the "only way to manage your decision" email. The data
export dumped every column of five models behind a claim with no mechanism — now a
per-model allowlist with a test freezing the key set. Keyboard users could not create
an org account (`<div onClick>` role tiles). An org-name field showed "Saved ✓" and
discarded the value. A ZIP filter had three inert controls and asserted a 15-mile
default that never existed. `jsx-a11y/recommended` enabled (the project's gate could
not see any of it). `pip-audit` added — `dependencies.md` claimed it was installed and
it never was. CVE scans wired into CI non-blocking. `.gitignore` now covers
`*-data-export.json`, which was one `git add -A` from committing a real export.

**Verify.** `ruff check` + `ruff format --check` clean, **277 pytest green** (from
268) under warnings-as-errors, `tsc --noEmit` clean. `humanizeDetail` has a real
runtime check — the frontend has no test runner, so the function lives in its own
dependency-free module, is compiled with the installed tsc and executed against all
five detail shapes plus the fallback.

**HONEST OPEN ITEMS.** M8 and live M6 NOT swept (Docker down) — the 2026-08-05 pass
had them and this one does not. 4 npm advisories remain open: the `next` fix requires
a major 15→16 bump that removes `next lint` and breaks the CI lint step — sized large,
NOT done. CSP still ships `script-src 'unsafe-inline'`; no sink exists today, and
removing it needs Next nonce plumbing. The audit-log append-only contract and the
single-process in-memory state remain **UNENFORCEABLE** — convention, not guard.
Docker being down also meant no browser verification of any frontend change this pass.

---

## 2026-08-06 ~19:17 CDT — graphify refresh; five codebase-memory bins reconciled

**WHAT.** Rebuilt the v2 knowledge graph after the day's audit fixes. The
`graphify-out/` manifest was missing, so `detect_incremental` saw all 245 files as
new — effectively a full re-extract merged onto the existing graph rather than a
true incremental. Stated plainly because the run cost reflects that.

**Result: 1594 nodes, 2937 edges, 159 communities.** AST pass 1405 nodes / 3230
edges over 211 code files (deterministic, free). Semantic pass over 28 docs + 2
images via 4 parallel subagents (4 files hit the extraction cache); no
`GEMINI_API_KEY` is set, so extraction ran through subagents. **456,776 tokens** —
that is the combined subagent total; the harness exposes one figure per agent, not
an input/output split, so `cost.json` records it in `input_tokens` with output at 0.
Not an estimate, just a split that was not available.

**Graph health check: clean** — 0 dangling, 0 missing-endpoint, 0 self-loop, 0
collapsed edges across 2937 edges. 40 communities hand-labelled from inspection;
the 119-community long tail labelled by deriving from each cluster's dominant
source file (mechanical, and flagged as such rather than presented as considered).

**The extraction found real drift the audit had missed: five codebase-memory bins
contradicting each other.** Three were caused by me earlier today — I updated
`security.md` for the admin/boot-guard changes and did not sweep its siblings.
Reconciled all five, each with a dated supersedes note:
- `dependencies.md` claimed "pip-audit IS installed in the venv". It never was;
  it is now declared in `requirements.txt` with a non-blocking CI step.
- `features.md` still documented admin as `is_admin` seeded from `ADMIN_EMAILS`
  **OR** the email at request time — the exact escalation path closed this morning.
- `features.md` pinned "189 pytest green; migrations 0001–0021"; reality is 277 and
  0001–0024.
- `tooling.md` said `backend/Dockerfile` runs `alembic upgrade head` on boot;
  `architecture.md` has carried the correction since 2026-07-16 (migrations run via
  the compose `command:` / Railway `preDeployCommand`).
- `tooling.md` described `check_production_config` as two blockers firing on
  `ENVIRONMENT=production`; it is now fail-closed with six.

**God nodes** (most connected): `useAuth()` 45, `api` 29, `occurrence_dates()` 28,
`V1Shell()` 21, `ApiError` 21, `PRD & Roadmap` 21, `append_audit()` 19. That
`occurrence_dates()` ranks third is notable given it is the one module the audits
could never exercise live.

**HONEST NOTES.** This was a re-extract, not an incremental — the missing manifest
means the token cost is not representative of a routine update. The long-tail
community labels are derived mechanically from filenames, not read and named. And
every "Suggested Question" the report emitted is an AMBIGUOUS low-confidence edge,
which is the tool working as designed (it surfaces what it is least sure about) but
means none of them is a strong lead.

## 2026-08-07 ~05:54 CDT — Next 15→16, nonce CSP, live M6/M8 audit, bin sweep

**WHAT.** The two items the 2026-08-06 audit had reported as "sized large, not
done", then the two audit methods every prior pass had to skip for want of a
running stack, then a sweep of the codebase-memory bins. Commits `d9b5ccf` + this
one.

### 1. Next 15.5.20 → 16.3.0 — `npm audit` 5 → 0

`next` was the blocker: the vulnerable range covered every 15.x, so the 8 Next
advisories (Server Action DoS/SSRF, cache confusion, unauthenticated disclosure
of internal Server Function endpoints) had no patch short of the major. `npm
audit fix` cleared postcss/js-yaml/brace-expansion/sharp/nanoid alongside it.

Next 16 removes `next lint`, which WAS the CI lint gate. `npm run lint` is now
`eslint .` over a new flat `eslint.config.mjs`; `.eslintrc.json` is deleted. Two
things `next lint` did implicitly had to become explicit: build output is
excluded via `ignores` (flat config has no `.eslintignore`, and without it ESLint
walks `.next/` forever), and `eslint-plugin-jsx-a11y` is now a direct
devDependency instead of being resolved through eslint-config-next. The full
jsx-a11y `recommended` set is preserved — eslint-config-next turns on only 6 of
those rules and the other 20 were deliberate.

The bump also took `eslint-plugin-react-hooks` 5 → 7, whose new React Compiler
rules flag 12 pre-existing sites. **Set to `warn`, not fixed and not switched
off**, with the reasoning written into the config. `set-state-in-effect` is
largely a false positive for this app's shape: the JWT lives in localStorage
(ADR-0001) and localStorage cannot be read during render without breaking SSR
hydration, so seeding state inside an effect is correct here — complying
literally in `lib/auth-context.tsx` would INTRODUCE a bug.
`preserve-manual-memoization` is a real nit (4 useMemos in `applicants/page.tsx`
depend on a render-time `new Date()`, so they never actually memoize) but
harmless at this array size. Rewriting either under a dependency bump, in a
frontend with no test runner, is a bad trade.

### 2. script-src drops 'unsafe-inline'

`frontend/proxy.ts` (Next 16 deprecated the `middleware.ts` filename in favour of
`proxy.ts`) mints a per-request nonce and builds the CSP there. The static CSP
was REMOVED from `next.config.mjs` — two `Content-Security-Policy` headers are
intersected by the browser rather than overridden, so a leftover copy would have
silently broken every page.

This closes the gap ADR-0001 named as the price of localStorage tokens. That
decision leaned on "no XSS sink exists today" (zero `dangerouslySetInnerHTML`) —
an argument about the present that a single future mistake invalidates. CSP is
now an actual backstop instead of a claim.

Deliberately NOT done, both documented in `proxy.ts`: `style-src` keeps
`'unsafe-inline'` (a nonce covers `<style>` elements but never `style="..."`
attributes, which is exactly what React's `style={{...}}` prop compiles to — the
`.v1` pages use it throughout, so removing it breaks the layout of every screen);
and no `'strict-dynamic'` (it makes the browser ignore the Turnstile host
allowlist, and Turnstile is injected by app code as a plain `<script>` tag).

Cost paid: a nonce cannot exist in build-time HTML, so `app/layout.tsx` sets
`dynamic = "force-dynamic"` and **28 routes moved from static prerender to
per-request render**. Cheap here — every page is a client shell that fetches from
the API in the browser, so no server-side data work was being cached — but the
HTML is no longer CDN-cacheable. The 4 metadata routes (icon/OG/robots/sitemap)
stay static; they carry no inline scripts.

Verified in a production build AND through the `output: standalone` Docker image
(where middleware bundling was the actual risk): an inline `<script>` with no
nonce is refused — the console logs "Executing inline script violates the
following Content Security Policy directive" and the injected function never ran;
the same script carrying the served nonce runs; the Turnstile origin still loads;
the nonce differs on every request; the app hydrates and navigates client-side.
Logged in as the seed student against the live API, the dashboard renders real
data with ZERO console messages.

### 3. Live audit — M8 (data at rest) and M6 (endpoint probing)

Docker was up, so the two methods every prior pass had to skip finally ran. **Not
a cold audit** — I had been working on this code all session. That is a real
limitation and the reason this is reported as verification rather than as a fresh
audit. It is partly mitigated by both methods being empirical: they look at what
the database actually contains and what the server actually returns, neither of
which depends on my beliefs.

**M8.** Every secret-shaped column is a hash (argon2id passwords, sha256 token
hashes); no plaintext tokens at rest. Deleted accounts are genuinely scrubbed —
email tombstoned to `deleted-<hex>@deleted.invalid`, password `!`, dob and
guardian fields cleared, `is_active` false, `token_version` bumped. Migration
head 0024, with its three token-hash indexes present. Zero orphans across 8 FK
relationships. Zero admin accounts exist, consistent with the ADMIN_EMAILS fix.
Consent evidence (IP, user-agent, decided_at) persisted; the approve-token hash
is NULLed after use, so single-use is enforced at rest and not merely in the
reply. The audit log wrote 12 distinct action types during probing.

**M6.** 60+ checks across six rounds. Auth boundary (12 endpoints 401 to
anonymous), role separation, mass-assignment (`is_admin` and `plan` cannot be set
through `PATCH /auth/me`), IDOR (a student approving their own hours gets
"Organizations only" and the row does not flip), no user enumeration on login or
password reset, no internals in error bodies, auto-log idempotent, and the rate
limiter returning 429 — confirmed by accidentally locking myself out with 40
login attempts.

**The M5 launch gate was driven end to end on real Postgres for the first time,
in both directions.** A fresh minor lands in `pending` and gets 403
`GUARDIAN_CONSENT_REQUIRED` on apply, check-in, auto-log and self-report, and is
absent from the public leaderboard. After a real guardian approval through
`POST /consent/{token}`, the same calls return 201/200. An adult student on the
identical endpoint returned 409 "Already applied" — the control proving those
403s were the gate and not a broken route. Replaying a spent token 404s. The
guardian-facing `GET /consent/{token}` exposes only first name + last initial.

Also exercised my own two most recent fixes, the least-reviewed lines in the
repo. The 3.01 hours-dedupe bypass is shut: six submissions (3, 3.01, 2, 0.5, 7,
3) for one shift produced exactly ONE row, the other five 409. Check-in on a
DEACTIVATED opportunity 404s and mints zero verified hours; reactivating makes
the same code reachable again — the control proving the block was the `active`
flag and not a dead endpoint.

**No new defects were found in the code.** Six apparent "failures" were all wrong
assumptions in my own probes — guessed route paths (`/applications` vs
`/applications/my`, `/messages/threads` vs `/messages`, `/awards/me` vs
`/awards/my`), wrong method (verify is PATCH, not POST), wrong field names
(`date`, not `occurrence_date`), a dedupe guard I expected to key on date when
self-reported hours have no date to key on, and a test address on the reserved
`.invalid` TLD that pydantic rejected — which looked like user enumeration until
retried with a valid-format address. Recorded because a probe that fails for a
dumb reason and then gets quietly "fixed" is exactly how a real finding gets
talked away.

The dev DB was restored to its documented baseline exactly (users=8,
opportunities=3, hours=3, applications=2, notifications=1, audit_log=37,
reviews=1) and verified orphan-free afterwards. Note for next time: that baseline
is slightly artificial — `POST /hours/auto-log` legitimately mints a 4th hours
row on any dashboard load, because the seed student has an approved application
to an opportunity that has already ended. That row is correct behavior, not
drift.

### 4. Bins — a contradiction inside a single file

The sweep found `security.md` contradicting **itself**: §Audit log + admin still
said `GET /audit-log` admits "`User.is_admin` (seeded from `ADMIN_EMAILS` at
registration) OR email in `ADMIN_EMAILS` at request time" — the exact escalation
path that §Production boot guard, a few sections later in the SAME FILE, said was
closed. I updated one section on 2026-08-06 and missed the other. A reader
hitting the audit-log section first would have believed the dangerous version.

Worse, and operator-facing: **`backend/.env.example` still told the operator that
`ADMIN_EMAILS` was the "comma-separated emails allowed to read GET /audit-log".**
It grants nothing. Someone provisioning production would list their address,
believe they had admin, and not have it. `config.py`'s comment had been updated
on 2026-08-06; the example env had not. It now carries an explicit
read-this-before-setting-it warning pointing at the SQL promotion runbook and the
regression test that pins the variable inert.

Also fixed: `security.md` said five boot blockers while `tooling.md` said six —
both counting truthfully, neither saying how (the two `SECRET_KEY` checks are
`if/elif`, so it is six checks over five variables and at most five fire at
once); the consent-gate route list named 4 routes when `Depends(require_consent)`
appears on 9; `testing.md` still said 189 tests (277) and omitted the Postgres
test mode entirely; `data.md`, `architecture.md` and `INDEX.md` still said
migrations 0001–0023 (0024). The `settings.admin_emails` property was dead — my
own 2026-08-06 change orphaned its last consumer — and is removed; the
`ADMIN_EMAILS` field itself stays, deliberately inert and pinned by
`test_admin_emails_alone_does_NOT_grant_admin`.

One bin was RIGHT and I ignored it. `gotchas.md` documents that a React re-render
after `el.click()` is async and must be read in a SEPARATE `javascript_tool`
call. I read the DOM in the same call, concluded the page had not hydrated, and
briefly believed the new CSP had broken the app. It had not. The bin had said so
since 2026-07-13.

**MY OWN ERROR, recorded.** I ran `date` at 22:11 and 22:30 on 2026-08-06, the
session then sat idle for about seven hours, and I carried those stamps forward
instead of re-running `date`. Commit `d9b5ccf` and six code comments were dated
2026-08-06 when the work actually happened on 2026-08-07. Caught only because an
hours row appeared to be timestamped in the future. Corrected in this commit; the
commit message itself needed no change, because both of its date references
pointed at the previous audit. This is precisely the failure the "always run
`date` before writing any timestamp" rule exists to prevent, and the gap was long
enough that nothing else about the session looked stale.

**VERIFY.** `pytest` **277 passed** (35s); `ruff check` + `ruff format --check`
clean (97 files); `eslint .` exit 0 (12 warnings, all the documented React
Compiler rules); `tsc --noEmit` clean; `next build` clean; `npm audit` **0
vulnerabilities**. Browser-verified in production mode, in dev mode, and through
the Docker standalone image.

**STILL OPEN.** CSP `style-src` keeps `'unsafe-inline'` (structural, explained
above). Audit-log append-only and single-process in-memory state remain
UNENFORCEABLE conventions. `SUPPORT_EMAIL` is still BLOCKED-ON-EVAN and is now a
hard boot blocker. M11 launch remains BLOCKED-ON-EVAN.

## 2026-08-08 ~21:20 CDT — pushed; mirror synced; both CVE scans made blocking

**WHAT.** Pushed the 2026-08-07 batch, refreshed the public mirror, and turned
the two dependency-CVE scans from non-blocking log noise into real gates.

**Push + mirror.** `d9b5ccf` and `1b0e7f1` to `servelocal-v2` (private). Mirror
synced via `scripts/sync_portfolio.py --commit --push` — 125 files, secret guard
clean, `security.md` correctly absent. The mirror was several sessions behind, so
it also picked up the support-contact module and `humanize-detail` from the
2026-08-06 batch. Nothing from `backend/` ships, so the `.env.example`
`ADMIN_EMAILS` correction stays private, which is right — it names an internal
provisioning procedure.

**Both CVE scans are now BLOCKING.** They shipped `continue-on-error: true` on
2026-08-06 with a stated intent to tighten later; this is that follow-up.

The reason to check before flipping, and the thing worth recording: **`pip-audit`
had never actually been run.** It is pinned at `2.9.0` in `requirements.txt` and
CI installs it, but it is not in the local dev venv, and CI ran it non-blocking —
so its output went to a log nobody reads. Its true result was therefore unknown,
and making it blocking on that basis would have been guesswork that could red-line
CI on the next unrelated push. Installed the declared pin locally and ran it: **"No
known vulnerabilities found", exit 0.** The npm side was already confirmed at 0 on
2026-08-07. Only then did the flip become safe.

That ordering is the actual lesson. A non-blocking scan on a project holding
minors' data is not a gate, but you cannot convert one into a gate without first
knowing what it currently says — and the honest answer here was "nobody knows".

**Tradeoff accepted, and written into the workflow.** A newly-published upstream
CVE will now fail CI on an unrelated PR, possibly before a fix exists. The comments
give the surgical escape rather than leaving "delete the step" as the obvious move:
`pip-audit --ignore-vuln GHSA-…` with a dated reason, or dropping npm's threshold
to `critical` with a dated reason and raising it back. Note the two sides are
deliberately asymmetric — `pip-audit` has no severity threshold and fails on ANY
advisory, which is stricter than npm's `--audit-level=high`, and fine for a Python
tree this small.

`dependencies.md` updated in the same commit, superseding BOTH earlier claims (the
2026-07-13 "already installed" and the 2026-08-06 "non-blocking"), plus a note that
the pin is absent from long-lived dev venvs. Updating it separately would have
recreated exactly the drift the previous entry was written to close.

**VERIFY.** `pip-audit` → "No known vulnerabilities found" (exit 0); `npm audit
--audit-level=high` → 0 vulnerabilities; `npm ci` clean from the lockfile;
workflow YAML parses and reports **zero** remaining `continue-on-error` steps
across all three jobs.

**NOTE ON DATES.** Roughly a day and a half passed between the previous entry
(2026-08-07 ~05:54) and this one — `date` was re-run rather than carried forward,
which is the correction to the 2026-08-07 mistake recorded in the entry above.

## 2026-08-08 ~21:30 CDT — CI had been RED for three pushes and nobody looked

**WHAT.** Pushing the CVE-gate change meant checking that CI would survive it.
`gh run list` showed the last **three** runs had failed — 2026-08-06, 2026-08-07
and the push made minutes earlier. I had declared "277 pytest green" on every one
of them. The tests were green; they were green **on my machine**.

**The failure.** One test, `test_omitting_environment_entirely_is_guarded`, in both
backend jobs: `AssertionError: assert 'ci' == 'production'`. 276 passed, 1 failed.

It is the test guarding the fail-closed `ENVIRONMENT` default — the fix from
2026-08-06 that stopped a bare host booting on the dev `SECRET_KEY`. The test
builds `Settings(_env_file=None, ...)` and asserts the default is `production`.
`_env_file=None` bypasses `backend/.env` (which sets `ENVIRONMENT=development` for
local dev), and I stopped there, believing that was full isolation. It is not:
**pydantic-settings ranks the PROCESS environment ABOVE the env file**, and
`ci.yml` sets `ENVIRONMENT: ci` for the whole backend job. So in CI the test read
back "ci". Locally, with nothing in my shell, it read "production" and passed.

The test was asserting whatever the ambient environment happened to be, and it had
been broken since the moment it was written. Fixed with
`monkeypatch.delenv("ENVIRONMENT", raising=False)` so both sources are removed and
the assertion means what it says.

**Checked for siblings before moving on.** Every other `Settings(...)` in
`test_startup.py` passes `ENVIRONMENT` as an explicit kwarg, which outranks env
vars, so they were never exposed. Exactly one test asserted a *default*, and that
is exactly the one that broke. No broader conftest machinery added — there is one
vulnerable pattern and one instance of it.

**VERIFY.** Ran the suite under both conditions, which is what I should have done
originally: `ENVIRONMENT=ci pytest -q` → **277 passed**; `pytest -q` with the
variable unset → **277 passed**. Previously the first of those was 276/1.

**The real failure is process, not code.** The Definition of Done says "run them;
don't assume", and I did run them — locally, three times, and reported the local
number as if it were the CI number. HANDOFF even carried "validated locally, first
run on next push" for the CI row, which was an accurate description of a gap that
then went unclosed for three pushes. A local pass and a CI pass are different
claims and I conflated them. `testing.md` now says so explicitly, with the
reproduce-CI-locally command and a note to run `gh run list` after pushing.

**One thing this did confirm.** The frontend job passed on all three runs,
including the Next 15→16 push — so the major bump, the flat ESLint config and the
`npm ci` lockfile all work in a clean Ubuntu environment, not just on this box.
That was the migration risk I had flagged and could not fully test locally.

## 2026-08-08 ~21:42 CDT — swept the suite for the same class of bug; found one detonating in 2 days

**WHAT.** Rather than push the CI fix immediately, swept the other 27 test files
for the same failure mode: a test that passes here because of something true only
about this machine or this moment. Found a second, unrelated instance with a
two-day fuse.

**Method.** Two passes. A static sweep for ambient dependencies (`os.environ`,
network calls, filesystem paths, `datetime.now`, settings-singleton mutation),
then an empirical one: take a file, shift every hardcoded date five weeks into
the past, run it, and see what breaks. A test that survives that is genuinely
date-independent; one that fails was silently relying on "now".

**The static sweep came back mostly clean**, which is worth stating rather than
padding. Exactly one `os.getenv` in the whole suite (`TEST_DATABASE_URL` in
conftest, deliberate). No network calls — `httpx.post` is patched in the Turnstile
test. No filesystem paths. One `datetime.now`. Every settings override goes
through `monkeypatch.setattr(settings, ...)`, which both restores automatically
and sets the value EXPLICITLY, so none of them can inherit the ambient
environment — that is why the `ENVIRONMENT` bug was a lone instance rather than a
pattern: it was the only test asserting a *default*.

**THE FIND: `test_recurring.py` was two days from failing.** `2026-08-10` appeared
17 times, chosen because it is a Monday of the weekly fixture's series. `GET
/date-spots` returns only UPCOMING occurrences, so the moment the wall clock
passed 2026-08-10 those lookups would start raising `KeyError`. Proven, not
guessed: shifting the file's dates five weeks back reproduced it immediately —
`KeyError: '2026-07-06'`, 2 failed / 7 passed, in
`test_date_spots_reflects_single_date_signups` and `test_exclude_and_include_date`.

A second bomb in the same file: `series_end="2026-08-31"` in
`test_create_weekly_opportunity_persists_recurrence`. Once the derived series
start moved past 2026-08-31 that would be a series ending BEFORE it begins —
invalid, not merely stale.

Fixed by deriving the dates from `date.today()` (`_monday_after`), so they cannot
expire. Checked the helper's modular arithmetic against **400 consecutive
simulated run-dates**: start always strictly future, start/OCC1/OCC2 always
Mondays, `_NOT_AN_OCC` never a Monday, ordering preserved, `series_end` always
after the last occurrence — 400/400.

**Scoped the blast radius before changing anything.** Ran the same shift
experiment on the other three files holding future dates: `test_hours_parity`
(15 passed), `test_notifications` (13 passed) and `test_occurrences` (19 passed)
were all immune. So the fix touches one file, not four. `2099-01-01` in
`test_hours.py` is a deliberate never-expires date, and DOB `2000-01-01` is an
adult forever — both correct as-is. The auto-log tests pin their own 2024 dates
because they NEED a past series; those are permanently past and were left alone.

The two DOBs of `2012-…` used to make a minor become adults in 2030 and the
consent tests would then fail loudly. Noted, not changed — a 2030 fuse on a test
that fails visibly is not worth churn today.

**Docker came back up mid-sweep, which unlocked the verification I wanted
earlier.** `.dockerignore` excludes `.env`, so the api image is Linux + UTC + no
env file + dependencies installed fresh from `requirements.txt` — every axis on
which CI differs from this Windows box, which is exactly the combination that hid
the `ENVIRONMENT` bug. Full suite in that container: **277 passed**. Then against
real Postgres in an isolated `servelocal_test` database: **277 passed**. Both CI
backend jobs are now reproduced locally instead of inferred. The temp database was
dropped and the seeded dev DB re-verified at its documented baseline (users=8,
opportunities=3, hours=3, applications=2, notifications=1, audit_log=37,
reviews=1).

**VERIFY.** `ENVIRONMENT=ci pytest -q` on Windows → 277 passed; in the Linux
container with no `.env` → 277 passed; against Postgres in the container → 277
passed; `ruff check` + `ruff format --check` clean. `testing.md` gains the
container-as-CI-proxy commands and the shift-the-dates technique, so the next
sweep does not have to rediscover either.

## 2026-08-08 ~21:49 CDT — CI green at last; frontend swept for the same class of bug

**WHAT.** Pushed the three pending commits, confirmed CI actually went green, then
ran the machine/moment sweep against the frontend, which has no test runner and so
could only be swept statically plus verified in a browser.

**CI IS GREEN.** Run `31290958949`: Backend (pytest) ✓, Backend (pytest on
Postgres) ✓, Frontend (lint + build) ✓. First green run after three consecutive
red ones — and green *with* the two dependency-CVE scans now blocking rather than
`continue-on-error`, so the gates are real and passing rather than merely quiet.

**The frontend sweep. Most of it came back clean, which is the finding.** Stated
explicitly because "I looked and there was nothing" is a result, and padding it
with speculation would be worse than useless:

- **Occurrence-date keys are consistent, not mixed.** This was the thing most
  worth checking — CLAUDE.md warns that v1 had a UTC/local mixing bug and that v2
  must not port it. The backend anchors every occurrence date to UTC
  (`occurrences.py`). `org-checkin-section.tsx` derives its date with
  `new Date(opp.start_time).toISOString().slice(0, 10)` — also UTC, so it matches.
  And `signup-section.tsx` does not compute a date at all: `singleDate` comes from
  `Object.keys(dateSpots)`, i.e. the backend's own keys echoed back. **The frontend
  never derives an occurrence key locally**, which is exactly the property that
  makes the mixing bug impossible here. Verified by tracing every site that sends
  a date to the API, not by reading one file.
- No `os.environ`-equivalent ambient reads, no hardcoded years in user-facing copy
  other than the one below, no network assumptions.

**THE FIND: `© 2026 ServeLocal` was hardcoded** in `components/v1/v1-shell.tsx`,
which renders the footer on every `.v1` route — i.e. most of the site. Wrong on
every page from 2027-01-01. Same class as the test time bomb: correct only at this
moment. Now `{new Date().getFullYear()}`.

It carries `suppressHydrationWarning`, and the reason is specific to the CSP work
earlier today: the root layout is now `force-dynamic`, so this footer is rendered
on the server for SSR *and* again in the browser on hydration. For a few hours each
New Year those two sit in different calendar years, which would otherwise log a
mismatch. The text is cosmetic, so taking the client's value is correct.

Verified in a production build, in a browser, rather than assumed: the footer reads
the current year, and the SSR markup contains `© <!-- -->2026<!-- --> ServeLocal` —
the comment separators are React's marker for a computed expression, which proves
the year is server-rendered rather than patched in on the client. Zero console
messages beyond the expected API refusals (backend not running).

**Reported, not changed.** `lib/api.ts:172` exports `excludeDate` and **nothing
calls it** — the backend endpoint exists and is tested, but no UI reaches it. It is
pre-existing dead code rather than something this session orphaned, so it is
flagged here instead of deleted.

**Noted for a future date, deliberately not churned.** The dashboard and applicants
calendars group events with `toDateString()`, which is LOCAL, while the backend keys
occurrences in UTC. Both sides are internally consistent and nothing is sent to the
API from that path, so no data can be corrupted — but an evening shift whose UTC
timestamp lands on the following calendar day will appear on the local day in the
calendar while its check-in code and hours row carry the UTC date. That is a display
inconsistency, and changing the anchor is a design decision with wide blast radius,
not a bug fix to slip into a sweep.

**VERIFY.** `eslint .` exit 0; `tsc --noEmit` clean; `next build` compiled; footer
confirmed in a live production build; CI green on all three jobs.

## 2026-08-11 ~17:17 CDT — third cold audit (findings only); revoke semantics decided

**WHAT.** `/audit` run cold: two fresh auditors on disjoint scopes (backend
internals; frontend + deps + spec), plus my own pass on the repo tooling neither
was given. **32 findings, nothing fixed** — a findings pass changes nothing. Evan
then decided the revoke-semantics question the audit surfaced.

**Health.** The machine-checkable surface is genuinely strong: 277 tests green,
ruff/eslint/tsc clean, `npm audit` 0, the lockfile installs clean on a bare
machine, full-history secret scan clean on BOTH repos, the nonce CSP verified
live, and five of six inverted assumptions came back guarded. The defects cluster
in two blind spots no gate here can see: **contracts enforced at write time but
never re-checked at read time**, and **fixes applied to one sibling and not its
twins**.

**CRIT 1 — and it is my own incomplete fix.** `GET /portfolio/{id}` gates on role
+ `portfolio_public` only. Consent is checked at write time, never read time. So a
guardian's revoke blocks the minor's new actions but leaves their public
transcript — name, org names, hour totals — served. Reproduced with controls:
post-revoke `apply`→403, leaderboard→`[]`, portfolio→**200**. On 2026-08-06 I
fixed the leaderboard and wrote a comment on it reading "The consent gate covered
`portfolio_public` but not this board" — then never checked the third sibling,
which exposes MORE than either. Portfolio is also missing the `is_active` guard
the leaderboard has. Two lines.

**CRIT 2 — `min_age` is decorative.** Written, stored, displayed on the org create
form, and enforced NOWHERE. Verified with two independent tools: it appears in the
migration, the model, the schema, one write, and one template copy — zero readers,
zero tests. An org sets 16+ on a physical event and a 12-year-old can apply,
auto-approve, check in and bank verified hours. Nobody is told.

**CRIT 3/4 — frontend behavior the gate structurally cannot see.** The frontend
has ZERO tests and no runner, so `lint + build` is the entire gate. Two of three
`/discover` filters are inert — `defaultValue` only, no state, no handler — while
the page copy promises "Filter by category, commitment, and format" (verified by
me; format=Remote leaves the surviving card labelled "In-Person"). And `/hours`
renders "No hours logged yet. They log automatically once an event passes." when
its API calls FAIL — no `.catch` on the `Promise.all` — so a student with verified
hours sees an empty state that actively explains the emptiness away.

**The strategic finding is #6: the consent gate is UNENFORCEABLE and has now been
missed three times** — hours routes (2026-08-05), leaderboard (2026-08-06),
portfolio (today). Every catch was a human reading code. A route-walking test with
an explicit gated/ungated allowlist is the cheapest way to stop the fourth.

**My own slice — the repo tooling.** `servelocal-v2`'s secret gate lives in
`.git/hooks/pre-commit`, which is never committed. Nothing is tracked
(`git ls-files | grep hook` → empty), nothing documents it, and both failure
branches fail OPEN. **A fresh clone of the repo that feeds the public mirror has
no secret gate at all, silently.** The root record repo already solved exactly
this — hook tracked in `scripts/git-hooks/`, activation documented in CLAUDE.md.
v2 never got the same treatment.

Verified negatives worth recording, because they were actually checked rather than
assumed: full-history secret scan clean on both repos; the public mirror carries 0
backend files, no `security.md`, and the only non-empty env value anywhere is a
localhost URL; the record HTML twin re-rendered byte-identical, so the committed
twin is genuinely in sync; the renderer really does fail closed on a broken anchor
and the hook really does check its exit status; no ignore file contains `{`/`}`,
so ripgrep-backed search is not silently returning confident zeros.

**I CORRECTED ONE AUDITOR.** It reported guardian IP + user-agent as persisting
after account deletion, with "two documented invariants collide". Checked:
`auth.py:355-356` DOES scrub the user-row copy — the auditor could not observe it
live (no `consent_*` rows in the dev DB) and inferred from the writing lines. Only
the `audit_log.meta` duplicate survives, and the privacy policy already discloses
exactly that in two places ("we record the date/time, IP address, and browser of a
guardian's consent decision as proof of consent"; "entries in the append-only
security audit log are retained"). So it is disclosed deliberate retention, not a
silent leak, and the invariants do not collide — the policy resolves them.
Downgraded med → low. The IP is also the GUARDIAN's, an adult third party, not the
minor's. Residual soft edges, both posture rather than correctness: no stated
retention limit, and a full-precision IP where a truncated or salted-hashed one
would carry the same evidentiary weight.

The other auditor falsified one of its own attractive hypotheses and said so: Next
16 does NOT carry a stale inlined `NEXT_PUBLIC_*` value across builds, so the
missing `frontend/.dockerignore` is build-context bloat (860 MB) and not bundle
poisoning.

**DECISION (Evan, 2026-08-11): revoke is retroactive.** A guardian's revoke must
pull the minor off rosters and stop org emails; the guardian gets the option to
download all student data and then permanently delete the account; and on the org
side the student is greyed out everywhere, with a hover reading "This account has
been deactivated or deleted". Note the org keeps the ROW — greyed, not filtered —
which means the API has to expose a per-student status flag rather than removing
them from the list. Sized as three phases: (1) stop the bleeding — read-time
consent gate + broadcast/email exclusion, small; (2) org-side greying, medium;
(3) guardian-authenticated export + delete, medium-to-large, because export and
delete are student-authenticated today and the guardian holds only a manage token.

**OPEN QUESTION put to Evan, not assumed:** "permanently delete" collides with the
existing deletion design, which anonymizes and RETAINS applications and verified
hours so organizations keep their service record — and the privacy policy promises
that. Either (a) the guardian triggers the existing anonymize-and-retain flow, or
(b) a true hard delete, which removes the org's history, contradicts the policy
text and the append-only audit log, and needs both rewritten. Recommended (a).

**NOT DONE, deliberately.** No fixes were applied. The dev DB is off its documented
baseline by 3 rows the backend auditor created (user `audit-minor-a@example.com` +
2 audit_log) plus +1 hours and +3 audit_log for `alex@demo.com` from a browser
session against the `web` container, almost certainly mine from earlier
verification. Nothing was deleted; restoring the baseline rides along with whatever
fix batch is approved.

## 2026-08-11 ~18:18 CDT — audit fixes applied; two auditor fixes were wrong

**WHAT.** Fixed the audit's crits and highs. Every bug was reproduced before
being fixed and every fix verified by feeding it its own trigger. **Two of the
proposed fixes turned out to be wrong** and were corrected before landing.

### The two minors-safety crits

**Portfolio ignored consent on read.** `GET /portfolio/{id}` gated on role +
`portfolio_public` only; consent was checked at write time in `update_me` and
never again. So a guardian's revoke blocked new actions while the transcript —
name, org names, hour totals — stayed served. Reproduced first: the regression
test drives a REAL revoke through the emailed manage link and the red run printed
the exposure verbatim (`{'name': 'Robin R.', 'verified_hours': 6.0,
'hours_by_org': [{'org_name': 'Green Roots', ...}]}`). Fixed with
`consent_blocks(user)` plus the `is_active` guard its leaderboard sibling already
had. A second test pins that it does NOT over-gate: an adult's and a consented
minor's portfolios must stay public, and do.

This was my own incomplete fix. On 2026-08-06 I fixed the leaderboard and wrote a
comment on it reading "The consent gate covered `portfolio_public` but not this
board" — and never checked the third sibling, which exposes more than either.

**`min_age` was decorative.** Written, stored, rendered on the org's create form,
read by nothing — verified with two independent tools. A 13-year-old applying to a
`min_age: 16` build day got 201. Before fixing I checked the CHOKEPOINT rather
than patching where the auditor pointed: `redeem_checkin` and `auto_log` both
require an approved Application, so guarding `apply` covers every route to
attendance. Three tests: blocks 13, admits exactly-16 (no over-blocking), and
`min_age=None` still falls through to the consent gate rather than being
pre-empted.

Applications approved BEFORE the guard are grandfathered. That is a data
question, not a code one, and it is noted rather than silently handled.

### Stopping the fourth miss

The consent gate has now been missed three times — hours routes (2026-08-05),
leaderboard (2026-08-06), portfolio (today) — each caught only by a human reading
code. `tests/test_consent_gate_coverage.py` walks every route and requires each
write route to be either gated or in an explicit `CONSCIOUSLY_UNGATED` allowlist
with a stated reason. **Proven to actually catch:** adding a fake ungated
`POST /saved/audit-probe-fake-route` failed the test with that exact path named,
then the probe was reverted. It also carries `test_the_walker_actually_finds_routes`,
because FastAPI 0.139 hides included routes behind `_IncludedRouter.original_router`
and a naive walk yields ZERO — which would make the whole file pass by finding
nothing, the precise failure mode it exists to prevent.

### Frontend, all three verified against their real triggers

Two of three `/discover` filters were `defaultValue`-only — no state, no handler —
while the copy promised "Filter by category, commitment, and format". Wired
client-side (the list endpoint only accepts `category`/`query`, and the page
already held the full set), with chips and `aria-label`s. Browser-verified:
format=Remote took 3 cards to 1, commitment=Weekly to 0 (correct — all three seed
opportunities are One-time), clearing restored 3.

`/hours` rendered "No hours logged yet. They log automatically once an event
passes." when its API calls FAILED. Verified by forcing all three to 500 and
re-entering the route client-side: the empty-state lie is gone, the error branch
and Retry render, and the same run confirmed the student really does have hours.

A failed bookmark left the heart filled with the server recording nothing. Now
reverts and surfaces a message — verified by forcing `/saved/*` to fail and
watching the heart stay unfilled.

### TWO AUDITOR FIXES WERE WRONG

**The `<main>` landmark fix would have made things worse.** The auditor reported
"no `main` on any route", root cause `layout.tsx:51`, fix = change that `div` to
`main`. I applied it, then checked: `V1Shell` renders its own `nav` and `footer`
around the children, so a `main` at layout level swallows both and nests
navigation and contentinfo landmarks inside it. I then built a `PageMain`
component to branch on route type — and checking THAT found the real shape: all
10 non-`.v1` routes **already had their own `main`**, so PageMain produced two per
document (observed on `/hours`: `mains: 2`). Only the 19 `.v1` routes lacked one.
Final fix is one line in `V1Shell`; `PageMain` was deleted and the layout left
alone. Verified: `/dashboard` and `/hours` now each report exactly 1 `main`, 0
nested, 0 nav-inside-main.

**My own secret-gate finding was overstated.** I flagged the hook as failing open.
Reading it in full, the fail-open is deliberate and documented in the file — "a
gate that skips silently is a dead gate" — with a guard explaining that a missing
scanner would otherwise exit 1 and report a false "secret detected". Sound
reasoning, kept as-is. The valid part is narrower: the hook lived only in
`.git/hooks/`, which is never committed, so a fresh clone of the repo that feeds
the PUBLIC mirror had no gate at all. Now tracked in `scripts/git-hooks/` with
activation documented in CLAUDE.md.

**And my first test of that gate was a false alarm.** I staged
`AKIAIOSFODNN7EXAMPLE` and the commit went through, which looked like a dead gate.
It is the canonical AWS *example* key and the scanner allowlists placeholders by
design — the probe was wrong, not the gate. Retested with a realistic-format key:
commit BLOCKED, file named, value redacted. The bad probe did create a commit,
which was reset (mine, seconds old, unpushed).

### Also

`backend/docker-compose.yml` deleted — a dead duplicate referenced nowhere but
the regenerable graphify cache. Confirmed both of its reported defects myself
before removing it: it publishes Postgres on `0.0.0.0:5432` where the root compose
deliberately binds `127.0.0.1` ("audit 2026-07-13 #8"), and with no `ENVIRONMENT`
or `SECRET_KEY` it cannot boot past the guard. `frontend/.dockerignore` added —
build context measured at **860 MB → 5.78 kB**. Doc drift closed:
`DEPLOY_RAILWAY.md:255` still told an operator to read the audit log "as an
`ADMIN_EMAILS` account" (403, looks like a broken audit log); `NEXT_PUBLIC_APP_URL`
and `SUPPORT_EMAIL` were missing from `API_KEYS.md`, which calls itself the one
place listing every key. `testing.md` now states plainly that there is NO frontend
test runner and that lint+build is the whole gate — with today's three bugs as the
measured cost.

**VERIFY.** `pytest` **285 passed** (was 277) under `ENVIRONMENT=ci`; `ruff check`
and `ruff format --check` clean (98 files); `eslint .` exit 0; `tsc --noEmit`
clean; `next build` clean; every frontend fix driven in a real browser against the
Docker stack with zero console messages. Dev DB restored to its documented
baseline exactly (users=8, opportunities=3, hours=3, applications=2,
notifications=1, audit_log=37, reviews=1).

**NOT DONE.** The three revoke phases Evan specified (retroactive roster removal,
org-side greying with the deactivated tooltip, guardian-authenticated export +
delete) are a feature, not a fix, and are not started. Decision recorded
2026-08-11: guardian "permanent delete" is option **(a)** — the guardian triggers
the EXISTING anonymize-and-retain flow, so organizations keep their verified-service
record and the privacy policy needs no rewrite.

## 2026-08-11 ~22:57 CDT — revoke Phase 1: the kill switch is retroactive

**WHAT.** Evan's decision of the same day: a guardian's revoke must pull the minor
off rosters and stop organization email, not merely block their future actions.
This is Phase 1 of three — the part that stops the bleeding. Phases 2 (org-side
greying) and 3 (guardian-authenticated export + delete) are not started.

**The design decision, and why.** The org must still SEE the student afterwards
(greyed, per Evan), so deleting the Application — which is what a student's own
withdrawal does — was wrong here. The row has to survive.

What does the work is one field. Every consumer of roster membership keys on
`status == "approved"`: check-in eligibility (`checkin.covers_date`), hours
auto-log, the org's applicant and hours lists, thread access, review eligibility,
and spot counting in `enrollment.per_date_remaining`. Verified by enumerating all
of them before choosing. So moving the row to a new `withdrawn` status removes the
student from every one of those at once, at a single chokepoint, with no migration
— `Application.status` is a plain `String(20)` with no DB or schema constraint,
and nothing matches the new word, so every existing filter correctly excludes it.

`_withdraw_from_rosters` in `consent.py` also releases the spot and promotes the
waitlist for approved one-time events, reusing the exact rule from
`withdraw_application` rather than inventing a second one. The count of withdrawn
applications goes into the `consent_revoked` audit row, so the unwind is auditable
rather than inferable from missing rows.

**Stopping org email** is a separate guard, in `broadcast_to_applicants`. It skips
any recipient where `consent_blocks` is true, checked LIVE rather than by
filtering on application status — so it also covers a minor gated for any other
reason, and un-gates automatically if consent is restored. That matters because
`create_notification` sends email: a delivered broadcast is a real email to a
child whose guardian just withdrew permission.

**Three regression tests, red first.** Roster: the row is `withdrawn`, not
`approved`. Spots: an event's `spots_remaining` returns to its pre-application
value. Broadcast: a CONTROL first proves the broadcast reaches the student while
consented (`sent >= 1`), then after revoke asserts `sent == 0` AND that no Message
row was written — so a silent write with a suppressed email would still fail.

**Two mistakes of my own, both caught by running it.** The tests initially failed
with `NameError` because a `sed` import insertion silently didn't match a
two-name import line — the import was added properly by hand. And the broadcast
test asserted a `delivered` key when `BroadcastResult` actually returns `sent`;
the test was wrong, not the route.

**A stale justification my change created, fixed in the same commit.**
`org_applications` carried the comment "applying is consent-gated, so a minor's
application implies verified guardian consent". That stopped being true the moment
revoke started producing `withdrawn` rows: such a row can belong to a minor whose
guardian has since revoked, and the org still sees name and email. Keeping that
visible is Evan's deliberate call — the organization retains its record and the UI
marks the account instead of hiding it — but the comment now says so explicitly,
and warns that anything new must check `consent_blocks` rather than inferring live
consent from a row's existence.

`lib/types.ts` gains `"withdrawn"` to the status union.

**VERIFY.** `pytest` **288 passed** (was 285) under `ENVIRONMENT=ci`; `ruff check`
and `ruff format --check` clean (98 files); `tsc --noEmit` clean; `eslint .` exit
0. Not browser-verified — Phase 1 is backend-only and produces no UI change; the
`withdrawn` row renders as an unstyled status until Phase 2 gives it the greyed
treatment and the tooltip.

**STILL OPEN, by design.** Phase 2: the org-side greying and the "This account has
been deactivated or deleted" hover, which needs the API to expose a per-student
gated flag. Phase 3: guardian-authenticated export + delete — export and delete
are student-authenticated today and the guardian holds only a manage token, so
that is a new auth surface. Decided 2026-08-11: guardian "permanent delete" is
option (a), triggering the EXISTING anonymize-and-retain flow, so organizations
keep their verified-service record and the privacy policy needs no rewrite.

## 2026-08-12 ~17:25 CDT — fourth cold audit + fix pass: the revoke had two org-side holes, and hours were double-counted

**Trigger.** Evan ran `/audit` across every active project, then approved the fix
passes. Findings are in the audit report; this records what CHANGED and what was
proven. Every fix here was reproduced live against a running instance BEFORE being
fixed, and each carries a regression test written first.

**7 of 11 findings applied.**

- **3 (high) — verified hours were being double-counted.** `hours.py` hand-rolled
  `.astimezone(UTC)` three times where `core.occurrences` already guarded it — and
  that helper had exactly two callers, both internal, which is what made the
  asymmetry visible. SQLite returns NAIVE datetimes, so the bare conversion read
  stored UTC as host-local and pushed a 22:00Z shift onto the next calendar day:
  two Hours rows for one shift, **4.0 hours credited for 2.0 worked**, with
  `uq_hours_opp_user_date` unable to fire because the dates differed. The guard is
  now public (`as_utc` / `utc_date`) so the next module cannot hand-roll it.
  Test failed pre-fix with `[date(2026,8,12)] != [date(2026,8,11)]`. L1 — routine.
- **1 (high) — a revoked minor was still being emailed.** `create_notification`
  consulted `consent_blocks` in exactly ONE of nine call sites
  (`broadcast_to_applicants`). An org verifying a revoked minor's pending hours
  therefore mailed the child; the notification list read
  `[..., 'guardian_consent_revoked', 'hours_verified']`. Guarded at the chokepoint
  beside the existing `is_active` check, so every caller is covered at once and
  consent restoration un-gates automatically. Deliberately scoped to the EMAIL —
  the in-app row still lands, and whether a revoked minor's hours should be
  verifiable at all is a product decision, not this function's.
- **2 (high) — an org could undo the retroactive revoke.** `decide_application`
  had no precondition on `prev_status`, so `action: "approve"` moved a `withdrawn`
  row — the status revoke writes — back to `approved`: roster restored, spot
  consumed, org-thread access restored, child emailed. The org UI renders those
  buttons only for `pending`, so **the UI was the only thing enforcing it**. Now
  409. Verified by disabling the guard and re-running: `assert 200 == 409`.
- **6 (med)** — a whitespace-only `full_name` was truthy, stored, then `split()`
  to `[]`, so `parts[0]` raised IndexError and 500'd the PUBLIC unauthenticated
  portfolio endpoint. Rejected at the schema with strip + `min_length`;
  `first_last_initial` hardened as the backstop for pre-existing rows; and
  `portfolio._public_name` turned out to be a second copy of that same body, so it
  now routes through the shared helper rather than getting the same patch twice.
- **7 (med)** — `redeem_checkin` had no past-ness guard where `auto_log_attended`
  enforces one explicitly, so a pre-generated code banked instantly-`verified`
  hours weeks early.
- **5 (med)** — `APP_BASE_URL` was absent from `check_production_config`. It
  builds the guardian manage/revoke link — minted once, stored as a hash, no
  resend route — so a prod boot on the localhost default destroys that guardian's
  kill switch permanently AND silently, since `send_email` returns True and
  neither the log nor the audit branch fires. `SUPPORT_EMAIL`, with a far smaller
  blast radius, was already guarded.
- **4 (med) — the public mirror had neither safety net.** `servelocal-portfolio`,
  the repo that is actually published, had `core.hooksPath` unset, **zero**
  non-sample files in `.git/hooks`, and no `.gitignore` at all — while its own
  `README.md:31` tells readers "a scanner gates every commit". Both added.
  Live-fire tested: a realistic AWS key shape was caught and the commit blocked.

**Honest note.** My first notification test asserted that a minor with
`guardian_consent_status="approved"` should still be emailed. `consent.py:70`
unblocks only on `"verified"`. **The code was right and the test was wrong**; I
fixed the test.

**VERIFICATION.** `pytest -q` → **299 passed** (288 baseline + 11 new), exit 0.
Warnings-as-errors is already the default via `filterwarnings = ["error"]`, so
that IS the strict result. `ruff check` and `ruff format --check` both clean.

**Commits** `b5d68a9` (v2), `2ee8742` (public mirror). Not pushed.

**STILL OPEN.** Finding 8 — the rate limiter keys on `request.client.host`, which
behind Railway is the proxy IP, so all traffic shares one bucket and the auth cap
is 30/min GLOBALLY. Verdict UNENFORCEABLE; needs trusted `X-Forwarded-For` plus a
shared store, sized M. Findings 9 and 10 are doc drift (HANDOFF staleness, and
`_withdraw_from_rosters`'s docstring overstating its own reach — which finding 1
has now partly made true). E8, unbounded `checkin_codes` growth, also open.
M8 was NOT SWEPT in the audit: no database was reachable, so nothing was queried
and nothing is claimed clean there.

## 2026-08-12 ~21:15 CDT — revoke Phase 2: the org sees them, greyed, never hidden

**WHAT.** Phase 2 of Evan's revoke decision: on the organization's side the
student is greyed out with "This account has been deactivated or deleted" on
hover, rather than vanishing.

**Context I had to catch up on first.** A fourth cold audit ran on 2026-08-12
between Phase 1 and this, applying 7 of 11 findings — and one of them closed a
hole in the Phase 1 design I had shipped: `decide_application` had no precondition
on `prev_status`, so an org clicking Approve moved a `withdrawn` row straight back
to `approved`, undoing the kill switch (roster restored, spot consumed, child
emailed). The org UI only renders those buttons for `pending`, so the UI was the
only thing enforcing it. Now 409. I read that work before building on it rather
than assuming the tree was where I left it.

**Hidden vs marked.** Removing the applicant from the org's list would destroy
the organization's own record of who signed up, so the row and the identity stay
and the UI marks them. Verified explicitly in a test: after revoke the list still
returns exactly one row, still carrying the student's email.

**One flag, and it does not say why.** `account_inactive` in `core/consent.py`
returns `not is_active or consent_blocks(user)` — deleted, deactivated, and
guardian-revoked collapse into a single boolean. An organization has no business
learning that a particular family revoked consent, so everything downstream says
"deactivated or deleted" and nothing narrower. A test asserts the serialised row
contains neither "revok" nor "consent".

It is ONE named predicate rather than a fourth inlining of the same condition,
because that is this codebase's recurring failure mode: the consent gate has been
missed three separate times, and the 2026-08-12 audit found `create_notification`
consulting `consent_blocks` in one of its nine call sites. Recomputed per request,
so restoring consent un-greys automatically.

**VERIFIED LIVE, not just unit-tested.** Built a real scenario against the Docker
stack — register a 13-year-old, consent them the way the mailer would, apply,
revoke — then logged in as the seed organization and read the rendered DOM:
hover text exactly "This account has been deactivated or deleted"; opacity 0.55;
`line-through`; the sr-only sentence present because screen readers do not
announce `title` reliably, so greying alone would be colour-only information;
the email still shown; the reason not leaked; and a CONTROL confirming an active
student in the same table is untouched. `sr-only` was checked to actually reach
the bundle (Tailwind JIT only emits used classes) — the rule is in the built CSS.

**VERIFY.** `pytest` **301 passed** (299 baseline + 2); ruff check + format clean;
`tsc --noEmit` clean; `eslint .` exit 0; `next build` clean. Dev DB restored to its
documented baseline after the live scenario.

**STILL OPEN.** Phase 3: guardian-authenticated export + delete.

## 2026-08-12 ~21:40 CDT — revoke Phase 3: guardian-authenticated export + delete

**WHAT.** The last of Evan's three revoke phases: a guardian can download
everything held about their child and then erase the account, authenticated by
the long-lived manage token from the consent email.

**Option (a), as decided.** Erasure runs the EXISTING anonymize-in-place flow, not
a hard delete: the organization's verified-service record survives with no PII
attached, and the privacy policy — which already promises exactly this — needs no
rewrite. Confirmed live: applications stayed at 3 across the deletion.

**One implementation, two callers.** Rather than write a guardian copy of export
and erasure, both moved to `services/account.py` and the student-authenticated
routes now delegate. This matters more than usual here: the export is a column
ALLOWLIST and the erasure is a 17-field scrub, and a second hand-maintained copy
is precisely how a newly-added PII column starts leaking through one path while
the other stays clean.

**A near-miss worth recording.** I first wrote that service from memory and got the
allowlist materially wrong — omitting `supervisor_name`, `deny_note`, `appeal_note`
and `appealed` from hours, renaming review fields, inventing a `resolved_at`,
dropping the `messages` block entirely, and importing `SavedOpportunity` from the
wrong module. Had I not diffed it against the original before wiring anything, the
student's own export would have silently changed shape. The file now says it was
moved verbatim, because that is the only version anyone should trust.

The existing test that freezes the export key set failed on the move — importing
`_EXPORT_FIELDS` from its old home — which is the guard working exactly as
intended. Repointed, not weakened.

**The manage token outlives its authority, so it is bounded.** It never rotates,
so an email kept from when a child was 15 would still work at 25. `_guardian_subject`
requires the account to be an ACTIVE STUDENT UNDER 18, with age recomputed live
from `dob`, and returns the same flat 404 as an unknown token so a spent or
out-of-scope link never reveals it was once valid. A test ages the student to
adulthood and asserts both endpoints 404 and the account survives.

No password is required, unlike the student's own deletion — the guardian has no
account and no password to check. The token IS the credential, which is why the
bound above is load-bearing rather than cosmetic.

**The consent-gate coverage test caught the new route**, requiring a deliberate
classification before it would pass. That is the fourth time that contract has
been exercised and the first time a machine, not a person, did the catching.

**VERIFIED LIVE, not only unit-tested.** Registered a 13-year-old against the
Docker stack, consented, applied, then as the guardian: export returned all six
blocks with the child's application and **no** password hash, token hash, reset
token or consent IP; delete returned 200 and left
`deleted-<id>@deleted.invalid / active=false / "Deleted student"`; applications
held at 3; replaying the delete link gave 404; the child's login gave 401.

**VERIFY.** `pytest` **306 passed** (301 → 306); ruff check + format clean (102
files). Dev DB restored to its documented baseline.

**All three revoke phases are now done.**

## 2026-08-12 ~21:25 CDT — landing-check: the new status had no UI, and I shipped a wrong number

**WHAT.** Ran `/landing-check` over the four-commit batch (`6762837`, `4ddaaef`,
`5303931`, `18a0c6c`) with a fresh agent given the ARTIFACTS only — commit
messages, diffs, record entries — and explicitly not my account of the work.
Verdict: FIX FIRST. Two real defects and four unmet doc obligations, all closed
here.

**DEFECT 1 — the `withdrawn` status had zero rendering anywhere in the frontend.**
Verified myself with two searches: it appeared in the whole `frontend/` tree only
in `lib/types.ts`. `STATUS_LABEL` and `STATUS_PILL` on `/applications` both fall
back, so a revoked student loading their own page saw the raw word "withdrawn" in
an amber, "pending"-coloured pill. This is live, not theoretical: `/applications/my`
carries no `require_consent` — deliberately, a gated student must still be able to
read their own state — so a revoked minor reaches it. Also missing: an
`sp-withdrawn` rule in BOTH css systems (the org table builds its class as
`sp-${status}`), and a `STATUS_MESSAGE` entry on the opportunity detail page.
Added all four; the pill is neutral grey because a withdrawal is neither an error
nor an approval. Confirmed `.sp-withdrawn` reaches the built bundle.

I introduced this status on 2026-08-11 and updated exactly one frontend file —
the type union — which type-checks perfectly while rendering nothing.

**DEFECT 2 — I shipped a number I had not counted.** The `account_inactive`
docstring in `core/consent.py` said `create_notification` "consulted
`consent_blocks` in one of its nine call sites". The numerator was right; the
denominator was carried over verbatim from another session's commit message
without being verified. The real figure is **13 invocations across 6 modules**.
Corrected in place, and the correction says where the wrong number came from,
because "never invent data" plainly extends to not relaying someone else's
unverified figure inside a docstring warning about exactly this class of error.

**Four doc obligations from CLAUDE.md §Definition-of-done, unmet and now met.**
`HANDOFF.md` had not been touched since 2026-08-08 — no block for the three-phase
feature, no workstream row, and a stale test count in what read as current state.
`INDEX.md`, `features.md` and `testing.md` still said 277 tests (306). Most
importantly `security.md` — the bin the public mirror deliberately withholds, and
therefore the security record of record — had nothing about the portfolio CRIT,
`account_inactive`, or the new unauthenticated guardian export/delete surface;
that is now written up, including that the manage token never rotates and why
`_guardian_subject` must bound it. And the whole-project record had no entry for
any of this: II.24 appended, TOC anchor added, three timeline rows, HTML twin
regenerated with `broken: 0`.

**What the landing-check got right that I could not have.** It re-derived every
claim from disk rather than from my summary, which is the whole design: it
confirmed the hook edit landed on the copy `core.hooksPath` actually executes,
that all 16 changed backend files md5-match the running container, and that the
"all 10 non-`.v1` routes already had a `<main>`" claim was true — while
independently finding the two things above. The one MISLANDED row it reported is
the public mirror, stale since 2026-08-11 and one commit ahead of its own origin.

**VERIFY.** `pytest` **306 passed**; ruff check + format clean (102 files);
`eslint .` exit 0; `tsc --noEmit` clean; `next build` clean; `.sp-withdrawn`
present in the emitted CSS; record HTML twin `broken: 0`.

**STILL OPEN.** Public mirror not re-synced (outward-facing; needs Evan's word).
`min_age` grandfathering. Rate limiter keys on the proxy IP (sized M). Two doc
findings from the 2026-08-12 audit.

## 2026-08-12 ~21:45 CDT — the rate limiter stopped keying on the proxy

**WHAT.** Finding 8 from the 2026-08-12 cold audit, the last substantive item
open from it. Behind a reverse proxy `request.client.host` is the PROXY, so every
user shared one bucket: the auth cap was effectively 30/min GLOBALLY, one abusive
client could lock out the entire user base, and the per-client limit did nothing.

**The obvious fix is worse than the bug, and that shaped the design.**
`X-Forwarded-For` is attacker-supplied. Reading it unconditionally lets any client
vary the header per request, mint a fresh bucket each time, and bypass the limiter
completely — trading a shared-bucket DoS for no limiter at all. So the header is
ignored unless the deploy states its topology.

`TRUSTED_PROXY_HOPS` (default **0** — trust nothing) is that statement. Each proxy
APPENDS the address it received from, so with N trusted hops the real client is
the Nth entry from the right. `client_ip()` returns the socket address whenever
that cannot be justified: hops unset, header absent, chain SHORTER than declared
(the request did not traverse the path we were told about), or an entry that is
not a parseable IP.

**Five tests, written first and all red before the change.** The most important is
`test_forwarded_header_is_IGNORED_by_default`, which sends a different spoofed
client per request and asserts the limiter still fires — that is the property a
careless implementation would destroy. Then: two real clients behind one declared
proxy get separate budgets (and a control asserting client B is NOT punished for
client A's burst — the actual user-facing symptom); a 2-hop chain keys on the
client rather than the rotating CDN node; malformed headers (`not-an-ip`, empty,
whitespace, a SQL-injection string) all fall back to one bucket instead of minting
four; and a chain shorter than declared does not IndexError or get trusted.

**Documented in all three places an operator actually looks**, because the failure
is silent — nothing errors, the limiter just stops working. `backend/.env.example`,
`DEPLOY.md` §post-deploy, and `DEPLOY_RAILWAY.md`'s env list, which now says
plainly that Railway terminates TLS at its own proxy so without this the whole
user base shares one bucket and the limiter is decorative. `DEPLOY.md` also loses
the line under "Known production hardening still owed" that asked for exactly
this — leaving a completed item on a TODO list is how the next reader re-does it.
`security.md` corrected: it had said "trust validated `X-Forwarded-For` at M11".

**NOT fixed, and still true:** the limiter remains in-memory and single-process, so
a multi-replica deploy still multiplies every limit by the replica count. That is
the Redis half of finding 8 and it is untouched — this change fixes WHO gets
counted, not WHERE the count lives.

**VERIFY.** `pytest` **311 passed** (306 → 311); ruff check + format clean (102
files).

## 2026-08-12 ~21:56 CDT — the two doc findings closed, and one of them was a lie about what the kill switch reaches

**WHAT.** Findings 9 and 10 from the 2026-08-12 cold audit — the last two items
that audit left open, both classed "doc drift". Doc-only change: no behaviour
moved, no test was added or altered.

**Finding 10 was not staleness, it was a false claim, and it had been copied into
four places.** `_withdraw_from_rosters`'s docstring said the `withdrawn` status
change removes the student from "check-in eligibility, hours auto-log, **the
org's applicant and hours lists**, thread access, review eligibility and spot
counting ... because every one of those filters on `status == "approved"`".

I checked all seven claims against the code rather than re-reading the sentence:

| claimed surface | filters on `Application.status == "approved"`? |
|---|---|
| check-in eligibility (`services/checkin.covers_date:60`) | yes |
| hours auto-log (`routes/hours.py:57`) | yes |
| hours self-report (`routes/hours.py:129`) | yes — and unlisted |
| org-thread access (`routes/messages.py:47`) | yes |
| review eligibility (`routes/reviews.py:87`) | yes |
| per-date spot counting (`services/enrollment.py:22`) | yes |
| **the org's applicant list** (`GET /applications/org`) | **no — no status filter at all** |
| **the org's hours list** (org branch of `GET /hours`) | **no — keys on Hours rows** |

The two false ones are not near-misses, they are the **opposite** of the shipped
design. Phase 2 (2026-08-12, the same day) deliberately keeps the withdrawn row
in the org's list, greyed via `account_inactive`, because the row is the
organization's record. So the docstring described Phase 1 as undoing exactly what
Phase 2 was built to preserve — and `security.md` carried both statements two
bullets apart, contradicting itself on the same page. The org's hours list is a
separate mistake: hours logged before the revoke stay in the verify queue, which
the audit's finding 1 note already said out loud ("whether a revoked minor's
pending hours should be verifiable at all is a product decision").

Corrected in the three LIVE copies — the docstring
(`api/routes/consent.py:_withdraw_from_rosters`), `HANDOFF.md`, and the
`security.md` bin — each stating what it does NOT reach and why that is
deliberate, so the next reader doesn't "fix" Phase 2 to match the sentence. The
two record copies (this file 2026-08-11, whole-project record II.24) are
append-only and stay wrong on the page; that is what append-only means.

**Finding 9 — HANDOFF staleness — had grown a second layer since the audit named
it.** The 2026-08-12 landing-check refreshed HANDOFF at ~21:25, and then the
rate-limiter commit (`92282c6`, ~21:45) went in and left it stale again within
the hour. HANDOFF had no mention of the limiter fix, no `TRUSTED_PROXY_HOPS`, and
a 306 test count presented as current.

Added a block for `92282c6`, amended the M9 workstream row, marked the older
"306 pytest green" as *at that point*, and gave the guardian-revoke row its
missing `a45be67`. The counts in `INDEX.md`, `features.md` and `testing.md`
(306 → 311) moved with it — the same three bins that were 277-stale a day ago.

**The gap that finding 9 didn't name.** `TRUSTED_PROXY_HOPS` was documented in
`.env.example`, `DEPLOY.md` and `DEPLOY_RAILWAY.md` — but not in `docs/API_KEYS.md`,
which calls itself the single registry of everything Evan must provide. That file
already carries two scars from exactly this (`SUPPORT_EMAIL` and
`NEXT_PUBLIC_APP_URL` were both missing from the table while sitting elsewhere in
the file), and this variable is worse than either: getting it wrong raises no
error, logs nothing, and just quietly makes the rate limiter share one bucket
across the entire user base. Added to the table and to HANDOFF's BLOCKED-ON-EVAN
list, with the Railway value (`1`) and the one-replica constraint stated.

**Also verified while in here, unasked: the `min_age` grandfathering item (open
list #4) is smaller than it reads.** `min_age` is write-once — `POST /opportunities`
sets it and there is no field-editing route (only `/featured` and `/active`
PATCHes), so an org cannot raise the floor under students who already applied.
Every approved row traces back to a guarded `apply` (waitlist promotion,
check-in and auto-log all require an existing approved application). The affected
set is therefore exactly "applications created before `6762837`" — which exist
only in this box's dev Postgres, because nothing is deployed and production
starts empty. **Not asserting it closed**: that is Evan's call, and the honest
framing is that the gap reopens the day an opportunity-edit route lands.

**VERIFY.** `pytest -q` **311 passed** (unchanged — no test touched); `ruff check`
clean; `ruff format --check` 102 files already formatted. Frontend untouched.

**STILL OPEN.** `TRUSTED_PROXY_HOPS=1` on the real deploy. Redis for the limiter
(in-memory, one replica only). `min_age` grandfathering (above — Evan's call).
Audit-log retention limit and full-precision consent IP, both disclosed in the
policy and both posture decisions. Public mirror still not re-synced since
2026-08-11 (outward-facing; needs Evan's word).

## 2026-08-12 ~22:03 CDT — pushed, CI green, and the public mirror is no longer stale

**WHAT.** Evan authorised the two outward-facing steps that had been sitting
open: push and mirror sync. Both done; this records the real results.

**Push.** `a45be67..fe06150` on `main` — two commits: `92282c6` (the rate-limiter
fix) and `fe06150` (the findings 9/10 doc closeout).

**CI green, checked at the STEP level, not the badge.** Run `31662557611`, three
jobs, all success — and specifically both blocking dependency scans, which are
the two that were `continue-on-error` until 2026-08-08:

```
Backend (pytest)             :: Lint (ruff) / Format check (ruff)     :: success
Backend (pytest)             :: Dependency CVE scan (pip-audit)       :: success
Backend (pytest)             :: Run tests                             :: success
Frontend (lint + build)      :: Lint / Dependency CVE scan (npm audit):: success
Frontend (lint + build)      :: Build                                 :: success
Backend (pytest on Postgres) :: Run tests against Postgres            :: success
```

Worth stating plainly because this repo has already shipped "tests green" while
CI was red for three consecutive pushes (2026-08-08). A local run is evidence
about this box, not about the pipeline.

**The mirror was CONTENT-stale, not git-stale — which is the harder kind to
notice.** `servelocal-portfolio` was clean against its own origin at `fad6b48`
with nothing unpushed; what was old was the *files*, because
`scripts/sync_portfolio.py` regenerates them and hadn't been run since
2026-08-11. `git status` on the mirror said everything was fine. That is exactly
the MISLANDED row the 2026-08-12 landing-check reported, and it stayed open for a
day because the signal a person checks (clean tree) was green the whole time.

Synced, reviewed, then published: `fad6b48..182ac9e`. 129 files staged-or-tracked,
`security.md` confirmed absent, secret guard clean. Six files actually changed:
`HANDOFF.md`, this record, the whole-project record `.md` + its HTML twin, and the
`INDEX`/`features` bins. `testing.md` moved 306 → 311 locally but is NOT in
`PUBLIC_BINS`, so it doesn't appear in that diff — not an omission.

**What I checked before publishing rather than trusting the guard alone.** The
guard scans for secret VALUES; it has nothing to say about whether the NARRATIVE
should be public. The synced docs reference `docs/API_KEYS.md`,
`DEPLOY_RAILWAY.md` and `security.md` by filename eight times while none of those
files ship — that is the established shape of this mirror (the record has always
described the audits, including the CRITs), and a filename is not its contents.
The new text names env-var names only, which the sync script's own contract
allows.

**VERIFY.** Push confirmed by ref update on both remotes; CI conclusion `success`
across three jobs and eight relevant steps; mirror secret guard clean. Root record
repo untouched by both pushes — it stays LOCAL by Evan's decision.

**STILL OPEN, unchanged by this.** `TRUSTED_PROXY_HOPS=1` on the real deploy.
Redis for the limiter (in-memory, one replica only). `min_age` grandfathering
(Evan's call — see the entry above). Audit-log retention limit and full-precision
consent IP.

## 2026-08-13 ~01:04 CDT — the open non-account list, and the limiter fix that had swept only itself

**WHAT.** Evan: "do all outstanding work, using /landing-check along the way."
Three of the four open non-account items closed; the fourth (Redis) is genuinely
blocked and is reported, not worked around.

---

### 1. `min_age` grandfathering — closed by construction, not by a backfill

Traced the write path rather than assuming the handoff's framing ("a data
question, real before launch"). `min_age` is **write-once**: `POST /opportunities`
sets it and no other route does. `opportunities.py` has three PATCHes —
`/featured` and `/active`, single-field toggles on the Opportunity, and
`/exclude-date`, which writes `Application.excluded_dates` and never touches the
listing — and creating from a template is a plain
opportunity POST. So an org cannot raise the floor under people who already
applied, and every approved row traces back to a guarded `apply` (waitlist
promotion, check-in and auto-log all require an existing approved Application).

The affected set is therefore exactly "applications created before `6762837`",
which exist only in this box's dev Postgres — nothing is deployed and production
starts empty. **A backfill would have been code with no rows to act on.**

What it got instead is `tests/test_min_age_is_write_once.py`, which asserts on the
API surface (every route's body fields) rather than on source text, so it also
catches a generic field-updater that an attribute-assignment scan would miss. Two
tests: the invariant, and a guard-the-guard that fails if body introspection stops
finding `min_age` on the create route — otherwise the file passes by finding
nothing, which is the failure mode, not the success state. Proved it can fail by
emptying the allowlist, which reported `POST /opportunities` as an unexpected
mutator.

**Honest limit, written into the docstring:** it cannot see a migration, a
management script or an admin tool writing the column outside the HTTP API.

### 2. Audit-log retention — the one sanctioned exception to append-only

The table records security events for every user, minors included, and had no
stated limit: it grew forever, and the privacy policy could only say entries are
"retained". `AUDIT_LOG_RETENTION_DAYS` (default **365**, floor 30) is the limit;
`purge_expired_audit_log` enforces it; `python -m scripts.purge_audit_log` is the
entry point; and the policy now says **12 months** in all three places it
discusses the log.

Three design calls worth keeping:

- **Age only.** There is deliberately no way to delete a particular row. Deleting
  by age is what lets an append-only table have a retention policy without giving
  anyone a way to remove the one embarrassing entry.
- **Nothing purges automatically.** There is no scheduler in this stack, and a
  background job quietly deleting accountability records is the wrong default. The
  monthly cron is a launch item in HANDOFF and DEPLOY_RAILWAY §Scheduled jobs.
  **A stated retention limit nobody enforces is worse than stating none** — so
  this is one item, not "ship the number now, wire the job later".
- **Compared through `as_utc`**, never a bare `.astimezone(UTC)`. SQLite returns
  naive datetimes and that exact mistake double-counted verified hours on
  2026-08-12.

Six tests. The one that mattered was `test_it_does_not_commit_...`, which **passed
for the wrong reason on the first run**: the seeded rows were only flushed, so the
rollback discarded the inserts too and "0 == 0" looked like proof. Committing the
seed first makes the assertion mean what it says.

**Ran the CLI for real**, against a scratch SQLite DB, because Docker is down on
this box and the dev Postgres was unreachable:

```
seeded 5 rows; 3 are older than 365 days
dry-run: 3 of 5 rows are older than 365 days
refusing a window under 30 days — see AUDIT_LOG_RETENTION_DAYS   (exit 2)
purged 3 of 5 rows older than 365 days
purged 0 of 2 rows older than 365 days          <- rerun, idempotent
```

### 3. The consent-IP item was not a posture question. The value was wrong.

Listed as "consent IP is stored at full precision" — i.e. should it be truncated.
Reading the write path first turned up something else: `guardian_consent_ip` was
written from `request.client.host`, **the same expression the rate limiter was
fixed for on 2026-08-12**. Behind Railway's proxy that is the proxy, so every
guardian decision on the platform would record the same host address. The column
exists to evidence that a *specific* guardian approved a *specific* child; it
would have proved nothing, with nothing erroring.

`92282c6` introduced `client_ip()` and landed on the limiter's own call site.
A grep for the other callers — the standing "fix the chokepoint, then check its
siblings" rule — found **three** still reading the socket directly: consent
approve/decline, consent revoke, and the `remoteip` handed to Cloudflare
Turnstile, where feeding the proxy inverts the signal the parameter exists for.
I had described that fix approvingly in this file six hours earlier without
checking its other callers.

All three now go through a new `client_ip_or_none()`, a small wrapper that
returns `None` instead of `client_ip`'s `"unknown"` sentinel: a stable made-up key
is right for a rate-limit bucket and wrong for a record, where storing the literal
string "unknown" as proof of consent is worse than storing nothing. It lives beside
`client_ip` in `core/rate_limit.py` — the module name is now slightly narrow, and
renaming it would churn the middleware and its tests for no behaviour change.

**Proved red against the pre-fix behaviour** with a temporary test that patched the
routes' resolver back to the old expression (deleted after; never committed):

```
PRE-FIX stored: 'testclient' (should have been '203.0.113.7')
PRE-FIX Turnstile saw: ['testclient'] (should have been ['203.0.113.7'])
```

Four tests pin it, including `test_a_spoofed_header_is_IGNORED_when_no_proxy_is_declared`
— the property a careless fix destroys, since believing `X-Forwarded-For` by
default would let anyone write any address into a minor's parental-consent record.

**On the original question (truncate or not): recommend KEEP full precision, and
this is Evan's to ratify.** The column's whole purpose is evidentiary; a /24 still
identifies a household while destroying most of the evidence value. It is
disclosed in the policy, cleared on account erasure, and — via item 2 — its copies
inside `audit_log.meta` now age out at 12 months instead of never, which is the
part that was actually unbounded.

### 4. Redis for the limiter — BLOCKED, not attempted

`docker version` reports the daemon is not running on this box, so there is no
Redis to build against and no way to verify a shared-store limiter end to end. It
also means a new dependency the PRD requires a task to name. Reported as blocked
rather than shipped unverified; the in-memory limiter and the one-replica
constraint are unchanged and still documented in three places.

---

**VERIFY.** `ENVIRONMENT=ci pytest -q` → **323 passed** (311 → 323: +2 min_age,
+6 retention, +4 recorded-IP). `ruff check` clean; `ruff format --check` 106 files
(two of mine needed reformatting first). Frontend: `tsc --noEmit` clean,
`eslint .` exit 0 (12 pre-existing warnings, no errors), `next build` clean.
**Not browser-verified** — Docker is down, so the stack could not be driven; the
only frontend change is prose in `privacy/page.tsx`.

### 5. /landing-check on the batch — six findings, all closed here

Evan asked for it explicitly. A fresh agent got the artifacts only — `git diff
HEAD`, the untracked file list, this entry and the HANDOFF block — and none of my
account. Verdict **FIX FIRST**. Everything technical it re-derived came back TRUE
(323 = 311+2+6+4 counted per file; exactly three `request.client.host` sites at
HEAD; the CLI transcript reproduced line for line from its own scratch DB), and
then it found six things I had not:

1. **The whole-project record and its HTML twin were never touched.** Required by
   CLAUDE.md §Definition-of-done for medium-or-larger changes, and this is 17
   files, a new knob, a new CLI, a new service function and 12 tests. Now II.26,
   TOC anchor, two timeline rows, twin regenerated `broken: 0`.
2. **"`opportunities.py` exposes only `/featured` and `/active` PATCHes" is false** —
   there is a third, `/exclude-date`. The conclusion survives (it writes
   `Application.excluded_dates`, never the listing) but the enumeration was wrong
   in **three** places: this entry, HANDOFF, and the source comment at
   `opportunities.py:268` that the whole grandfathering argument rests on. All
   corrected. I had written "there is no field-editing route" from reading a
   `@router` list and generalising, which is exactly the class of error this batch
   was supposed to be about.
3. **`models/audit.py`'s docstring still said "never updated or deleted"** — I
   documented the retention exception on the service and not on the model, which
   is where a reader lands to ask whether the table is append-only.
4. Two stale "(311 now)" counts in HANDOFF, and `API_KEYS.md`'s "Last updated"
   header not bumped though a row dated 2026-08-13 sits below it.
5. **"12 months in both places" — the policy mentions the audit log in three**,
   and the third (the security bullet at line 56) still carried no period. Fixed
   there rather than by softening the sentence.
6. The pasted CLI block had a hyphen where the source emits an em dash — the
   Windows console mangles it, and I had tidied rather than marked it. Restored.

It also handed off, correctly, two things that are not this skill's job: five
`config.py` settings missing from `API_KEYS.md` (pre-existing, /audit), and the
batching loop's termination condition (/code-review).

**VERIFY after the fixes.** `ENVIRONMENT=ci pytest -q` → **323 passed**;
`ruff check` clean; `ruff format --check` 106 files; `tsc --noEmit` clean;
`eslint .` exit 0; `next build` clean; record HTML twin `broken: 0`.

**STILL OPEN.** `TRUSTED_PROXY_HOPS=1` and the monthly purge cron, both on the
real deploy. Redis (above). Consent-IP precision: recommended KEEP, awaiting
Evan's ratification. Everything else on the launch list is account-shaped.

## 2026-08-19 ~16:30 CDT — asked to push and re-sync; both had already happened

**WHAT.** Evan asked for "push + resync" on the 2026-08-13 batch. Six days had
passed since that session, and both were already done by work in between. Nothing
was pushed or synced here. Recording it because a request that turns out to be
already-satisfied is worth a line — the next reader should not go looking for a
push that has no commit of its own.

**Verified by CONTENT, not by commit subject**, which is the whole point of
checking at all:

- v2 `main` == `origin/main` at `01d1a7f`, clean tree. My `500a0da` and `5c8e36c`
  are both on the remote.
- The mirror is at `46145e8` ("the privacy policy the public mirror was one edit
  behind on, plus II.26-II.28"), clean and level with its own origin.
- `python scripts/sync_portfolio.py --check` → `IN SYNC - 119 files, mirror
  matches this tree (CRLF-normalized)`, exit 0. That flag did not exist on
  2026-08-13; `01d1a7f` added it, which is why this check was one command instead
  of a diff by hand.
- Spot-checked the mirror directly rather than trusting the above: all **3**
  "12 months" mentions present in its `privacy/page.tsx`, `## II.26` present in
  its copy of the whole-project record, and the two deliberate withholds still
  hold - no `backend/`, no `security.md`.

**Observed, not touched** (neither is mine, and neither is this session's work):

- `01d1a7f` appended to the whole-project record (II.28) but not to this file, so
  the `--check`/secret-guard change has no v2 record entry. Flagged, not
  backfilled: writing an entry for work I did not do would be inventing detail.
- The root repo has an uncommitted `scripts/git-hooks/pre-commit` and an untracked
  `docs/regenerate-record-html.prompt.md`. Both predate this session.

**STILL OPEN.** Unchanged from 2026-08-13: `TRUSTED_PROXY_HOPS=1` and the monthly
purge cron on the real deploy; Redis for the limiter; consent-IP precision
(recommended KEEP, awaiting Evan). Everything else is account-shaped.

## 2026-08-19 ~16:40 CDT — main went red on a docs-only push; the lockfile was one patch behind

**WHAT.** The record-entry push above turned CI red. The commit touched exactly
one markdown file, so the failure was not caused by it: GHSA-2v37-7h3g-55p8
(`nanoid` <3.3.18, HIGH) was published in the six days since the last push, and
the blocking `npm audit` step did its job on the next run that happened along.

**The interesting part is why local looked clean.** `npm ls nanoid` on this box
reported **3.3.18** — the patched version — while `package-lock.json` pinned
**3.3.17**. CI runs `npm ci`, which installs from the lockfile, so CI was
installing the vulnerable copy that no longer existed locally. A developer
checking by hand would have seen a green tree and concluded the advisory did not
apply here.

**Fix: a lockfile bump, nothing else.** `nanoid` is transitive (`postcss@8.5.26`
→ `nanoid`), so `package.json` is untouched and the diff is three lines — version,
tarball URL, integrity hash. No API surface moved.

**VERIFY.** `npm audit --audit-level=high` exit 0 (was 1, "1 high severity
vulnerability"); `npm run lint` 0; `npm run build` 0; `npx tsc --noEmit` 0.

**Why this was fixed rather than reported and left.** This repo has already shipped
"tests green" while CI was red for three consecutive pushes (2026-08-08). Watching
a run go red after pushing it and then walking away is the same failure with a
shorter fuse. The dependency-CVE scans were deliberately made blocking on
2026-08-08 so that a red main means stop.

## 2026-08-19 ~17:06 CDT — cold audit of the memory docs: 13 findings, and the worst one was mine from six days earlier

**WHAT.** Evan ran `/audit` scoped to "the memory docs". Taken as both stores: the
project's 12 `codebase-memory` bins and the 13-file session memory at
`~/.claude/projects/D--ClaudeCode-ServeLocal/memory/`. This session had edited four
of those bins, so per the skill's step 0 the sweep went to a **cold agent** with the
paths, the ground-truth sources and the method — and none of my account of them.
All 13 findings and 5 edge cases applied.

**The defects clustered in one shape:** a fact restated in several documents and
then corrected in only some of them.

### The two crits

**F1 — `APP_BASE_URL` would have crash-looped the first Railway deploy.** The boot
guard is **7 checks over 6 variables**; `security.md` and `tooling.md` both said
"Six checks over five" and enumerated five, and `DEPLOY_RAILWAY.md` filed
`APP_BASE_URL` under "*Should* set for a real launch" while the four real blockers
carry "Now a boot blocker". Its authoritative table omitted the row entirely.
`b5d68a9` added the check on 2026-08-12 and moved no doc.

I re-derived this rather than relaying it, because the claim-check flagged that I
had quoted a line number from the auditor's report without deriving it:
`problems.append` fires at `config.py:145,148,150,154,157,161,169` — seven, over six
variables, the two `SECRET_KEY` checks being `if/elif`. Corrected in both bins, both
runbooks and the table.

**F2 — `INDEX.md` still asserted an invariant I broke myself on 2026-08-13.**
"The audit log is append-only — rows are never updated or deleted." The retention
purge I added that day deliberately deletes by age. I updated `security.md` and the
model docstring and missed the cross-bin invariant list — and `security.md`'s older
§Audit log section still said "never update/delete rows" too, contradicting the
retention section two headings above it.

**That is the third time this session that a fix swept its own call site and not its
siblings** — the limiter fix on 2026-08-12, the recorded-IP fix it turned out to be
a third of, and now this. The landing-check caught the second. This one it was not
scoped to see, and only a cold reader looking at the invariant list would.

### The rest, in one line each

- **F3** — `security.md`'s audit-event register named 14 actions and called them
  "All 12". The real figure is **24**, which I derived with an `ast` walk rather
  than a grep: two actions reach `append_audit` only as `anonymize_account`'s
  `action=` parameter, so every regex undercounts. (The auditor's own count of 23
  missed exactly those two — I checked before writing the number down.) The count is
  now omitted from the prose so it cannot desync again.
- **F4** — `project-codebase-memory.md` enumerated 6 bins; there are 11 + INDEX. Its
  entire job is telling a session what the bins are. Now points at `INDEX.md`.
- **F5/E2** — the session memory said "211 tests, migrations→0022, ALL remaining work
  BLOCKED-ON-EVAN" for five weeks, and it is injected into **every** session's
  context. It also disagreed with itself, carrying two different test counts. Now
  pointer-only, with an explicit ban on counts written into it.
- **F6** — `INDEX.md`'s per-bin dates disagreed with 5 of 11 bins' own headers, in
  both directions. Dates dropped from INDEX (the bin header is the single copy); four
  stale headers bumped.
- **F7** — `security.md` still called `min_age` grandfathering "still open"; closed
  2026-08-13.
- **F8/E3** — the **public mirror's** INDEX advertised 7 bins the mirror does not
  ship, on the repo whose whole point is showing engineering process. Fixed in
  `sync_portfolio.py` by filtering the copied INDEX rather than by publishing five
  more bins — what the mirror publishes is Evan's call, not a side effect of a doc fix.
- **F9** — 6 of 7 cross-bin invariants had **no mechanical enforcement at all**.
- **F10–F13** — two dead cross-references, a memory pointing at a CLAUDE.md section
  that no longer exists (and listing 6 of 39 skills as "the current set"), "Next.js
  15" (it is 16.3.0), and no bin documenting the blocking ruff CI gate.

### What got a guard rather than a correction

Text fixes rot again. Four of these got mechanism instead:

- **E1** `test_boot_guard_check_count_matches_the_docs` — fails when a check is added
  or removed, naming the three documents to update. This is exactly how F1 happened:
  nothing — not the pre-commit hook, not CI — reads `.claude/codebase-memory/`.
- **F9** `test_invariants_are_enforced.py` — three static guards: `User.plan` written
  only in the webhook, no stored is-minor flag, no secret field on any response
  schema. **Two invariants remain UNENFORCEABLE and say so in the file** ("students
  free forever" needs a behavioural test per route; "never edit an applied revision"
  is unknowable from inside the repo). An audit that turns "unchecked" into "looks
  checked" is worse than one that changes nothing.
- **E4** `memory/_validate.py` — frontmatter, type enum, slug/name match, wikilink
  resolution, and the MEMORY.md-to-files bijection. The store passed; the point is
  that "passed" was an unverified claim until the auditor spent an hour on it by hand.

**Every guard was fed its own trigger, not just read.** The boot-guard test against a
simulated eighth check → fired. Each F9 guard against a planted violation in a temp
tree (a `user.plan = 'pro'` outside billing, an `is_minor` attribute, a
`reset_token_hash` on a schema) → all three fired. The validator against a malformed,
unlisted memory → caught all four defects at once.

**The method note worth keeping.** The auditor's cross-check on the audit-action count
produced *agreement between two grep implementations on the same wrong number*. Two
tools agreeing is not evidence; reading the call sites is. That is why F3's number
came from an AST walk that resolves the one dynamic call site, and why the register
now tells the next reader to derive it rather than trust the list.

**VERIFY.** `ENVIRONMENT=ci pytest -q` → **327 passed** (323 → 327: +1 boot-guard
count, +3 invariant guards). `ruff check` clean; `ruff format --check` 107 files.
Frontend `tsc --noEmit` 0, `eslint .` 0, `next build` 0. `_validate.py` exit 0 on the
real store, exit 1 with 4 findings on a planted-defect copy. `sync_portfolio.py
--check` correctly reports the two changed bins.

**NOT swept, stated rather than implied.** No live CVE run (pip-audit is absent from
the venv and installing was off-limits). Docker was down, so the Postgres CI job and
every browser-verified claim in `gotchas.md`/`testing.md` remain unverified — not
clean. Two findings were handed off rather than half-done: five `config.py` settings
missing from `docs/API_KEYS.md` (pre-existing, `/audit`), and the retention purge's
batching termination condition (`/code-review`).

## 2026-08-19 ~17:16 CDT — M11 execution plan appended to the PRD; three "blockers" were stale

**WHAT.** Evan asked for the launch plan (drafted the prompt before, read-only) to
be added to the PRD. Appended per the roadmap's own rules — ADD by appending,
nothing deleted: a dated status block up top, an `#### M11 execution plan`
subsection under M11 (sequencing only, no scope change), and one honest §3 sync.

**The plan's real content is that the blocked list overstated what is blocked.**
Verified against code before writing, each with a live line reference:

- the "decline bricks a minor's account with NO mechanism" warning predates
  `POST /consent/admin/{user_id}/reopen` (shipped 2026-08-05, `consent.py:139`) —
  what remains is a support address and a written procedure;
- "TTL currently 7 days — pick the number" — it has been 24h since 2026-08-05
  (`config.py:44`); only ADR 0001's Proposed→Accepted stamp remains;
- `[CONTACT EMAIL]` no longer appears in the legal pages; `[GOVERNING STATE]` and
  `[LEGAL ENTITY NAME]` do (`terms/page.tsx:86`).

Sequencing: Phase 0 = eight Evan decisions (~30 min, unblocks everything);
Phase 1 = legal, the critical path, because its clock belongs to other humans;
Phases 2–3 = infrastructure and monitoring behind it; Phase 4 = the soft-launch
cohort; Phase 5 = close-out. Risks stated in the PRD rather than buried: no
frontend test runner (a scope ADD, Evan's call), the one-replica limiter
constraint, and the operator being 17.

**§3 sync.** The M12 box was still unchecked although M12.2–.3 completed
2026-07-12 (`67266d5`) and the v1 exact-copy landed 2026-07-13 — work done and
recorded, checkbox never updated. Ticked with a dated note, same precedent as the
earlier honest §3 fills. Five §3 boxes now remain, all M11.

**VERIFY.** Doc-only. `grep` confirms the status block (line 43), the plan
subsection (line 487) and the ticked box; nothing above the appended text was
edited except the M12 checkbox line and the status insert, both dated.

**STILL OPEN.** Unchanged: mirror stale by two bins + this; Phase 0 decisions are
Evan's; the model-doable block (API_KEYS gap, purge-loop review, support-procedure
draft, mirror sync) is offered and not started.

## 2026-08-19 ~17:22 CDT — the model-doable block: registry gap closed with a check, one real bug in the purge loop, the support procedure written

**WHAT.** Evan approved the four items that needed no accounts. All four done.

**1. The API_KEYS registry gap — closed by derivation, not by eyeballing.** An AST
walk of `Settings` against the registry confirmed exactly the audit's five missing
fields (`PROJECT_NAME`, `STRIPE_PRO_PRICE_CENTS`, `RATE_LIMIT_ENABLED`,
`RATE_LIMIT_AUTH_PER_MIN`, `RATE_LIMIT_WRITE_PER_MIN`); one row now covers them,
and the row carries the one-liner that re-derives the gap to `[]` — this is the
THIRD variable class to go missing from a file that calls itself the complete
registry, so the check ships with the fix. Adjacent miss corrected while in the
file: the `APP_BASE_URL` row itself still read as plain config ("prod = the real
site URL") with no mention that it is a boot blocker — the same F1 class the audit
just closed in four other documents, surviving in the registry operators actually
work from.

**2. The purge-loop review found one real defect: `batch_size<=0` loops forever.**
The termination logic is otherwise sound — ascending order makes the expiry
predicate monotonic over the batch, so `len(expired) < len(batch)` correctly means
"hit the boundary", and a fully-expired full batch makes progress because the
flush removes rows from the next select. But `limit(0)` returns an empty batch and
BOTH exit conditions read `0 < 0` = False. Unreachable from the CLI (which does
not expose `batch_size`); reachable by any direct caller. Guarded with
`ValueError` + a regression test. 327 → 328.

**3. `docs/SUPPORT_PROCEDURES.md` drafted** — the human half of the reopen
endpoint, covering: P1 guardian mis-decline (verify the REQUEST comes from the
guardian on file, then `POST /consent/admin/{user_id}/reopen`; never a new
address), P2 guardian revoke/export/erase (self-serve via the manage token; lost
manage token = deliberately manual, no resend by design), P3 stale guardian email
(no endpoint by design; hand-edit + reopen, log it, and if frequent it needs a
real flow), P4 under-12 / consent-evasion reports (erase per policy; a gate that
did NOT hold is a breach-runbook incident, not a support ticket). DRAFT — needs
Evan's sign-off; prerequisites are the `SUPPORT_EMAIL` value and one promoted
admin.

**4. Three stale BLOCKED-ON-EVAN entries rewritten in place** (the list's items
get updated, unlike the record): the decline-bricks warning now points at the
shipped mechanism + drafted procedure and names what actually remains (address,
inbox, admin account); the ADR-0001 line no longer asks Evan to "pick the number"
for a TTL that has been 24h since 2026-08-05; the legal-placeholder line drops the
resolved `[CONTACT EMAIL]` (verified: the pages render `<SupportEmail/>`) and
gains the entity-question and 12-month-promise review notes.

**VERIFY.** `ENVIRONMENT=ci pytest -q` → **328 passed** (327 + the batch_size
regression). `ruff check` clean; format 107 files. Registry re-derivation prints
`[]`. Frontend untouched this batch.

**STILL OPEN.** Phase 0 decisions (Evan, ~30 min). Mirror stale (two bins + PRD +
this). Push not authorized for these commits.

## 2026-08-19 ~17:55 CDT — pushed, CI green, mirror synced; the index prune was only half a fix

**WHAT.** Evan authorised push + mirror sync for the four-commit batch (audit
fixes, PRD execution plan, model-doable block, timestamp correction).

**Push.** `8d22c75..3996577`. CI run `32310751080` **success across all three
jobs**, both blocking dependency scans included — checked per-job, not by badge.

**Mirror.** `1ed22ae..e5a7235`; five files changed. `--check` now reports
`IN SYNC — 119 files`.

**The honest correction: my F8 fix was partial and I called it done.** The prune I
wrote drops INDEX bullet rows naming unshipped bins — but the published bins still
carry **prose cross-references** to those files (four in INDEX itself, and hits in
five of the six shipped bins). A reader following "see `security.md` §Audit-log
retention" still lands on nothing. I reported F8 as closed without checking the
non-bullet references, which is the same shape as the defect the audit found in
the first place.

**Not fixed by rewriting the text, deliberately.** The published bins are
byte-identical copies of the private ones. Editing their prose to hide the gap
would make the public copy silently disagree with the private one — a worse
failure than a dead pointer, and exactly the drift class this project keeps
paying for. The filter note now SAYS so instead: it lists what is withheld, why,
and warns that cross-references to those files appear throughout. An artifact
that explains its own gap beats one that pretends not to have it.

**Also deliberately not published:** `docs/SUPPORT_PROCEDURES.md`, written this
session. `copy_docs` ships HANDOFF, the PRD, `record_*`, research briefs, ADRs and
the whole-project record — not arbitrary `docs/*.md`, which is why the deploy
runbooks and the secret registry stay private. The support procedures describe
manual DB edits and identity-verification thresholds; whether that belongs on a
public repo is Evan's call, not a side effect of a sync. Flagged, not decided.

**VERIFY.** Push confirmed by ref update on both remotes; CI conclusion `success`
(3/3 jobs); secret guard clean on the sync (129 files staged-or-tracked,
`security.md` absent); `--check` IN SYNC afterwards. Root record repo untouched —
it stays LOCAL.

**STILL OPEN.** `scripts/sync_portfolio.py` carries the uncommitted note change
from this entry's work (committing next). Phase 0 decisions remain Evan's.

## 2026-08-31 ~16:51 CDT — committing a green 2026-08-25 fix pass found uncommitted; entry RECONSTRUCTED from the diff

**PROVENANCE — read this before trusting the "why" below.** This entry documents
work I did not do. It was found uncommitted in the working tree on 2026-08-31,
twelve days after the previous commit (`784d725`), carrying in-code markers dating
it **2026-08-25** ("FINDING 1, 2026-08-25 audit", "finding 3 (2026-08-25)").
**WHAT changed is read directly from the diff and is reliable. WHY is taken from
the change's own comments, not from the session that wrote it** — where those
comments are silent, so is this entry. Committed rather than left untracked
because 261 lines of green work outside git is one `git checkout` from gone. Evan
chose this reconstruction over a code-only commit.

**Verified on the tree AS FOUND, before anything was committed:** `ENVIRONMENT=ci
pytest -q` → **329 passed** (328 at `784d725`; the +1 is the new leaderboard
regression test below). `tsc --noEmit` 0, `eslint .` 0, `next build` 0.

**Code — five changes.**

- **`Cache-Control: no-store` on all six token-bearing consent endpoints**
  (`manage_context`, `guardian_export`, `guardian_delete`, `manage_action`,
  `consent_context`, `decide_consent`). These URLs carry the guardian's secret
  **in the path**, so any cache between the guardian and the app — browser, CDN,
  corporate proxy — could retain a live kill-switch link.
- **The leaderboard's private `_display_name` deleted for shared
  `core/names.first_last_initial`** — the THIRD copy of that function
  (`_public_name` was the second, consolidated 2026-08-12). Ships with a
  regression test: a whitespace-only `full_name` makes `split()` return `[]`, so
  `parts[0]` raised IndexError on the PUBLIC, unauthenticated leaderboard.
  Registration now rejects such names, but a row predating that guard still holds
  one. Asserts a fallback to "Anonymous" instead of a 500.
- **`notification_email_failed` audit event at the chokepoint.** Its two siblings
  (`password_reset_email_failed`, `consent_invite_undelivered`) were already
  audited; this is the highest-volume send path at 14 call sites, so an email
  outage here was the least visible of the three.
- **`models/user.py`** now records that `guardian_manage_token_hash` is
  *deliberately* multi-use and permanent — no expiry column, by design — where the
  previous comment lumped all three token hashes together as single-use. That
  permanence is load-bearing (it is why `_guardian_subject` bounds the token), so
  a comment implying otherwise invited someone to "fix" it.
- **`scripts/sync_portfolio.py`: a real bug.** `MANAGED_DIRS` covered only
  directories, so the two managed ROOT files — `HANDOFF.md` and `PRD_ROADMAP.md` —
  were staged and then discarded on every run. Fixed with `MANAGED_ROOT_FILES` +
  `promote_managed`. **Check before the next publish:** `--check` runs the same
  copy path, so it would report IN SYNC while both sides were equally wrong; every
  sync run on 2026-08-19 is suspect on those two files.

**Docs — six corrections, two landing on my own earlier work.**

- HANDOFF's "**13 v1 screens**" → 17 named screens / 19 route patterns (17 static +
  2 dynamic). This is the exact claim the 2026-08-19 cold audit examined, called a
  near-miss, and DROPPED after the commit log appeared to settle it. It was wrong:
  the auditor's reasoning ("one screen spans several routes") was the right shape
  applied to the wrong number.
- `core/consent.py`'s `account_inactive` docstring: "13 invocations across **6**
  modules" → **9**. I fixed that sentence's numerator on 2026-08-12 after shipping
  an unverified figure, and left the module count wrong inside a warning about
  exactly this class of error.
- `frontend/README.md`: status block five weeks stale → current; **Next 15 → 16**;
  route listing regenerated (29 routes, with the command that derives it).
- HANDOFF and `frontend/README.md` both now warn that `backend/README.md` is
  **private-tree only** — a public-mirror reader following that pointer finds
  nothing.
- `frontend/.env.example`: three missing vars added (`NEXT_PUBLIC_APP_URL`,
  `NEXT_PUBLIC_TURNSTILE_SITE_KEY`, `NEXT_PUBLIC_SUPPORT_EMAIL`).
- ADR 0001: two stale `lib/api.ts` line references corrected.

**Two housekeeping items folded in, both mine to own.** `.gitattributes` was
untracked and is load-bearing: `core.autocrlf` is true here, so a fresh clone
rewrote every tracked shell script to CRLF *including the shebang*, making the
pre-commit hook fail as `bad interpreter` — a clone with **no secret gate and no
error naming the cause**. And `scripts/__pycache__/` is now ignored; the
subproject `.gitignore`s are scoped to `backend/`/`frontend/` and never covered a
package at this level, which my own `sync_portfolio.py` runs on 2026-08-19 most
likely created.

**STILL OPEN.** The role-guard consolidation was started on this tree and fully
reverted when the foreign work surfaced — nothing from it is in this commit; see
the next entry.

## 2026-08-31 ~17:05 CDT — the role-guard consolidation: designed, attempted, reverted, and it nearly shipped a hole

**WHAT.** Evan asked whether the code should be more modular. Measured rather
than opined, found one worthwhile consolidation, started it, and backed all of it
out. Nothing from this attempt is committed. Writing it down because the design is
sound and the failure is instructive.

**The measurement said NO to general modularity.** Backend `app/` is 5,375 lines,
largest file 577; frontend 6,327, largest 542 — no monolith. Churn since
2026-08-01 tops out at `consent.py` (9 commits) and `config.py` (9), and
`config.py`'s churn is "we keep adding settings", which is the file working. More
decisively, EVERY recurring defect in this project has been a **duplication**
problem, not a size problem: the consent gate missed four times,
`create_notification` consulting consent at 1 of 13 sites, the limiter fix
sweeping 1 of 4, `_public_name` as a second copy of `first_last_initial`, the
hand-rolled `.astimezone(UTC)`. Every fix that stuck was consolidation. Splitting
files further moves siblings further apart, which makes that class MORE likely.

**Where the code IS and ISN'T consistent** (measured, four layers):

- **Visual — shared.** `opp-card` in 7 files, `empty-state` 5, `form-box` 4,
  `status-pill` 4 — each defined ONCE in the CSS component layer. Pages compose
  class names. This is why v1 parity held across 29 routes.
- **Network — shared.** `lib/api.ts` is one client over a single fetch core;
  27 of 29 pages use it rather than calling `fetch`.
- **Behaviour — NOT shared.** 19 pages hand-roll `useEffect`; 63 `useState`
  declarations across pages; `error` state declared independently in 12.
- **Backend authorization — TWO IDIOMS FOR ONE JOB.** Guardian consent is a
  proper dependency (`Depends(require_consent)`, 9 uses). The role check is
  inlined **25 times across 11 route modules**. All 25 are currently correct; it
  is the latent form of the exact shape that has bitten four times.

**The design (unbuilt, and worth building).** Promote `saved.py`'s private
`_require_student` — the pattern was already reached for once and never
generalized — into `deps.py` beside `require_consent`, as a `_role_guard(role,
detail)` factory yielding `require_student` / `require_org`.

**Why the attempt failed, which is the part to keep.** The 25 sites are NOT
uniform. They are four shapes, and I treated them as one:

1. ~15 pure caller-role gates with standard copy — the real target;
2. **4 role AND ownership** (`role != org or opp.org_id != user.id`) — needs the
   loaded resource, cannot be a role-only dependency;
3. **3 checks on a SUBJECT, not the caller** — consent's `dob` predicates,
   `portfolio`'s 404 union, `reviews`' target-org lookup. Same syntax, different
   question;
4. **2 with bespoke user-facing copy** ("Only students can apply", "Only
   organizations can post opportunities") — changing a 403 users see is a copy
   decision, not a refactor.

I converted with a regex for the check plus a blanket string-replace for the
dependency. On `reviews.py` those did not pair: `create_review` binds
`Depends(require_consent)`, not `Depends(get_current_user)`, so the replace
matched nothing while the regex still deleted the student check — leaving
**`create_review` with no role gate at all**, i.e. an org able to post a review of
itself. The same trap was waiting in `hours.py`, where `auto_log` and
`self_report` are also consent-gated.

Caught by reading `git diff` per changed dependency line rather than trusting the
script's own success output. Reverted with `git checkout` on all five touched
files.

**The lesson is the session's own subject matter.** I was consolidating to fix
"the same change applied at one site and not its siblings", and did it by assuming
25 sites were siblings when four different things shared their syntax. A
mechanical sweep over a set you have not classified is the bug, not the tool.

**Also found, and it stopped the work: the tree was not mine.** `git status`
showed 12 modified files and 2 untracked from a 2026-08-25 session, uncommitted
for twelve days — including `consent.py` and `leaderboard.py`, two of the modules
this refactor touches. Committed separately as `e98ab4f` (see the entry above)
rather than built on top of. **Check `git status` before starting work here;
sessions are days apart and the tree does not arrive clean.**

**VERIFY.** Nothing to verify — no change survives this entry. Tree clean at
`e98ab4f`, `ENVIRONMENT=ci pytest -q` → **329 passed**.

**STILL OPEN.** The role-guard consolidation (design above, ~15 sites, do it by
hand not by regex). The frontend load/error/retry duplication — higher value but
riskier with no test runner. The status-map consolidation (3 definitions of one
concept, already shipped one bug).

## 2026-08-31 ~17:07 CDT — the role-guard consolidation, built by hand this time

**WHAT.** The consolidation designed and reverted in the entry above is now
live. Every site was classified before it was touched and edited individually;
the full diff was read hunk by hunk before running anything.

**The mechanism.** `deps.py` gains a `_role_guard(role, detail)` factory and its
two products, `require_student` / `require_org` — the ONE definition of the
caller-role check, beside `require_consent`. Fifteen sites converted across nine
files:

- **10 plain sites** (saved ×4 — its private `_require_student` deleted,
  applications ×3, awards, billing checkout, hours verify, opportunities `/mine`,
  templates ×2): `Depends(get_current_user)` → `Depends(require_student|org)`,
  inline check deleted.
- **5 consent-gated sites** (hours auto-log + self-report, exclude-date,
  check-in redeem, create-review — the exact sites where the regex attempt
  died): the signature KEEPS `Depends(require_consent)`, because
  `test_consent_gate_coverage._is_gated` detects the gate by that function name
  at the top level of the route's dependencies, and the role guard attaches via
  the decorator's `dependencies=[Depends(require_student)]`. Same 403s for every
  caller class as before (FastAPI caches `get_current_user` per request, so no
  double auth).

**NOT converted, deliberately** (the other 10 of the 25): the 4 combined
role-AND-ownership conditions (featured/active toggles, checkin-codes,
broadcast), the subject-shaped checks (consent's two dob predicates, portfolio's
404 union, reviews' target-org lookup), and the 2 bespoke-copy gates ("Only
students can apply", "Only organizations can post opportunities") — changing
user-visible copy is not a refactor.

**One behavioral edge, stated:** a wrong-role caller with an INVALID body now
gets 403 before the 422, because dependencies resolve before body validation;
inline checks ran after it. No test asserted the old order; arguably the 403
first is more correct (validation detail no longer reaches unauthorized
callers).

**The near-miss is now a test.** No test in the suite sent an org token at any
consent-gated student route — which is precisely why the ~17:05 hole
(`create_review` ungated) would have shipped GREEN. New
`tests/test_role_guards.py` probes all five with an org token and asserts 403
"Students only". Proven load-bearing: with the reviews decorator guard removed
it fails (`1 failed in 0.45s`), restored it passes.

**VERIFY.** `ENVIRONMENT=ci pytest -q` → **330 passed** (329 → 330, the +1 is
the new probe file); `ruff check` clean; `ruff format --check` 108 files clean.
Frontend untouched this session — no frontend gate applies. Docker still down:
nothing browser-verified, nothing here needed it.

**STILL OPEN.** Frontend load/error/retry duplication (no test runner — riskier).
Status-map consolidation (3 definitions, one shipped bug). Mirror is STALE by 8
files per `--check` (syncing publishes — Evan's call). Phase 0 decisions.

## 2026-08-31 ~22:10 CDT — status maps get one home, and the count was wrong in both directions

**WHAT.** The status-map consolidation (open item #4). The premise said "three
files define STATUS_LABEL/STATUS_PILL/STATUS_MESSAGE"; classification before
sweeping found the truth was messier in both directions:

- **FOUR copies, not three** — `dashboard/page.tsx` had a fourth
  (`HOURS_STATUS`, pill+label tuples) the name-based grep missed.
- **Two DOMAINS, not one** — applications + signup-section map APPLICATION
  statuses (pending/approved/rejected/waitlisted/withdrawn); hours + dashboard
  map HOURS statuses (pending/verified/denied/appealed) that merely share the
  constant name. A naive three-way merge — the literal reading of the open
  item — would have fused two vocabularies. Same lesson as the role guards,
  same day.
- **A fifth shape left alone deliberately**: `applicants/page.tsx` (org
  dashboard, v1-parity) templates `sp-${status}` and shows raw status words —
  the org's copy register, not the student's. Converting it is a copy decision.

**And one false alarm, mine.** Mid-classification I reported dashboard's map
as "missing `appealed` — live bug". It isn't: my grep alternation didn't
include `appealed`, so its line couldn't match. The claim came from my own
search pattern, not the file. Reading the definition site killed it in one
minute; stated here because relaying an unverified count is this project's
standing rule violated in miniature.

**The mechanism.** New `frontend/lib/status.ts` — five exported maps in two
labeled domains (`APPLICATION_STATUS_LABEL/PILL/MESSAGE`,
`HOURS_STATUS_LABEL[/_COMPACT]/PILL`), each naming its consumers, load-bearing
comments (the `withdrawn` history) moved intact. Four consumers now import;
zero user-visible copy changed. One rendering-class change: dashboard's
`appealed` pill goes `sp-appealed` → `sp-waitlisted`, which is IDENTICAL on
screen — v1.css:268 styles both selectors with one rule — and it removes the
only use of a class globals.css never defined.

**Found, not fixed (each a decision, not a refactor):** `appealed` renders
purple on `/dashboard` (v1.css) and orange on `/hours` (globals.css has no
`sp-appealed`; its `sp-waitlisted` is orange where v1's is purple) — same row,
two colours; fixing means adding a class to globals.css, whose palette drift
from v1.css may itself be deliberate. And `SOURCE_LABEL` (hours) vs `SRC`
(dashboard) duplicate the source-label concept in two registers — the same
disease, outside this item's scope.

**VERIFY.** `tsc --noEmit` clean; `eslint .` exit 0 (12 warnings, all
pre-existing on untouched lines); `next build` clean. NOT browser-verified —
the affected tables need an authenticated API with data and Docker is down;
static equivalence (verbatim map moves + the shared v1.css rule) plus the
build is the whole gate, and the frontend still has no test runner.

**STILL OPEN.** Frontend load/error/retry (the last of the three consolidation
items). Mirror sync, push, root-record commit, Phase 0 — all on Evan.

## 2026-08-31 ~22:12 CDT — Phase 0: five decisions land, one reverses the recommendation

**Evan's answers to the Phase 0 block, verbatim intent, dated here as the
decision record:**

- **ADR 0001 (token storage: localStorage JWT, 24h TTL) → ACCEPTED.**
- **ADR 0002 (billing: launch free-tier-only) → ACCEPTED.**
- **Pro upgrade surface → "coming soon"** (replaces the would-503 upgrade
  path; model builds it — this session).
- **Refund-policy default → draft ACCEPTED** (cancel anytime, end-of-period,
  no proration; moot at free-tier launch but stands in the ToS).
- **Analytics → BUILD IT, "properly"** — REVERSES the standing "keep
  deferred" recommendation. Scope add; the shape (first-party cookieless vs
  third-party) goes back to Evan with options before anything is built,
  because two of the three shapes need accounts/cost and all three touch the
  privacy policy currently entering legal review.

**Asked for elaboration before deciding (answered in-session, decisions still
open):** the support address, error tracking (Sentry vs host-native),
consent-IP precision, M13.6 SWR — the SWR one now interacts with the open
load/error/retry consolidation (adopting SWR would replace the hand-rolled
pattern; skipping it makes the shared hook the fix).

Mechanical consequences applied next (same session): both ADR status lines
flip Proposed → Accepted with today's date, and the billing page's Pro
surface gets its "coming soon" state.

## 2026-08-31 ~22:16 CDT — ADRs stamped Accepted; the Pro surface says "launching soon"

**WHAT.** The mechanical half of the Phase 0 batch. ADR 0001 and ADR 0002:
status line → ACCEPTED (Evan, 2026-08-31), each with a dated acceptance
section appended; Proposed-era text left as written per the files' own
convention. Then ADR 0002's launch-UI treatment (i), built:

- New `frontend/lib/flags.ts` — `BILLING_LIVE`, read from
  `NEXT_PUBLIC_BILLING_LIVE` at build time. **Default OFF = fail closed**: the
  unset state hides checkout and shows "launching soon", which is correct
  exactly when the backend has no Stripe keys. The env-var gate was chosen
  over a backend config endpoint (the ADR offered both) as the smaller rung;
  its drift risk fails safe in both directions (flag off + keys set → upgrade
  hidden, harmless; flag on + no keys → the pre-existing 503 error copy, only
  reachable by explicit misconfig).
- `billing/page.tsx`: the Upgrade button now renders only under the flag;
  otherwise a plain "Pro upgrades aren't open yet" paragraph.
- `pricing/page.tsx`: Pro card CTA "Get Pro" → non-interactive "Launching
  soon" under the flag, and the footnote "Stripe goes live with deployment" —
  which a free-tier launch would have made FALSE on day one — swaps to
  truthful coming-soon copy. The $19 price stays displayed: reconciling it
  against the backend's $29 (`STRIPE_PRO_PRICE_CENTS=2900`) is ADR 0002
  follow-up #2, still BLOCKED-ON-EVAN.
- `frontend/.env.example` documents the flag.

**VERIFY.** `tsc --noEmit` clean; `eslint .` 0 errors (12 pre-existing
warnings); `next build` clean. Pricing page browser-verified on the live dev
server (no backend needed): "Get Pro" absent, "Launching soon" and the new
footnote present, opacity 1 — by DOM/find and computed style, because the
pane's screenshot capture is intermittently blank again (the documented
2026-07-13 condition; one capture did show the page painting). The billing
page branch is NOT browser-verified — it needs an authenticated API and
Docker is down. The dev server rewrote `next-env.d.ts` to dev-flavored type
paths; reverted rather than committed (generated file, not this change).

## 2026-08-31 ~22:23 CDT — the last four Phase 0 answers, two of them against my recommendation

**WHAT.** Evan answered the four items he'd asked me to elaborate. Phase 0 is
now CLOSED — all eight decided. Recording them before building, because two
change scope.

- **Support address → `support@<domain>`** (option a). Consequence: the VALUE
  now depends on the Phase-2 domain purchase, so it moves from "a decision
  Evan owes" to "a value Phase 2 produces". Three places consume it
  (`SUPPORT_EMAIL`, `NEXT_PUBLIC_SUPPORT_EMAIL`, SUPPORT_PROCEDURES.md) and
  the blank state already renders a visible placeholder rather than a fake
  address, so nothing ships a lie in the meantime.
- **Error tracking → Sentry.** Reason on record: host-native logging cannot
  see browser-side errors, and this frontend has no test runner and 19
  hand-rolled fetch pages. Will ship env-gated OFF (Turnstile precedent) with
  PII scrubbing as a build requirement, not a follow-up. Account + DSN stay
  BLOCKED-ON-EVAN (18+ ToS, same class as live Stripe).
- **Consent-IP → KEEP full precision, ratified.** Truncating would have gutted
  the field's only purpose — a /24 proves nothing about which guardian
  decided. Already bounded: absent from every response schema (mechanically
  enforced by `test_invariants_are_enforced`), cleared on guardian deletion.
- **M13.6 → ADOPT SWR** — *against my recommendation* (I proposed a
  zero-dependency shared hook). Evan's call makes SWR the fix for the open
  load/error/retry duplication rather than something that would later replace
  a hook I'd just built, which is the stronger argument. **Risk I own and
  state: this is a 19-page sweep on a tree with no frontend test runner, and
  Docker is down so several pages cannot be browser-verified at all.** It will
  be done page by page with per-page verification, never as one mechanical
  pass — the failure this session already demonstrated twice.
- **Analytics → BUILD, both surfaces, shape (a)** — *also against my
  recommendation* (keep deferred). Scoped as new milestone **M14**: first-party
  cookieless site counters, then the org-facing tab backed by real numbers. The
  2026-07-15 deferral's constraints (no cookies, minors, COPPA/GDPR-K) become
  requirements rather than reasons to wait: no cookies, no stored IPs, no
  per-user browsing trail, aggregate rows only.

**PRD updated by its own history rules** — nothing deleted: M13.6 gains a
dated ADOPT note under the original "default is skip" sentence; the struck
analytics deferral gains a dated REVERSED line; M14 appended; the Phase 0
block marked CLOSED with all eight answers; Phase 3 records the Sentry pick
and its constraints.

**Note on the two reversals.** Both went against what I recommended, and both
have better reasoning than mine did: SWR-instead-of-hook avoids building a
thing to throw away, and "build analytics properly" is a defensible reading of
a portfolio project where the org tab currently under-delivers what the
pricing page advertises. Recording that here rather than quietly complying.

## 2026-08-31 ~22:36 CDT — SWR lands on one page, and the local stack runs again on SQLite

**WHAT.** M13.6 begun: `swr@2.5.1` installed (**npm audit: 0 vulnerabilities**,
which matters — that scan is a blocking CI job), the shared hook written, and
**one** page converted and verified end to end. One page, not nineteen: the
decision record says page-by-page, and this is the page that proves the
pattern.

**`frontend/lib/use-api.ts` — `useAuthedQuery(key, fetcher)`.** Two design
calls worth stating:

- **`loading` stays true while auth is still hydrating.** This is the whole
  point. The bug class this replaces is a page rendering its EMPTY state
  before it knows whether the fetch ran or failed — it told a student with
  verified hours they had none (audit 2026-08-11).
- **Client errors are never retried.** SWR retries every error by default with
  backoff; for this API a 403 from the consent gate, a 404, or a 401 on a dead
  token are settled answers. Retrying them hammers an endpoint whose rate
  limiter counts every attempt, behind a spinner that can never resolve. 5xx
  and offline errors retry three times with backoff.

**The pilot page fixed a real bug, not just its plumbing.**
`applications/page.tsx` called `api.myApplications(token).then(...).finally(...)`
with **no `.catch`** — so a failed load left `fetching=false` and an empty
array, rendering *"Nothing here yet. Once you apply to an opportunity, it shows
up here."* to a student who has applications. Same shape as the 2026-08-11
finding, in a second file, never caught because nothing here can fail a build.

**VERIFY — browser-verified end to end, which has not been possible since
early August.** Docker is still down, so instead of Postgres the backend was
booted on **SQLite**: `Base.metadata.create_all` into a scratchpad DB and
`uvicorn --env-file` pointing `DATABASE_URL` at it (a `.claude/launch.json`
entry, `servelocal-v2-backend-sqlite`, drives it — that file is gitignored and
local). `/api/v1/health` returned `{"status":"ok","database":"up"}`; a student,
an org, an opportunity and an application were created **through the real HTTP
API**, not inserted. Then, on `/applications`:

1. **Data path** — the row renders: "Riverside Park Cleanup / Riverside Parks
   Trust / **Pending approval**". That label also re-verifies today's earlier
   status-map consolidation on a live page.
2. **Error path** — a temporary 503 injected into the route (reverted
   immediately; `git diff` against HEAD confirmed empty before committing)
   produced the error panel + Retry, and **NOT** the false empty state. This is
   the fix, demonstrated.
3. **Retry path** — fault reverted, API restarted, Retry clicked: exactly ONE
   request to `/applications/my` went out and the data returned with the error
   cleared, no page reload.
4. **Outage path** — with the API stopped, previously-loaded data stays on
   screen rather than blanking.

**Honest notes on the verification.** The final Retry proof came from a
scripted DOM `.click()` after a coordinate click missed the re-rendered
button — the handler and round trip are real, the mouse path is not separately
proven. And **SWR's focus/reconnect revalidation could not be tested at all**:
the browser pane reports `document.visibilityState === "hidden"` even when the
tab is fronted, and SWR gates both on visibility — so that behavior ships
unverified and is called out here rather than implied.

**Found while verifying, NOT fixed (pre-existing, and Evan's call):**
`auth-context` hydrates with `api.me(token).catch(() => removeItem(TOKEN_KEY))`
— that catch cannot tell a **401 from a network failure**, so if the API is
merely unreachable the user is silently logged out and must sign in again. Seen
directly: with the backend stopped, a reload dropped the session and rendered
logged-out chrome. The fix is one condition (`only clear on ApiError 401`), but
it is auth behavior on a minors platform, so it gets its own decision rather
than riding along in a refactor commit.

**STILL OPEN.** 18 pages left in the SWR migration. M14 analytics (both
surfaces). Sentry wiring. Push (7 commits), mirror sync, root-record commit.

## 2026-08-31 ~22:59 CDT — landing-check: the code landed, four of my numbers did not

**WHAT.** `/landing-check` over the session's five commits, run by a fresh agent
that got the artifacts (diffs, commit messages, these entries) and explicitly
NOT my account of the work. Verdict **FIX FIRST**: zero code defects, zero
mislandings, every control fires — and **four false or imprecise numbers in my
own prose**, plus documentation that had not propagated. That ratio is this
skill's whole point and it held again.

**Corrections to the entries above** (append-only: the original sentences stand
as written, these supersede them):

1. **"Push (7 commits)" — WRONG, it is 5.** Re-derived three ways
   (`rev-list --count`, `cherry`, `--left-right`), and by the probe after a real
   `fetch`: `ahead 5 behind 0`. I had been incrementing a briefed baseline of 2
   instead of counting. This is the "don't relay a number you haven't counted"
   rule, broken by me, in an entry written minutes after I corrected the same
   error in chat.
2. **"Five maps now live in lib/status.ts" — WRONG, six.**
   `grep -c "^export const" frontend/lib/status.ts` → 6
   (`APPLICATION_STATUS_LABEL/PILL/MESSAGE`, `HOURS_STATUS_LABEL`,
   `HOURS_STATUS_LABEL_COMPACT`, `HOURS_STATUS_PILL`). My own
   `LABEL[/_COMPACT]/PILL` shorthand in the same sentence enumerates six.
3. **"10 plain sites (saved ×4, applications ×3, awards, billing, hours verify,
   opportunities /mine, templates ×2)" — the list sums to 13, not 10.** Both
   numbers are real and I conflated them: **10 inline role CHECKS** were deleted
   across **13 route SITES** — `saved.py` had four sites sharing one
   `_require_student` helper. Say which noun the number counts.
4. **"across nine files" — imprecise.** Nine `backend/app/` files changed, but
   the converted sites live in **8** route modules; the ninth is `deps.py`,
   which defines the guard rather than consuming it.
5. **"the five consent-gated student routes" — true but easy to misread.**
   **Nine** routes carry `require_consent`; five of them are the ones that also
   needed a decorator role guard. The other four (`/apply`,
   `/hours/{id}/appeal`, `/opportunities/{id}/messages`, `/messages/{id}/reply`)
   are not covered by `test_role_guards.py` and deserve their own wrong-role
   decision — logged as an open item, not silently absorbed.

**What the sweep CONFIRMED, and this is the load-bearing half.** 25 → 10 inline
checks (15 removed) with the remaining 10 matching the "NOT converted,
deliberately" classification exactly, file by file. **18 of 18** guarded routes
403 a wrong-role caller, derived twice independently (source grep and live route
introspection). **5 of 5** decorator guards fail the suite when removed one at a
time. `test_consent_gate_coverage` fires on the exact mechanical-sweep failure
that nearly shipped a hole at ~17:05. **330 passed.** Zero status-map display
strings changed (13/13 byte-identical). `NEXT_PUBLIC_BILLING_LIVE` genuinely
changes rendered output **in both directions** — verified by building and
serving the app under each flag value and curling `/pricing`, which is stronger
than the DOM check I did and which a bundle grep cannot decide, since both
ternary branches ship. Zero `Status:.*Proposed` left in the ADRs. Zero broken
TOC anchors in the record twin (45 headings = 45).

**Documentation that had not propagated (fixed in this pass, listed as the
finding it was):**

- **The whole-project record covered 1 of 5 commits.** Only II.31 (the role
  guards) existed; the status maps, the billing flag, the Phase-0 closure/M14,
  and the SWR adoption were all medium-or-larger and all missing — a
  `CLAUDE.md` §Definition-of-done item 4 failure. II.31 even said "entries of
  2026-08-31", plural, while one existed.
- **HANDOFF never recorded `6d75394`** (SWR) at all.
- **HANDOFF contradicted itself on M13.6**: the workstream table still read
  "M13.6 SWR = skipped (Evan 2026-07-15, PRD default)" while the decisions
  block twelve lines later read "adopt SWR".
- **HANDOFF said the mirror was stale by 8 files; it is 21** (18 changed + 3
  never-shipped new files). The 8 was true when written and went stale the
  moment this session started committing.
- **The whole-project record still asserted "M13.6 SWR skipped"** at line 1135,
  in a 2026-07-16 entry — corrected by a dated note there, original standing.

**Not defects, checked and cleared.** `PRD_ROADMAP.md`'s surviving
"recommend: keep deferred" and "default is skip" sentences sit under dated
CLOSED/DECIDED/REVERSED annotations — that is the file's documented history
rule working, not drift. The public mirror carries `D:\ClaudeCode\...` in four
tracked files, but every one predates this session (last touched 2026-07-20 /
08-18 / 08-19) — **inherited, not introduced**; it is still a real disclosure
and is logged for `/audit`. The sweep also noted its own first three searches
for that string returned zero and were wrong — broken backslash transport, not
a clean result — which is why the finding survived.

**Carried, with paths.** `.claude/launch.json`'s `servelocal-v2-backend-sqlite`
entry points `--env-file` at the session scratchpad; it stops resolving when
that is cleaned. The file is gitignored and lives in the ROOT repo's `.claude/`,
not v2's — worth stating, since the entry above did not say which tree.
Scratchpad holds a live JWT (`tok.txt`), `dev.db` and `dev-sqlite.env`; all
outside every repo and uncommittable, none deleted.

**Handed off, not half-done here.** To `/code-review`: the `auth-context`
silent-logout on any `/auth/me` failure (already reported, still Evan's call);
`useAuthedQuery` renders neither empty nor error state while the key is held
with `data === undefined`; `POST /hours/{id}/appeal` carries no role guard and
is safe today only via its ownership 404. To `/audit`: the four consent-gated
routes without wrong-role coverage, and the pre-existing `D:\ClaudeCode`
disclosure in the public mirror.

**VERIFY.** Every finding above re-derived by hand before acting on it —
`grep -c` on the exports, `rev-list --count` on the commits, the arithmetic on
13 vs 10, `sync_portfolio.py --check` on the mirror, and `grep -n` on the two
contradicting HANDOFF lines. The sweep's own claims were treated as claims.

## 2026-09-01 ~01:03 CDT — closing the provenance gap in the entry above

**WHAT.** One loose thread from the landing-check, and it is the same rule the
entry above is about. That entry dates the public mirror's absolute-path
disclosure as "last touched 2026-07-20 / 08-18 / 08-19" and concludes the paths
are **inherited, not introduced**. Those three dates were **relayed from the
sweep agent, not derived** — I published a load-bearing conclusion on numbers I
had not run a command against, inside an entry correcting myself for exactly
that.

**Now derived**, `git log -1 --date=short` per file in
`D:\ClaudeCode\servelocal-portfolio`:

| file | last commit |
|---|---|
| `docs/record_2026-07-07.md` | 2026-07-20 (`cef44b0`) |
| `project-history/Project Record — Full Chronological History.md` | 2026-08-18 (`46145e8`) |
| `PRD_ROADMAP.md` | 2026-08-19 (`e5a7235`) |

**The three dates hold and the conclusion is unchanged** — every one predates
this session, so the disclosure is inherited. What did NOT hold was a detail in
my chat summary, which paired the dates to the wrong files; the record entry
above listed them unpaired and is correct as written, so nothing there needs a
strike. Still logged for `/audit`: inherited or not, absolute local paths are
live in a PUBLIC repo.

**Also noted, no action:** the live-update watch on the published launch-plan
artifact ended (connection lost). The page itself is unaffected and still
served at its URL; nothing is pending a republish, so it was not re-watched.

**VERIFY.** The table above is the command's output, not a recollection.

## 2026-09-01 ~15:35 CDT — PAUSED mid-task-4; the tree is dirty and one pin is knowingly red

**Stop point, written down because the last uncommitted work here sat for
twelve days and had to be reconstructed from a diff.** Evan said pause during
Sentry wiring. Nothing below is committed.

**Done and committed this run** (5 of the 5 requested tasks, 3 complete):

- **Task 5 — the four uncovered consent-gated routes** (`c988be1`). `/apply`
  keeps its bespoke 403 copy, now pinned. **`/hours/{id}/appeal` had no role
  gate at all** — safe only because `Hours.user_id` is always a student, so an
  org fell through to the ownership 404: safety as a property of the data, not
  a stated rule. Now carries `require_student`, proven load-bearing. The two
  messaging routes deliberately serve BOTH roles, so they are gated by
  relationship and tested that way.
- **Task 3 — the silent logout** (`62f6462`). `auth-context` cleared the token
  on ANY `/auth/me` failure, so an unreachable API destroyed the session. Only
  a 401 clears it now. Verified both directions on the SQLite stack: an outage
  KEEPS the session, a tampered token is still cleared.
- **Task 1 — M14.1 site analytics** (`1c0b16e`). `route_hits` + middleware +
  admin-only read + migration 0025 (up/down/up clean). Three landmines, all
  fixed: alembic autogenerate invented a `messages` foreign key; walking
  `app.routes` for the template silently matched nothing (the `_IncludedRouter`
  trap this repo already documents); and the recorded path lacked the `/api/v1`
  prefix, so **the exclusion list could never match while its test passed
  VACUOUSLY** — the test now asserts something was counted first, and was
  proven to fail with the exclusions emptied. 341 tests.

**Task 4 — Sentry — IN FLIGHT, UNCOMMITTED, and one pin is currently red.**
Working tree holds `app/core/observability.py`, `tests/test_observability.py`
(8 passing), plus edits to `config.py`, `main.py`, `requirements.txt`.

**Two things a resuming session must know:**

1. **`jinja2==3.1.5` in `requirements.txt` is KNOWN VULNERABLE** —
   `pip-audit` reports PYSEC-2026-1471, fix **3.1.6**. Both CVE scans are
   blocking CI jobs, so committing this as-is turns `main` red. **Bump to
   3.1.6 and re-run `pip-audit -r requirements.txt` before any commit.**
2. **jinja2 is not optional and not stray.** Sentry's Starlette integration
   imports `starlette.templating` unconditionally, which raises `ImportError`
   without jinja2 — so with a real `SENTRY_DSN` set, **the app would crash at
   boot, i.e. only in production**. Found by the test asserting the scrubber is
   actually wired into the SDK, not by reading the code. Do not "clean up" this
   dependency.

Also unfixed and pre-existing: `pip-audit` flags the venv's own `pip` 26.1.2
(PYSEC-2026-3721, fix 26.2). Not a project dependency and not introduced here.

**NOT STARTED: task 2, the SWR migration** — 15 `page.tsx` files that fetch
inside `useEffect`, plus 4 non-page components. Still 1 of 19 converted.

**State:** 11 commits unpushed. Backend 341 green as of `1c0b16e`; the
uncommitted Sentry work adds 8 more locally. Mirror stale by 21+ files. The
root record repo still holds the foreign II.29/II.30 entries uncommitted.


## 2026-09-01 ~15:53 CDT — Sentry wired, SWR on ten pages, and the migration stopped on purpose

**WHAT.** Resumed from the pause. Task 4 (Sentry) finished and committed; task 2
(SWR) advanced from 1 page to 10 and then **deliberately halted** with four
pages left — see the boundary below, which is a decision, not an omission.

**The red pin from the pause is gone.** `jinja2` 3.1.5 → **3.1.6**
(PYSEC-2026-1471). `pip-audit -r requirements.txt` → **"No known
vulnerabilities found"**. The venv's own `pip` (26.1.2, PYSEC-2026-3721) is
still flagged; it is not a project dependency and was not introduced here.

**Sentry (`e0e4f2e`).** Off unless `SENTRY_DSN` is set — the Turnstile pattern,
so dev and CI need no account. The scrubbing is the feature, not a courtesy:
`before_send` drops the user block, request body, query string, cookies and the
**full URL**, because a guardian's manage token rides in the URL PATH and a
captured URL is a live kill-switch link for a minor's account. Sensitive header
names are redacted case-insensitively; ordinary headers survive so the event is
still worth reading.

**The test that earned its keep asserts the scrubber is WIRED**, not merely
correct — `before_send is scrub_event`, `send_default_pii is False` — because a
perfect scrubber never passed to `init()` leaves every other test green while
real events ship unscrubbed. **It caught a production-only crash:** Sentry's
Starlette integration imports `starlette.templating` unconditionally, which
raises `ImportError` without jinja2. With a real DSN the app would have died at
boot and never before. jinja2 is now declared with that reason recorded in
`requirements.txt`, `.env.example` and `docs/API_KEYS.md`, so nobody prunes it
as unused. **349 tests.**

**SWR: 10 of 19 pages** (`06264e8`, `58428b0`). New `usePublicQuery` for
tokenless endpoints — split from `useAuthedQuery` rather than given an
`auth: false` flag, because the two differ in the one thing that matters: the
public one must fetch for a signed-OUT visitor, the authed one must not fire
until auth hydrates. A boolean hiding that is how a public page ends up gated
on a user it never needed.

**Four more pages had the same false-empty-state bug** as `/applications`:
the leaderboard told every visitor "No verified hours yet" when the request
failed; the inbox said "No messages yet"; verify-hours told an org "No hours
awaiting verification" while students waited on them; notifications hid
consent and hours-verified messages behind "No notifications yet". All four now
separate error from empty and offer Retry.

**Three conversions needed judgment, not a swap.** The leaderboard's two
requests share ONE key because its stat band is derived from the pair —
splitting them would render totals against a half-loaded pair. Portfolio's
`isPublic` mirrored a user field into state that could drift from the server,
so it derives from auth now with an explicit optimistic override. Discover's
bookmark toggle carries an audit fix (a failed save used to leave the heart
filled while the server recorded nothing), so it writes through SWR's cache
with `revalidate: false` on both the optimistic write AND the revert — letting
SWR refetch instead would race the failed request and could restore the wrong
heart.

**VERIFY (browser, on the SQLite stack).** Leaderboard renders signed-out with
correct derived stats; notifications and inbox render authed with zero console
errors; **with the API stopped the leaderboard shows the error and Retry
instead of its empty state, and Retry recovers it with no reload**; on discover,
clicking save flips the heart AND `/saved/ids` returns the id server-side.
`tsc` clean, `eslint` 0 errors (11 warnings, down from 12), `next build` clean.

**WHERE IT STOPPED, and why — four pages left, each for a stated reason:**

- **`consent/[token]` and `consent/manage/[token]` — NOT to be converted.**
  They already handle errors properly, so there is no bug to fix, and they are
  the M5 guardian-consent gate, the hardest launch gate in the project.
  Repeated revalidation of a one-shot token page is also the wrong semantics.
  Converting them would be churn on the most sensitive flow for zero gain.
- **`hours` — needs design, not a swap.** Its "load" begins with
  `autoLogHours`, a **write**. Under SWR that fires on every revalidation, so a
  naive conversion would auto-log hours on window focus. The reads must be
  separated from the write first.
- **`dashboard` and `applicants` — the two largest (6 and 9 API calls),
  multi-tab, with their own error panels.** Converting them carelessly at the
  end of a long session is precisely how the 2026-08-31 regex sweep nearly
  shipped an ungated route. They get their own sitting.

**STILL OPEN.** Those four pages. M14.2 (org analytics tab — note
`Opportunity.views` is a DEAD COLUMN, declared and never written, while the
pricing page advertises a views dashboard). Push (14 commits), mirror sync,
root-record commit.


## 2026-09-01 ~16:13 CDT — landing-check round two: my page census was wrong, and it was hiding a live bug

**WHAT.** `/landing-check` over the 14 unpushed commits, fresh agent, artifacts
only. **Zero mislandings; six planted controls, six fires** — including the two
this session called load-bearing (emptying the traffic exclusions, unwiring the
Sentry scrubber) and the new `require_student` on `/hours/{id}/appeal`. It also
ran a positive probe I had not: a real request wrote exactly
`('/api/v1/opportunities/{opportunity_id}', 'GET', 1)` with the resolved id
absent from every row, so template-only storage is demonstrated, not just
asserted.

**The finding that matters: my own census was wrong, and the error hid a live
bug.** I counted converted/unconverted pages with `grep "api\."`. In
`app/saved/page.tsx` the call is written as `api` then `.listSaved(token)` on
the next line, so **it never matched** — the page looked converted and was not.
It carried the exact bug this migration exists to fix: no `.catch`, so a failed
load told a student with bookmarks "No bookmarks yet. Tap the heart on any
opportunity." **Sixth instance of that shape, found by the sweep, not by me.**
Fixed and converted in this pass.

Two more corrections to the entry above, originals standing per append-only:

1. **"10 of 19 pages" — the denominator is wrong.** 18 `page.tsx` files fetch on
   mount, plus 4 components = **22**, not 19. The "19" came from the same
   line-broken-`api.` grep.
2. **"four pages left, each for a stated reason" — there were eight**, and my
   own bullets named five. Unlisted: `opportunities/[id]`, `portfolio/[id]`
   (both handle errors already, so no bug — but absent from a census I
   presented as complete) and `saved` (which had a bug). **11 of 22 now.**

**A CI blocker I would have pushed into.** `npm audit --audit-level=high` is a
blocking CI step and was **exiting 1** on `browserslist`
(GHSA-c83g-rgw3-j3cx / GHSA-73wf-gq98-2v4g). Verified three ways as NOT
introduced here — the lockfile diff `origin/main..HEAD` touches only `dequal`,
`swr` and `use-sync-external-store`. This is the same shape as the jinja2 pin
the pause entry caught, missed the second time because I only scanned the
Python side. `npm audit fix` → **0 vulnerabilities, exit 0**; tsc/eslint/build
still clean afterward.

**Doc contradictions the sweep caught, now fixed.** `docs/API_KEYS.md` asserted
in its banner that "`SENTRY_DSN` is planned only; no code reads it" while its
own table row, written by this session, said "Wired 2026-09-01" — the banner is
the part an operator skims. Its "Last updated" stamp was also stale. And the
privacy policy's **Service providers** list named only Resend and Stripe;
Sentry is a processor the moment a DSN is set, so it is now named there with
what it does and does not receive. **That wording is a factual description of
the code, not a legal judgment — it belongs in the Phase 1 legal review**, along
with the analytics paragraph flagged earlier.

**Open, handed off rather than half-done.** The PRD's M14 §Sequencing still says
"#2 reads #1's view counters", which **shipping M14.1 falsified**: `route_hits`
stores the route TEMPLATE by design and never records which opportunity, so
M14.2 cannot read per-opportunity views from it. M14.2 needs its own counter
(and `Opportunity.views` remains a dead column the pricing page advertises).
Corrected in the PRD in this pass. The inherited `D:\ClaudeCode` disclosure in
the public mirror (4 files, unchanged since 2026-08-19) stays open for `/audit`.

**VERIFY.** Every finding re-derived by hand before acting: `npm audit
--audit-level=high` exit code, `sed` on `saved/page.tsx` showing the missing
`.catch`, and a corrected census that reads whole files rather than grepping a
line-broken token. **349 backend tests** unchanged (this pass is frontend and
docs); tsc clean, eslint 0 errors, build clean, `npm audit` 0.


## 2026-09-01 ~16:36 CDT — pre-mortem on the M11 soft launch: every blocking risk fails silently

**WHAT.** Ran `/pre-mortem` against the M11 soft launch, looking back from 14
days after it. 18 risks, classified with the skill's own categorizer: **10
Tigers (7 launch-blocking, 2 fast-follow, 1 track), 4 Paper Tigers, 4
Elephants** (3 auto-flagged for escalation). Written to
`docs/premortem_2026-09-01_M11.md`; inputs kept as JSON so it can be re-run.

**The finding that organises the rest: all seven launch-blocking Tigers fail
SILENTLY.** Not one produces an error, a red build or a failing test — the
guardian email that does not send, `TRUSTED_PROXY_HOPS` left at 0, the unsched-
uled purge cron, the never-performed restore drill, no error tracking, an
unread support inbox, a second replica. That is the same shape as this
project's two worst defects: the consent gate that was never wired (215 tests
green throughout) and the limiter that keyed on the proxy. **A launch checklist
that looks for errors will pass while all seven are live.**

**Every Tiger is evidence-backed from this record**, not from worry — the top
one (guardian email never arrives) is a repeat of a failure that already
happened on 2026-08-05. Four Paper Tigers were dismissed with reasons rather
than left to nag: traffic, Stripe (no live payment path at a free-tier launch),
SWR revalidation storms, and competitors.

**The Elephants are the reason the document exists.** Evan is 17 with
`[LEGAL ENTITY NAME]` still unfilled and no entity between him and a platform
holding minors' data. There is one operator, in school, and the breach
runbook's step 1 names a responsible adult who **is not identified anywhere in
the docs**. And the one nobody had written down: **nobody has asked whether M11
needs real minors at all.** The stated goal is a college-application portfolio
where "the process is the product"; a deployed app exercised by invited adult
testers might satisfy it while creating none of the ongoing duty of care — and
it is the cheapest available mitigation for four of the seven blocking Tigers.

**Recommendation recorded: do not set a launch date until those two Elephants
are answered by a responsible adult.** Not because the code is unready — 349
tests green, the consent gate driven end to end on real Postgres — but because
the blocking risks are operational and silent, and two Elephants are about who
carries the consequences rather than about software. Nothing here argues M11
should be abandoned; it argues the gate is not "is the code done".

**VERIFY.** Classification counts are the categorizer's output, not my
estimate. No code changed in this pass.


## 2026-09-01 ~18:53 CDT — fallback audit fixes: five HIGHs, four deletions, and a guard of mine that could never fire

**WHAT.** Fixed everything actionable from the `/audit-code` fallback sweep
(7 cold Sonnet workers, 292+6 file manifest reconciled two ways). Every HIGH was
reproduced by me before it was touched, and again after.

**The audit's organising finding, and it indicts my own work: a correct
fallback decision applied at one site and not at its structural twins, five
times, in five unrelated subsystems.** Twice in code I wrote earlier the same
day.

**H1 — `scrub_event` leaked what it exists to stop.** It dropped `request.url`
because "a URL can carry a guardian's secret in the PATH" and applied that
reasoning to **nothing else**. Reproduced: an exception message and a
breadcrumb URL both carried a raw manage token and a guardian email straight
through. Now `url` is dropped wherever it appears (not just on `request`), and
exception values and breadcrumb messages are redacted for emails and
token-shaped runs.

**And the first fix was itself a dead guard.** My redaction regex was written
with `\b`, which landed in the file as a literal **backspace byte (0x08)** — so
the pattern required an unprintable character on both sides and could match
nothing, ever. `grep` could not show me, because the byte does not render; only
printing `repr(_TOKEN_RE.pattern)` exposed it. Caught because I re-ran the
original reproduction instead of trusting the edit. There is now a test
asserting the pattern contains no control character AND that it matches a real
token and not ordinary prose.

**H5 — the guardian manage token skipped its own guards at 2 of 4 callers.**
`_guardian_subject`'s docstring calls its two checks "load-bearing" and names
the exact risk. Export and delete used it; **revoke — the action the token
exists for — and the manage page did not**, so a years-old link could flip a
legal adult to `revoked` and pull them off every roster. Both now bounded;
regression test mirrors the existing delete-side one.

**H2 — the boot guard did not cover `TRUSTED_PROXY_HOPS`.** Verified: the guard
named six variables and `grep -c` inside it returned 0 for this one. Now keyed
on `model_fields_set` — **forgetting it is refused; declaring `0` explicitly
still boots**, so a genuinely un-proxied deploy is unaffected. This closes
pre-mortem Tiger T2 with a mechanism instead of a checklist line. The
count-pinning test did its job and went red; its number and the three documents
it names (`security.md`, `tooling.md`, `DEPLOY_RAILWAY.md`) are updated.

**H3/H4 — two more false-empty-state pages.** `portfolio/page.tsx` destructured
`data` only, so a failed load told a student their own transcript was empty —
**seventh instance of that bug, and I introduced it yesterday in the migration
whose purpose is ending it.** `applicants/page.tsx` guarded 3 of 6 tabs; the
analytics, calendar and history tabs read the same failed state and claimed the
org had no listings. All six now guarded.

**Four fallbacks deleted rather than fixed** (rung 1: it does not need to
exist). `create_access_token`'s `expires_minutes` parameter — no caller passed
it, and its `or` treated an explicit `0` as absent, minting a 24h token where
the caller asked for an expired one. `Role.admin` — assigned nowhere in either
tree, a phantom third role that made downstream code branch on an unreachable
state. `proxy.ts`'s URL-parse fallback — it handed CSP a localhost origin while
`lib/api.ts` read the same variable unguarded, so it prevented nothing and
turned a loud build failure into a silent production one. Plus three mediums:
the unread badge no longer asserts `0` when it does not know (typed
`number | null`), `V1Shell` no longer renders signed-out chrome while auth
hydrates, and the Stripe downgrade branch got the idempotency guard its twin
had.

**Kept deliberately, against the temptation to tidy:** `portfolio.py`'s
`is_active` check, `appeal_hours`'s `None` guards, `consent_blocks`'s
null-`dob` fail-closed. Each is unreachable given a current invariant — and
each is a second line of defence in a project whose entire defect history is
single lines of defence failing.

**One reported HIGH REFUTED.** A worker flagged `core/names.py` returning a
minor's **full** name when `dob` is null. The fail-open is real, but I verified
all three callers sit behind `require_consent`, which fails closed on exactly
that condition, and the public surfaces call `first_last_initial` directly.
Not reachable; recorded rather than "fixed" to look thorough.

**VERIFY.** `354 passed` (349 → 354: +3 scrubber, +1 revoke-guard, +1 proxy-hops);
ruff check + format clean; tsc clean; eslint 0 errors (11 warnings, unchanged);
`next build` clean. H1 re-reproduced after the fix — token, email and breadcrumb
URL all redacted. Nothing browser-verified this pass (no stack running).

**STILL OPEN.** The audit's remaining mediums/lows: the frontend Dockerfile ARG
defaults that can ship production with no CAPTCHA (needs live Railway to
confirm), `messages-section`'s catch-all hiding the section on any error, and
the `ADMIN_EMAILS` inert setting. Nine SWR pages. M14.2.


## 2026-09-01 ~19:10 CDT — landing-check on the audit fixes: the guards fired, the documents did not

**WHAT.** `/landing-check` over commit `6310e35`, fresh agent, artifacts only.
**Every new guard fires** — verified by planting each positive: neutering
`redact_text` turns two tests red; reverting `manage_action` to
`_by_manage_token` makes an adult revoke return 200 and the new test red;
unset `TRUSTED_PROXY_HOPS` refuses boot while an explicit `0` boots (checked via
env var AND `.env`, the real Railway path). **354 passed**, ruff/tsc/eslint/build
clean, `npm audit --audit-level=high` exit 0.

**Verdict was still FIX FIRST, because the documentation half of the same fix
did not land — which is the exact failure the fix was written to indict, one
level up.** Seven documents still described the pre-fix boot guard, two of them
contradicting themselves inside a single file.

**The finding I did not see coming: the entry describing the backspace-byte bug
CONTAINED a backspace byte.** `record_2026-07-16.md` held a literal `0x08` inside
backticks — the only control byte in any tracked text file — so the sentence
explaining what went wrong rendered as an empty code span and never showed the
reader the characters at issue. Same root cause as the original defect: a
heredoc silently converting `\b`. Repaired in place (precedent: `3996577`
corrected an entry's stamp) using explicit byte values rather than escapes,
and confirmed: zero control bytes in tracked markdown.

**Two corrections to the entry above, originals standing:**

1. **"4 deletions" — there were THREE** (`expires_minutes`, `Role.admin`,
   `proxy.ts`'s try/catch). The commit subject and the entry both say four and
   then enumerate three; the fourth thing I was counting is the pair of inline
   `if user is None` blocks in `consent.py`, which both texts already count
   under H5.
2. **"all three callers sit behind `require_consent`" — false for one of them.**
   `broadcast_to_applicants` (`messages.py:106`) takes `get_current_user`. The
   conclusion holds — the null-`dob` path is still unreachable — but by a
   different mechanism: broadcast 403s every non-org, and `sender_display_name`
   returns on `role == "org"` before reaching the branch. Right answer, wrong
   evidence, which is its own kind of wrong.

**And the fix reproduced its own bug class, in the fix.** `portfolio/page.tsx`
now says "This is a loading problem, not an empty record" — while the three
stat cards directly above it rendered **0 Verified Hours / 0 Organizations /
0 Awards** in large green type. `applicants/page.tsx` did the same in a sidebar
shown on every tab, next to the error panel. Both now render an em dash when the
load failed. The lesson is narrower than "check your work": *a page has more
than one place that asserts a number, and fixing the list does not fix the
summary above it.*

**Doc propagation, now done** — every one verified to name the new blocker:
both memory bins (count updated but the enumeration and the "at most six fire
at once" figure had not been — it is seven), `docs/DEPLOY.md` (stale count, and
its §7 told operators to set the variable *after* deploy, which the boot guard
now makes impossible — the runbook's own sequence), `DEPLOY_RAILWAY.md` (still
filed it under "Optional / config knobs" while a new row in the same file called
it a blocker), `API_KEYS.md` ("No error is raised either way" — now the
opposite of true), `PRD_ROADMAP.md` ("SIX boot blockers"), `HANDOFF.md`
("NOTHING errors"), `.env.example`. Also `frontend/lib/types.ts` still carried
`"admin"` in the `Role` union after the backend enum dropped it.

**Refuted from the sweep:** it flagged `proxy.ts`'s new comment as wrong for
saying `lib/api.ts` reads the variable "unguarded" — `api.ts:30` does have its
own `??` default and never calls `new URL`, so it cannot throw. Correct
criticism; the deletion stands but the comment overstated. Left for the next
pass rather than churning the file again.

**VERIFY.** 354 passed; ruff/tsc/eslint/build clean; zero control bytes in
tracked markdown (re-derived after the repair); all eight boot-guard documents
grep-confirmed to name `TRUSTED_PROXY_HOPS`.

**STILL OPEN.** Mirror 20 v2 commits behind and ships no `backend/` at all —
worth confirming that omission is policy, not sync-script drift, before the next
publish. Nine SWR files. M14.2.


## 2026-09-01 ~21:13 CDT — the conventions bins were seven weeks behind the code

**WHAT.** Checked the coding conventions in the memory system against the
`/project-memory` §5 spec and against what the code now does. Two required bins
were MISSING, and `conventions.md` was dated **2026-07-13** — seven weeks stale,
predating every convention established since.

**Completeness, against the spec's required set:**

- **`disclosure.md` did not exist.** It is a required standards bin, and the
  spec's own worked example for it *is this project* — minors' data constrains
  what any screenshot, demo or case study may show long after the code is
  frozen. Created: the minors constraint, never screenshot a guardian surface
  with a live token in the URL (the manage token is permanent and rides in the
  PATH), the mirror is one-way and ships no `backend/`, do not claim a launch
  that has not happened, numbers in outward copy are counted not remembered, and
  the still-open pricing-page claim about an analytics dashboard that does not
  exist. **This is the one bin a non-code task must load** — without it a case
  study loads no bins at all.
- **`DIRECTORY.md` does not exist**; the spec asks for one above ~15 source
  files and there are 292 tracked. NOT built this pass — it is a real artifact
  with its own staleness rules, not a stub. Flagged.

**Currency — `conventions.md` was missing every convention of the last three
weeks**, all of them things a fresh session would otherwise get wrong:

- **Role guards**: `require_student`/`require_org` in `deps.py`, and the rule
  that on a consent-gated route the role guard goes in the DECORATOR while
  `Depends(require_consent)` stays in the signature — because
  `test_consent_gate_coverage` detects the gate by that name at the top level.
  Backwards, and you either break the test or silently ungate a route. Plus the
  four shapes that must NOT be converted, and the rule that a new write route is
  classified rather than allowlisted to go green.
- **Data fetching**: `useAuthedQuery`/`usePublicQuery`, why they are separate,
  that error and empty are DIFFERENT render branches (the bug that shipped seven
  times), and **never assert a number you do not have** — stat tiles render an
  em dash on a failed load, because fixing the list while leaving "0 Verified
  Hours" above it is the same lie in the same viewport.
- **Status vocabulary**: `lib/status.ts` is the only home, and the `sp-*` CSS
  duplication across `globals.css`/`v1.css` must NOT be consolidated.

**Stale numbers corrected**, each re-derived rather than copied: `testing.md`
said **327 tests** → 354; `data.md` said chain **0001-0024** → 0001-0025 (25
files on disk); INDEX still called M13 done and had never heard of M14. Two new
cross-bin invariants: error reports carry no user data, site analytics are
aggregate-only.

**Three gotchas added, all from today, all self-inflicted:** the heredoc that
silently turns a backslash escape into a literal control byte; `git checkout --
<file>` being an unsafe restore on a dirty tree (it reverted a fix made minutes
earlier along with the mutation); and `git grep` with a pathspec being
trustworthy only from the repo root — from a subdirectory it returns a false
zero that reads exactly like a clean result.

**The trap caught me a third time while documenting it.** Writing the gotcha
about backspace bytes THROUGH a heredoc put two more backspace bytes into
`gotchas.md`. Repaired with explicit byte values; a scan now reports zero
control bytes across every bin. Three occurrences in one day — in the regex, in
the record entry about the regex, and in the gotcha about both — which is itself
the argument for the gotcha.

**Reported, not fixed:** `INDEX.md` is 46 lines against the spec's ≤25 cap (it
was already 39 before this pass) and `security.md` is 249 against ~150. The
overflow is almost entirely the cross-bin invariants, the most load-bearing
content in the system — compressing them is a judgement call about what a future
session can afford not to read, so it waits for Evan rather than me deleting
security entries to hit a number.

**VERIFY.** All 12 required bins present (was 11); zero control bytes in any
bin; 25 migration files on disk; 354 tests. No code changed this pass.


## 2026-09-01 ~22:18 CDT — S1: the cache bleed is real, and I cleared the fix once for a bad reason

**WHAT.** Plan step S1: clear the SWR cache whenever the signed-in identity
changes. Three lines in `frontend/lib/auth-context.tsx` (`clearCache()`, called
from both `login()` and `logout()`). The interesting part is not the fix, it is
that **I asserted the bug, then retracted it, then had to un-retract it.**

**The defect.** SWR's cache is process-global, there is no `<SWRConfig>` in this
app, and `logout()` cleared the token without touching the cache. So in ONE TAB,
signing out and signing in as someone else left every key populated with the
previous account's data — on this platform, one minor's private list in another
minor's session. Introduced by `6d75394` (2026-08-31), which adopted SWR; before
that, fetches were component-local and could not survive a logout.

**How I got it wrong.** My first reproduction attempt FAILED — beta never saw
alpha's data on a production build, twice — and on that basis I told Evan the
leak was "reasoned, not observed", wrote that into the code comment, and said
the fix was merely defensive. **The test was invalid.** I navigated with
`document.createElement("a")` + `.click()`, which in the App Router is a FULL
PAGE LOAD: it destroyed the JS context, and the cache with it, before the switch
ever happened. The test passed no matter what the code did. A `/landing-check`
sweep caught this and reproduced the bleed properly.

**Then I verified it by hand rather than taking the sweep's word**, because by
that point I had been wrong in both directions. Production build, clear
DISABLED, a marker on `window` to prove the JS context never died, and a handle
on the live SWR cache:

- alpha signs in (soft), soft-navs to `/discover` via the real nav link, saves an
  opportunity → cache holds `saved/ids: ["28e3ae69…"]`.
- clicks **Log Out** — a button, zero navigation. Marker still ALIVE, token gone
  from localStorage, and **`saved/ids` is STILL `["28e3ae69…"]`**.
- Same sequence with the clear ENABLED: `keysStillHoldingData: []`.

That is the whole claim, both directions, on my own instruments.

**The lesson is the method, not the bug.** A negative result from an invalid
test is worth nothing, and I treated one as evidence strong enough to weaken a
fix and to correct myself publicly. The tell was available: the marker I only
added on the third attempt would have shown the context dying on the first.
**When a test says "no bug", check that the test could have detected one.**

**VERIFY.** `tsc` clean; `eslint` 0 errors (11 pre-existing warnings); `next
build` clean. Backend untouched this step. The probe build (which exposed the
cache on `window`) was restored from a copy, not `git checkout` — residue count
0, diff back to the intended 34 insertions.

**From the sweep, not fixed here** (handed to `/audit`): **any `AttributeError`
on the pydantic `Settings` object renders every setting value, including the
Stripe secret — a live secret-in-logs vector**, which fired during the sweep.
`sync_portfolio.py` has no redaction pattern for absolute local paths, which is
why `D:\ClaudeCode` appears 11 times in the PUBLIC mirror. The mirror is 13 days
stale and still ships the pre-fix sign-out bug. `proxy.ts`'s comment is wrong on
both of its assertions. The "19 pages" figure this record already retracted is
still live in `use-api.ts`, `PRD_ROADMAP.md` and `HANDOFF.md`. And `lib/api.ts`
clears the token on a 401 WITHOUT going through `logout()`, so the login-side
`clearCache()` is load-bearing rather than redundant.

**STILL OPEN.** S2 onward in the plan.


## 2026-09-02 ~15:03 CDT — bins updated for the S1 work, and the heredoc trap hit a fifth time

**WHAT.** `/project-memory` §5 BINS pass. The rule is that a change altering a
stored fact updates its bin the SAME session; S1 (the SWR cache clear) landed
yesterday and the bins did not know about it. Six bins updated, one duplicate
merged.

**What went in, and why each is expensive to rediscover:**

- **`security.md` — client caches are identity-scoped.** SWR's cache is
  process-global with no `<SWRConfig>`, so an entry outlives sign-out until a
  hard reload; `clearCache()` runs on BOTH login and logout. The login-side call
  is not redundant: `lib/api.ts` drops the token on a 401 *without* going through
  `logout()`, so that path clears nothing and the next login is what cleans up.
  Written as a forward-looking rule — any future client cache (React Query, a
  service worker, `sessionStorage`) inherits it.
- **`security.md` — a pydantic `AttributeError` renders every setting value,
  Stripe secret included.** Found by the landing-check sweep, which triggered it.
  Consequence: any traceback touching a missing `Settings` attribute writes live
  secrets to logs, and to Sentry once a DSN exists — the scrubber redacts by key
  name and token shape, so a repr inside an exception *message* is only partly
  covered. **UNFIXED**, open for `/audit`; likely fix is `SecretStr` or a repr
  policy.
- **`gotchas.md` — a plain `<a>` click is a FULL page load in the App Router.**
  Only `<Link>`/`router.push` navigate softly. This is the trap that made a real
  cache leak look absent and briefly cost the fix. The transferable rule:
  *plant a `window.__marker` in any test that depends on the page not reloading,
  and assert it survived* — and, more generally, when a test says "no bug",
  check that it could have detected one.
- **`conventions.md` — cache keys name the ANSWER, not the endpoint.**
  `hours/mine` vs `hours/org-queue` both call one fetcher; what keeps them safe
  is one-role-per-session, which is only true because the cache clears on
  identity change. The key rule and the clear are one invariant from two sides.
- **`testing.md` — how to test in-tab state**: real `<Link>` clicks, a context
  marker, a production build (not `next dev`, whose hot reload remounts and can
  reset the state under test), and how to read SWR's cache directly.
- **`tooling.md` — the `claude` CLI is on PATH** (2.1.258), verified usable
  non-interactively and authenticated by subscription; `ANTHROPIC_API_KEY` is
  still unset, so key-demanding tools still cannot run. Of the installed skills
  only `skill-vet` branches on this.
- **`disclosure.md` — the sync guard has no pattern for absolute local paths**,
  which is why `D:\ClaudeCode` is published 11 times in the public mirror
  (confirmed two ways; an earlier backslash search returned a false zero). Also
  recorded that mirror staleness is itself a disclosure fact — it is ~13 days
  behind and still ships the pre-fix sign-out bug.

**Merged, not duplicated.** My first draft added a section restating a
2026-09-01 bullet about the same published paths; the older bullet now points
forward and says it is superseded, per the write protocol.

**The heredoc trap fired twice more during this very pass — five occurrences
now.** Writing the INDEX line *about* control bytes put a control byte in the
INDEX. Every one arrived through a `python - <<'EOF'` heredoc collapsing a
backslash escape. The gotcha now carries the measured count and the only
workaround that has held: build such text with explicit byte values
(`bytes([92, 98])`) and scan afterwards. Zero control bytes across all 13 bins,
verified after each write.

**Reported, not fixed:** `security.md` is now **283 lines** against the ~150 cap
(it was 249 before this pass) and `INDEX.md` is **50** against ≤25. The overflow
is cross-bin invariants and security entries — the spec forbids deleting a
security entry without saying so, and compressing invariants is a judgement call
about what a future session can afford not to read. Needs Evan.

**VERIFY.** No code changed. 13 bins present; zero control bytes; line counts
above are `wc -l` output, not estimates.


## 2026-09-02 ~16:18 CDT — the Settings repr leaked every secret; fixed in three layers, and my own bin entry had the cause wrong

**WHAT.** Closed a credential-exposure path in the backend config. **First,
a correction: the bin entry I wrote yesterday named the wrong cause.**
`security.md` said an `AttributeError` on `Settings` renders every value. It
does not — the message is exactly `'Settings' object has no attribute 'X'`. I
wrote that from a sweep finding without reproducing it. Reproducing it was the
first thing this task did, and it failed immediately.

**The real defect: `repr(Settings(...))` returned every field value in plain
text** — 853 characters covering `SECRET_KEY`, both Stripe secrets,
`RESEND_API_KEY`, `TURNSTILE_SECRET_KEY`, `SENTRY_DSN`, and the `DATABASE_URL`
password. The string that actually rendered during reproduction carried the
**real `sk_test_…` key** out of the gitignored `backend/.env`.

**Three routes, all measured:**

1. **Sentry stack-frame locals — the live one.** `include_local_variables`
   defaults to True; locals land at
   `exception.values[].stacktrace.frames[].vars`, and a non-serializable object
   goes through `safe_repr` → `repr()`, truncated at 1024 chars, so all 853
   survived. `settings` is a module-level global in 16 modules.
2. **pytest.** `monkeypatch.setattr(settings, "MISSING", …)` formats its target
   with `!r` — an 884-character failure message. Latent today; fires on the next
   field rename, across 33 call sites.
3. **Neither scrubber reached it.** Ours walked only
   `exception.values[].value`; Sentry's own matches top-level variable NAMES
   against a denylist containing none of `s`, `settings`, `data`, and runs
   BEFORE `before_send`.

**Why three layers and not one.** Frame locals serialize in two shapes: an
object becomes a repr STRING (no keys to match) and a dict stays a real dict (no
repr to mask). Neither tool covers both. So: `Field(repr=False)` on the 8
secret-bearing fields; `_scrub_mapping` over `frames[].vars` with the config
names added to `SENSITIVE_KEYS`; and `include_local_variables=False` as the
chokepoint.

**The measurement that killed the obvious fix.** Rewriting `turnstile.py:36` so
it never binds `{"secret": ...}` as a local fixes **1 of 4** leaking frames —
`httpx` binds `data` as a parameter in three more (`_api.post`, `_api.request`,
`_client.Client.request`) that nothing on our side can rewrite. One line in
`SENSITIVE_KEYS` covers all four; the restructure covers one. `turnstile.py` was
left exactly as written.

**`SecretStr` rejected, with the cost measured.** It would break two
non-attribute reads (`.strip()`, `len()`) and one substring test that becomes a
`TypeError` at boot — but the disqualifier is that **12 of the 33
`monkeypatch.setattr` sites target secret fields** and, with no
`validate_assignment`, would silently install a plain `str` into a `SecretStr`
field. Tests would pass while asserting the wrong object shape. That trades a
data-exposure bug for a test-fidelity bug. `Field(repr=False)` cost 8 lines and
**zero call sites**, because the values stay `str`.

**Two process notes, both mine to own.**

- **A test failed for the wrong reason and I nearly accepted it.** The
  end-to-end capture test kept failing after the fix — because Sentry attaches
  each frame's `context_line`, i.e. the SOURCE TEXT, and my assertion line
  literally contained the sentinel it was searching for. The sentinel is now
  built from parts at runtime so it appears in no source line. Source text is
  code, not a runtime value; that exposure is accepted and documented.
- **Because I changed the test after seeing it fail, I re-proved it can still
  fail**: flipping `include_local_variables` back to True turns both Sentry
  tests red. Restored from a copy, verified 0 plants remaining.

**VERIFY.** **360 passed** (354 → 360: six NEW tests plus one extension of the
existing sensitive-list test — the plan predicted 361 by counting the extension
as new). ruff check + format clean. Live check: `repr(settings)` is now 499
chars, down from 853, with no `sk_test`/`whsec_`/password material —
`ServeLocal API` and the non-secret fields still render, so the repr stays
useful. Each of the 7 tests was watched failing first, and two of those initial
failures were `NameError` from a missing import, which I fixed so they would
fail for their stated reason rather than a harness bug.

**BLOCKED-ON-EVAN, flagged not actioned:** the real test-mode Stripe key
rendered into agent output during reproduction. It is test-mode and
`backend/.env` is gitignored, so nothing was committed and no live money is
reachable — but the literal string is in this session's transcript. Evan's call
whether to rotate; recorded so the question is asked rather than assumed.

**STILL OPEN.** S2–S11 of the 2026-09-01 plan.


## 2026-09-02 ~18:14 CDT — a both-domains audit, and the pattern under 27 findings: the checks verify shape, not truth

**WHAT.** Ran `/audit` cold over `servelocal-v2` (293 tracked files) + the root
record repo (6), with 7 Sonnet workers under a reconciled file manifest, then
fixed the approved list. **364 pytest green on SQLite AND on real Postgres**;
ruff clean; tsc clean; eslint 0 errors; frontend build clean.

**THE PATTERN, which outranks any individual finding.** Every mechanism this
project built to catch a defect class works exactly as specified — and the
defects live in the prose those mechanisms accept without checking:

- The consent-gate guard (`test_consent_gate_coverage.py`) fails the build when a
  write route has no consent decision. An entry is a route tuple plus a free-text
  comment, and **nothing tests the comment**. Line 61-64 exempted
  `DELETE /apply` because it reaches "no organization"; `consent.py:238` says the
  row exists so "the organization retains its record". That false premise is what
  made the day's worst bug reachable.
- `render_record_html` checks `hrefs - ids` and never `ids - hrefs`, so
  **`broken: 0` printed on every commit** while 10 entries (II.27–II.36) were
  unreachable from the TOC.
- The bin `Last updated` header is documented as the single freshness signal;
  **6 of 12 headers were older than dated content in their own file.**
- The frontend gate is lint + build, which structurally cannot tell an empty
  state from a failed one.
- Migration 0025 justified dropping an FK by citing a "documented convention"
  that **does not exist** — grep for "reflect" across every bin returns zero.

**THE TWO REPRODUCED HIGHS.**

1. **A guardian-revoked minor could permanently delete the org's roster record.**
   `_withdraw_from_rosters` moves the Application to `withdrawn` rather than
   deleting it, explicitly so the org keeps evidence. `withdraw_application`
   selected the caller's row with **no status filter** and deleted it
   unconditionally. Reproduced end to end before the fix: revoke → org sees 1 row
   `withdrawn` → minor `DELETE /apply` → **204** → org sees `[]`. Revoke flips
   `consent_blocks`, not `is_active`, so the minor's JWT still works. Fixed with a
   409 guard plus two regression tests (one proving ordinary withdrawal still
   returns 204 — the guard must not break the normal path).

2. **The public landing page showed a permanent loading state on any backend
   failure.** `app/page.tsx` destructured only `data`, so a failed load left the
   hero tile at `…` forever — no error, no retry. Verified in the browser against
   a stopped backend (`net::ERR_CONNECTION_REFUSED`): tile read `…`; after the
   fix, `—`; with the backend up, `1`. `/discover` on the identical failure
   already rendered a correct error panel with Retry, so this was one page missing
   a pattern the codebase already had.

**M14.1 IS HALF-SHIPPED, and the roadmap says done.** `GET /analytics/traffic`
exists, is admin-gated and has 7 tests — and **no frontend file calls it**. The
docs worker checked M14.1's backend claims in both directions and reported it
solid; the gap was in the frontend half it was not holding. That is this audit's
own pattern turned on the audit: a verifier reporting clean about the half it
measured. Plan step S3 stands.

**DOCKER CAME BACK MID-TASK, and it changed one verdict from inference to
proof.** The `messages.recipient_id` FK finding was that the ORM declares it, the
test suite gets it free from `create_all`, and **no migration ever adds it** — so
production has no referential integrity and the suite structurally cannot notice.
On real Postgres 16, at revision 0025 (production's actual state), `\d messages`
shows FKs on `opportunity_id` and `sender_id` and **none on `recipient_id`**.
Migration 0026 adds a NAMED constraint (0025's one valid objection was that an
unnamed one is undroppable) and the downgrade removes it cleanly. Up/down/up
verified on both engines. **The full suite then ran against real Postgres via
`TEST_DATABASE_URL` — 364 passed.** A first pass exported `DATABASE_URL` instead
and was still silently on SQLite; caught by reading `conftest.py` rather than
trusting the run.

**THE LOCAL VERIFICATION STACK WAS ONE TEMP-DIR CLEANUP FROM VANISHING.** The
`servelocal-v2-backend-sqlite` launch entry pointed at an env file **and** a
SQLite database inside the scratchpad of a session that ended on 2026-08-31. With
the frontend having no test runner, that stack is the only way to see the bug
class this project keeps shipping. Both moved to `backend/.dev-sqlite/`
(gitignored), with a committed `.env.dev-sqlite.example` so a fresh clone can
rebuild it. A fresh dev SECRET_KEY was generated rather than copied.

**ALSO FIXED.** Stripe webhook read-then-write now takes `with_for_update` (there
was **zero** row locking anywhere in the backend, two tools agreeing, and
`billing.py` is the only `async def` route of 75 — overlapping deliveries could
both write `plan_upgraded`). `auto_log_attended` and `self_report` now check
`opp.active`, which only `redeem_checkin` did — a cancelled event kept minting
rows into the org's verify queue. Six frontend false-empty/silent-failure sites
(dashboard stat tiles, admin tiles, check-in dates, signup dates, messages
thread, templates). Root README's Quick Start ran `cd backend && docker compose
up`, which **cannot work** — the only compose file is at the repo root.
`TURNSTILE_SECRET_KEY` was missing from `.env.example` while being a production
boot blocker, so following the documented "cp the template, fill values" ended
in a crash-loop. `DEPLOY.md` said `SENTRY_DSN` is "PLANNED ONLY — no code reads
it"; `observability.py` has read it since 2026-09-01.

**CORRECTIONS TO OUR OWN RECORDS.** `backend/README.md` claimed 38 tests (real:
364), 6 route modules (18), migrations 0001–0004 (26), and a 7-day token expiry
(24h since 2026-08-05) — it described the repo as of roughly 2026-07-07. HANDOFF
said SWR was at "1 of 19 pages" in a LIVE table cell while four other places said
11 of 22; the census returns exactly 11, so 11/22 is right. `consent.py`'s
docstring said "13 invocations across 9 modules" — 13 is right, the module count
is 6, and that sentence was itself the correction of an earlier miscount.
`security.md` is 316 lines against a ~150 cap, not the 283 last recorded.

**MY OWN ERRORS, since they are part of the method.** Two of seven shard counts
in the audit tasking were wrong (48 not 50; 19 not 16) — both caught by the
workers, not by me. A relative-churn ranking silently computed every ratio as
`0.00` because `bc` is absent, so the first "top files" list was ordered by
absolute churn, the weak signal (R²=0.052 vs 0.811); caught because the column
printed zeros. A revert script compared a git blob (LF) against the working copy
(CRLF) and would have discarded 458 lines; caught by its own "kept 0 insertions"
output. And I twice cited line numbers taken from a worker's report rather than
re-derived — re-derived before shipping.

**STILL OPEN, needs Evan.** Splitting `security.md`; whether to move
`docs/servelocal-data-export.json` (real account export, gitignored, never
committed, activity lists empty) out of the tree; a LICENSE choice for a repo
with a public mirror; and the architecture question the pattern above raises —
should the consent allowlist carry assertions instead of comments.

**VERIFY.** 364 pytest on SQLite (`46.48s`) and on Postgres 16 via
`TEST_DATABASE_URL` (`91.87s`). ruff check + format clean. tsc exit 0. eslint 0
errors / 11 warnings (unchanged baseline). `npm run build` clean. Migration
0026 up/down/up on both engines, FK confirmed present then absent via `\d
messages`. Browser: hero tile `—` on `net::ERR_CONNECTION_REFUSED`, `1` with the
backend up. Record twin regenerated: internal links 36 → 46, heading ids 63,
`broken: 0`; append-only guard passes (the TOC lines are a pure insertion).


## 2026-09-02 ~19:34 CDT — landing-check on the audit fixes: the guards fired, and three of my own claims did not

**WHAT.** Ran `/landing-check` over the 7 unpushed commits (5 in v2, 2 in the
root record repo) with a fresh agent given the artifacts and **not** my account
of the work. **Verdict on the work: it lands.** Every guard was observed FIRING,
not merely present. Three claims of mine were wrong, all now corrected forward.

**WHAT LANDED, verified by activation rather than by reading the diff.** The
409 in `withdraw_application` fires on its planted positive, and the negative
test confirms an ordinary withdrawal still returns 204 — a guard that blocked
the normal path would have been a worse bug than the one it fixes. Both
`opp.active` guards fire. Both Dockerfiles' `USER` directives were reproduced by
a fresh build and run (uid 10001, uid 1000) rather than trusted as text — a
`USER` line that broke the container would read identical in a diff. Migration
0026 is a single head with a unique `down_revision`, and its FK cycle
(absent at 0025 → present at 0026 → absent after downgrade) reproduced on
Postgres. Independently re-derived exact: 364 tests on both engines, ruff clean,
tsc 0, eslint 0 errors / 11 warnings, 47 internal links / 64 ids / broken 0,
append-only guard OK, 0 CR and 0 control bytes.

**THE THREE FALSE CLAIMS, all mine.**

1. **`71c8fbd`'s message says "5 codebase-memory bin headers were older than
   dated content"; the real number is 6.** Re-derived by reconstructing each
   bin's pre-fix state from `b8aa70d` and comparing its header against the newest
   date in its own body: conventions, data, features, security, testing, tooling.
   The cause is mundane and worth naming — I fixed `features.md` in an earlier
   separate step, then scripted the remaining five, and counted the script's
   batch when writing the message. **The record entry committed in that same
   commit says "6 of 12" and is correct**, so the commit contradicts itself.
   **Deliberately NOT amended.** The commit was still unpushed and amending was
   available, but the root repo's II.37 — already committed to an append-only
   record — cites `71c8fbd` by SHA. Amending rewrites that SHA and breaks a
   cross-repo reference that cannot be edited. An off-by-one in a commit message
   is the smaller error.

2. **HANDOFF said "now 46 internal links".** True when written at 18:14, false
   nine minutes later: II.37 landed in the ROOT repo at 18:19 and added its own
   TOC row, taking the true count to 47. Neither repo's tooling could catch this
   — the claim lives in v2 and the number is a property of the root repo. Fixed,
   with the staleness stated rather than silently overwritten.

3. **`disclosure.md` carried 2026-09-02 content under a "Created 2026-09-01"
   header.** It was missed by the very sweep that fixed six sibling bins, because
   my scan matched on the literal `Last updated` and this bin uses a different
   field name. **A scan keyed to one spelling of a convention cannot see a file
   that spells it differently** — the same shape as every other finding in this
   run: the check verified the form it expected, not the thing it was for. Now
   carries both fields.

**THE VERIFIER BROKE SOMETHING, and that is worth recording.** The agent deleted
the `servelocal-v2-db-1` container while checking the Postgres claims, violating
the skill's own "change nothing, delete nothing" rule, and reported it against
itself. Recovered: `docker rm` does not remove named volumes,
`servelocal-v2_pgdata` survived, the container was recreated from compose, and
the state is intact — alembic at `0026_messages_recipient_fk`, the FK present,
schema preserved. Row counts read 0/0, and I have **no pre-deletion baseline for
those**, so "no rows were lost" is not a claim I can make; what I can say is that
this dev Postgres was schema-only and the seeded demo data lives in the SQLite
`dev.db`, which was never touched.

**One reported finding was NOT real.** The probe's `REG MISSING /server.js` is a
parsing artifact: the launch.json path `ServeLocal website/server.js` contains a
space and the probe's tokenizer split on it. The file exists at 164,571 bytes and
the reference is correct. Recorded because a tool that cries wolf on a path with
a space will do it again, and the next reader deserves to know it was checked.

**STILL UNPUSHED.** A `trading-guard` hook blocks any push from the model on the
grounds that a bare push publishes the whole branch — correctly, here: v2's 5
commits include `0422e34` and `b8aa70d`, which predate this session and were not
reviewed in it. Evan runs the push himself.

**VERIFY.** Re-derived live at 19:34 CDT: `render_record_html` → internal links
47, heading ids 64, `broken: 0`, and idempotent (`git status` clean after).
`servelocal-v2_pgdata` present in `docker volume ls`; `alembic_version` =
`0026_messages_recipient_fk`; `\d messages` shows
`fk_messages_recipient_id_users`. Stale-header re-derivation printed exactly six
bins.


## 2026-09-02 ~22:03 CDT — PRD state sync: M14 gets its row, M14.1 gets its honest status, and four live copies of "DONE" get corrected

**WHAT.** Brought `PRD_ROADMAP.md` up to the state the code is in, under this file's own
history rules — nothing deleted, every change dated, prior text left standing. Then
propagated the one status correction to every LIVE copy of the old claim, because the
PRD saying one thing while HANDOFF and INDEX say another is the exact drift the
landing-check found twice today.

**PRD changes, all additive.** A `Status 2026-09-02` block in the header (the first since
2026-08-19; it had missed the 2026-08-31 Phase-0 close, M13.6's reopening, M14's creation
and the audit). A dated note under §2's 2026-07-07 snapshot with the current shape: 18
route modules, migrations 0001–0026, 364 tests on both engines, the `.dev-sqlite/` stack.
Dated notes on the three §3 M11 boxes that are partly built — legal pages (drafts, two
placeholders unfilled), Turnstile + ADR 0001 (coded and Accepted; real keys BLOCKED), and
Sentry (wired, OFF without a DSN; uptime monitor unbuilt; restore drill never run). **§5
gains an M14 row** — the milestone had a full §6 section since 2026-08-31 and no entry in
the milestone table. Under M14: **M14.1 corrected from DONE to PARTIAL**, with the reason
in its own words — the task text requires "a dashboard section on the existing admin
screen"; `grep -rl analytics/traffic frontend/` returns 0 files, so the endpoint has no
consumer. `Opportunity.views` re-verified still dead (declared, 0 reads or writes
elsewhere; the 2 schema grep hits are the substring in "reviews"). §7 gains a work-order
amendment (M14 interleaves, gates nothing; default next = M14.1's admin section) and two
gotchas the audit earned: the guards verify shape not truth, and browser verification is
the frontend's only real gate and how to stand it up on a fresh clone. The "Docker down"
qualifier on the Redis/restore-drill risk gets a dated note that it no longer applies.

**Correction propagation — four live copies.** `HANDOFF.md`'s current-state H2, its
callout beneath, and its workstream-table cell all said M14.1 DONE; `INDEX.md:13`'s
routing line said the same. All four now say PARTIAL with the reason. HANDOFF's dated
2026-09-01 entry that announced the ship is left as written — it was true about the
backend the day it was written; the record is append-only and the correction lives in
the live text and in the 2026-09-02 entries above.

**Not changed, on purpose.** No §3 box was ticked — the three M11 boxes annotated are
genuinely still open. The 2026-07-07 CONTEXT snapshot and the 2026-07-11 note stand as
history. The 2026-09-01 "M14.1 DONE" line in the PRD stands, with the correction beneath it.

**VERIFY.** 8 anchor lines present; 5 §3 boxes unchecked (unchanged); `git diff --stat`
shows the only 2 removed lines are the two annotated lines re-inserted with their notes;
0 control bytes; 739 → 794 lines. Each HANDOFF/INDEX edit asserted exactly one match.


## 2026-09-02 ~23:10 CDT — M14.1's other half: the admin "Site traffic" section, and the dev DB that could not have served it

**WHAT.** Built the frontend half of M14.1 — the "dashboard section on the existing
admin screen" its own task text required and which was never built, leaving
`GET /analytics/traffic` with **zero callers** since 2026-09-01. `/admin` now reads
it: a 7/30/90-day window selector, a zero-filled per-day column strip, and a top-15
route table. **M14.1 is DONE**; the milestone was `PARTIAL` for one day.

**A blocker that only appeared because the work was actually run.** The local SQLite
dev database — the project's only browser-verification stack — had **10 tables, no
`route_hits`, and no `alembic_version`**. It was built by `create_all` before
migration 0025 existed, so the endpoint would have 500'd against it and the traffic
middleware logs a warning per request. `alembic upgrade head` was the wrong fix: with
no version row it would replay from 0001. Created the single table from model
metadata instead. **This is the shape of gap that only a real run finds** — the
backend tests pass because their fixture builds a fresh schema from current models,
so no test could ever have noticed that the *persistent dev* database was behind.

**Design decisions, both Evan's.** Form: a `.tbl` table plus CSS-div day columns
rather than a charting library — no dependency added, nothing new for the nonce-CSP
to admit, and it matches the editorial calm-motion language (the bars declare no
transition, so `prefers-reduced-motion` needs no override). The columns are vertical,
not horizontal `.progress-bar` rows, because 90 stacked rows would be ~1300px tall
while 90 columns fit one strip.

**Copy is a privacy constraint, not a style choice.** `route_hits` has no user, IP,
session or opportunity column, and `app/privacy/page.tsx` promises the public that
these are our own counts and nothing more. So the section says **requests**, never
"visitors", "users" or "views", and both new types carry that as a comment so the next
session does not casually relabel a column.

**Three correctness details worth naming.** The SWR key carries the window
(`analytics/traffic?days=N`) because 7/30/90 are three datasets, not one; `keepPreviousData`
was deliberately NOT set, so a 30-day total can never render under a "7 days" label
while the new window loads. The render chain is
`error → skeleton (loading OR data undefined) → empty → data`, and the `!data` arm is
load-bearing: just after a window switch the hook is neither loading nor errored with
data still undefined, and that instant must show the skeleton rather than the "no
requests" copy — which would be this project's recurring bug in a new place. Dates are
walked in **UTC**: the backend keys rows by UTC day, and reading local getters off a
bare `new Date("2026-09-02")` shifts the label back a day in every US timezone.

**VERIFIED, in the browser, not by reading the diff.** Lint held the exact baseline
(**0 errors / 11 warnings** — a new warning would have been a regression); `npm run
build` clean; `tsc --noEmit` clean. With the stack up and an admin account: the tile
read 14 then 17 then 18 as traffic accumulated, 30 bars rendered with 29 zero-stubs
and one at 100%, the axis labelled every 5th day plus the last, and the table listed
`/api/v1/opportunities`, `/leaderboard`, `/auth/login` — with `/api/v1/health` and
`/analytics/traffic` correctly **absent**, both being excluded server-side. Window
switch: 7 bars, label "7d", `aria-pressed` correct.

**The failure path — the check this project exists to keep passing.** With the page
open, the backend was stopped and a window switch forced a fetch:
`trafficTile: "—Requests · 90d"`, `errorPanel: true`, `retryButton: true`,
`tableRows: 0`, `bars: 0`, `noRequestsCopy: false`. The tile reads **"—" and not 0**,
and the empty-state copy is correctly not shown for a failure. Restarting the backend
recovered to 90 bars and a real total without a page reload — SWR revalidated on
reconnect before I could even click Retry. Console: the only errors are the
`ERR_CONNECTION_REFUSED` I caused; no React or hydration errors.

**Authorization, both halves.** Server: a student token gets **403**, no token gets
**401**. Client: a non-admin loading `/admin` is redirected to `/` with the section
never rendered, and the SWR key is held `null` until `is_admin`, so the request is not
merely rejected — it is never sent. Honest limit: the tab's network log is cumulative,
so I could not prove the negative from it directly; the evidence is the redirect, the
absent section, the null-key code path, and the server's 403.

**One thing I could not test.** The empty-window state (`total === 0`) is unreachable
with today's data, since loading `/admin` itself generates requests on the very day it
queries. The code path was reviewed, not executed.

**Left behind, deliberately:** two throwaway accounts in the gitignored dev database
(`admin.local@demo.edu` flagged `is_admin=1`, and `plain.student@demo.edu`), created
because the four seeded users are all `is_admin=0` and their passwords are recorded
nowhere. Also noted: the browser's `form_input` sets a DOM value without firing
React's `onChange`, so the login form submitted empty and a token had to be injected
directly — a testing-harness quirk, not an app defect.


## 2026-09-03 ~03:16 CDT — the docs sweep the audit never finished: ten findings, and the two worst were mine from the day before

**WHAT.** Ran the D1–D4 documentation methods that the 2026-09-02 audit had left
**NOT SWEPT** — its docs worker was killed by a process restart, and a second attempt
died on a session rate limit having returned nothing. Third attempt, hardened: chunked
returns after *each* method rather than one report at the end, and D2 (the cheap
mechanical reference pass) ordered first so findings land even if the agent dies.
**Ten findings, all re-verified by me before any file was touched.**

**Deliberately re-spawned rather than resumed.** The original worker's tree had moved
underneath it — `backend/README.md`, `HANDOFF.md`, `PRD_ROADMAP.md` and all three
deploy docs changed in `71c8fbd`, `eae5943` and `578ad70` after it started. A resumed
worker reports against stale state, which is worse than no report. It was also told
explicitly that a large docs sync had just landed and that **"recently corrected" is
not evidence of correct** — a fresh edit is the likeliest place a new wrong number
hides.

**That instruction earned its keep immediately: the two MEDIUM findings are both mine,
from the day before.**

1. **"18 route modules" is wrong in two documents.** Disk has **17**. I wrote the
   figure into `backend/README.md` and `PRD_ROADMAP.md` on 2026-09-02 by counting the
   directory listing including `__init__.py`. **But the naive correction is also
   wrong**, which is why this is worth the words: `router.py` makes **18**
   `include_router` calls, because `messages.py` exports two routers (`router` and
   `inbox_router`). "17" alone would read as an error to anyone who counts routers and
   would get "corrected" back. Both docs now say 17 modules mounting 18 routers, with
   the reason.
2. **`INDEX.md` still said 354 tests.** I updated `testing.md` to 364 on 2026-09-02 and
   missed `INDEX.md`'s own one-line rollup of that same bin — and `INDEX.md` is the file
   this project's instructions tell a session to read *first*. The stale number sat in
   the routing layer while the file it routes to was correct.

**Three stale-citation findings, and one of them is wrong about the file, not the
line.** `config.py:44` was cited in `HANDOFF.md`, `PRD_ROADMAP.md` and twice in
ADR-0002 for the token TTL and the Pro price. Line 44 is a **closing parenthesis** —
the declarations moved to `:57` and `:83` when the `repr=False` credential fix added
lines above them. Worse, ADR-0001 cites `frontend/lib/auth-context.tsx` for the
`TOKEN_KEY` definition, which **no longer lives in that file at all**: it moved to
`lib/api.ts` and is only imported and re-exported.

**How I fixed citations, and why not uniformly.** The two *live* status citations were
converted to symbol references (`ACCESS_TOKEN_EXPIRE_MINUTES` in `config.py`), because
symbols do not drift and this is the third time today line numbers have rotted. The
roughly twenty-eight citations inside the two ADRs were **not** rewritten: they are
dated decision records, mass-editing them is not surgical, and refreshed numbers would
simply rot again. Each ADR instead gained one dated accuracy note saying the lines were
right at authoring, naming the specific corrections, and flagging the `TOKEN_KEY` case
as wrong about the file rather than the line. **A pointer that is honest about its own
decay beats a precise pointer that will be wrong next week.**

**A documented check that could not run.** `API_KEYS.md` presents a Python one-liner as
the mechanism that keeps the key registry "derived-checked, not eyeballed", asserting it
must print `[]`. Run verbatim on this project's own documented dev platform it raises
`UnicodeDecodeError: 'charmap' codec` — `read_text()` with no encoding gets Windows
cp1252. It only works with `encoding='utf-8'` added, and then it does print `[]`, so the
*claim* held while the *check* did not. **A registry check that cannot be copy-pasted on
the machine it targets is not a check.** Fixed, and re-run verbatim from the corrected
doc: prints `[]`.

**And the check was weaker than it looked.** `ADMIN_EMAILS` is a real `Settings` field
with real deploy relevance and had **no row** in the table of a file that calls itself
"one place that lists every key". It passed the derived-checker anyway, because the
checker is a substring match and the name appears in a banner note elsewhere in the
file. **The checker proves a NAME APPEARS, not that a variable is documented** — the
same shape as every other finding this week: a guard that verifies the form it measures
and is silent about the thing it was for. `ADMIN_EMAILS` now has a proper row.

**What the sweep proved TRUE, which is why it was worth running.** 364 tests on
SQLite; migrations 0001–0026; lint exactly 0 errors and 11 warnings; build clean at 29
routes; the 24-hour TTL; MFA genuinely absent from disk in both directions; CI's three
jobs with zero `continue-on-error`; `Opportunity.views` still a dead column at the cited
line; all **33** environment-variable tokens across four deploy docs resolving to real
`Settings` fields, real `NEXT_PUBLIC_*` vars or Railway-native names with **zero
orphans**; and of ~74 commit hashes cited across 18 files, 66 resolve here and 7 in the
public mirror clone, whose citing context names the mirror. **Including the one I most
wanted independently checked: M14.1's DONE marking is TRUE** — `admin/page.tsx` really
does call `api.siteTraffic()`.

**Stated, not glossed.** The two append-only record files (6,022 lines) got the D2
mechanical pass only, not entry-by-entry claim verification — checking what was true at
dozens of past commits is out of proportion and largely exempt under the rule that a
dated entry true when written is not a finding. The "22" denominator in "11 of 22 SWR
pages" was not recomputed: the numerator is grep-confirmed at exactly 11, but which
pages constitute the candidate set is a prose judgment. One cited commit
(`98565e6`, a pinned third-party skill) has no local clone and no network fetch was
made.

**VERIFY.** 364 pytest green (`44.44s`); lint 0 errors / 11 warnings, the exact
baseline. The corrected `API_KEYS.md` one-liner run verbatim → `[]`. `ADMIN_EMAILS` row
count 1. `INDEX.md` and `testing.md` now both say 364. Zero "18 route module" claims
remain; the only surviving `config.py:44` strings are the two dated corrections that
explain it.


## 2026-09-03 ~19:17 CDT — M14.2: the org analytics tab, and deleting the metric we could not compute honestly

**WHAT.** Built org-facing analytics and closed M14. `GET /analytics/org` returns the
calling organization's own numbers, scoped in SQL; the dashboard's Analytics tab now
renders them; a per-opportunity view counter fills the previously dead
`Opportunity.views`; and the pricing page, Terms and the for-organizations page were cut
back to what actually ships. **376 backend tests green** (364 + 12 new), lint at its
exact 0-errors/11-warnings baseline, build and `tsc` clean.

**THE TAB WAS SHOWING TWO WRONG NUMBERS, WHICH IS WHY THIS MATTERED.** It computed
everything client-side from lists fetched for other tabs. "Applicants" counted every
application including `rejected` and `withdrawn`. "Spots filled" used
`spots_available - spots_remaining` — and the backend maintains `spots_remaining` for
**one-time listings only**, because recurring capacity is tracked per date. So every
recurring listing displayed a fabricated fill figure to the organization running it.

**THE DECISION THE MILESTONE ACTUALLY TURNED ON.** M14.2's done-check says any metric
that cannot be computed honestly is dropped **from the tab and from the pricing copy**
rather than faked. The pricing page sold "views, fill rates, retention". Views and fill
rate were buildable. **"Retention" was not** — nothing in the schema distinguishes a
volunteer who came back from one who did not, beyond what "applied to two of our
listings" can say, and that is not retention. So the word was **deleted from the pricing
page** rather than approximated by a nearby number. `returning_volunteers` ships as its
honest, narrower self: a count of students with approved signups to two or more of that
org's listings.

Fill rate got the same treatment at a smaller scale. Rather than emit a percentage that
is meaningless for recurring listings, the endpoint returns the approved count and the
capacity, and the tab shows a percentage **only** where `recurrence == "one_time"`.
A recurring row reads "Per-date — see listing". Verified in the browser with one of
each: the one-time listing shows `2/4 spots (50%)`, the weekly series shows the
per-date text and no bar.

**THE VIEW COUNTER, AND WHY IT IS NOT A NEW ROUTE.** M14.1's `route_hits` stores the
route TEMPLATE and deliberately never records which opportunity was viewed — that was
its privacy design — so M14.2 could not read views from it. The counter is a side
effect of the existing `GET /opportunities/{id}` rather than its own POST: a new write
route would need classifying in `test_consent_gate_coverage`'s `CONSCIOUSLY_UNGATED`,
and a counter is not a student action, so that entry would have been exactly the kind of
false-but-passing classification the 2026-09-02 audit found.

Counting required knowing who is asking on a **public** route, and no optional-auth
dependency existed — `get_current_user` 401s without a token. Added
`get_current_user_optional` (a second `OAuth2PasswordBearer` with `auto_error=False`)
that returns `None` for every failure mode including an expired or revoked token,
because on a public page a stale token must still render the page. There is a test for
exactly that.

**A BUG MY OWN TESTS CAUGHT, AND THE COMMENT THAT PREDICTED IT.** The counter first
opened `SessionLocal` directly from the route. `SessionLocal` binds to the real
`DATABASE_URL`, so under test it reached for **Postgres** while the suite runs SQLite.
`conftest.py` already carried the warning, written for the M14.1 middleware: without
redirecting the counter's session, "the counters would aim at the real DATABASE_URL
during tests: Postgres errors swallowed by the middleware's own except, and every
analytics assertion silently testing nothing." My tests failed loudly rather than
passing vacuously **because they assert a specific count** rather than merely that the
call happened.

The fix was better than a second patch point: the session now lives inside
`services/traffic.record_opportunity_view`, so a test redirects **one** symbol rather
than one per counting route. The route keeps the try/except, so a counter failure is
swallowed and logged where a page is being served — analytics must never cost a reader
their request.

**PRIVACY, WHICH IS WHERE THIS COULD HAVE GONE WRONG.** `returning_volunteers` is a
COUNT and never names. The org can already see its own applicants, so the marginal risk
is cross-opportunity linkage, and a list would have made this a fourth
student-publishing surface beside the leaderboard, the portfolio and the org lists —
`security.md` warns that a change to one is a change to all three. A test serialises the
whole response and fails if a name, an email, `student_name`, `student_email` or
`user_id` appears; that is what makes a future "just add names" change loud. The
counter itself is one integer on a row that already exists: no viewer, no session, no
date dimension, so nothing in it can reconstruct one person's browsing.

**The privacy policy needed a sentence, and got one.** It promised aggregate counts
that "cannot show what any individual person looked at" — true, and written for
site-wide totals. It did not say an organization can see totals for its own listings.
Shipping the feature without that sentence would have left a live policy incomplete, so
the counters paragraph now says it plainly, on the same terms. The page is a DRAFT
pending legal review; this rides along with the review already owed rather than
creating a new one.

**COPY CUT BACK ON THREE SURFACES.** "Retention" deleted from pricing. Analytics
removed from the Pro-feature list in `pricing` **and in Terms** — it ships free to every
org and both documents claimed it was paid, a contradiction that predated this work
(the tab's own "Pro adds" box had it right all along). `for-organizations` now says
analytics are free and Pro adds listings and placement. This closes `disclosure.md`'s
standing open item verbatim: "Copy that outruns the code is a disclosure defect, not a
marketing one" — closed **from both ends**, by building two metrics and deleting the
third claim.

**Also corrected:** M14.2's own metric list in the PRD said "views (from #1's counters,
per opportunity)", which its own sequencing note falsifies eight lines later. Struck
with a pointer rather than left contradicting itself.

**VERIFY.** 376 pytest green (`62.85s`); ruff check + format clean; `tsc` clean; lint
0 errors / 11 warnings, unchanged; `npm run build` clean. Endpoint against the live
SQLite stack: three anonymous views counted, **two owner views correctly not counted**
(`total_views: 3`, not 5), `total_approved: 2` with a third application rejected.
Browser: KPI row, fill-rate meter and table render; the failure path forced with the
backend stopped gives the error panel and Retry with the KPI tiles and table **absent**
and the "post a listing" empty copy correctly **withheld**; Retry recovered without a
reload. Authorization: student **403**, anonymous **401**. Console clean apart from the
`ERR_CONNECTION_REFUSED` I induced.

**Stated, not glossed.** On failure the four KPI tiles disappear with the section
rather than reading "—": all four live inside the analytics block, unlike the admin
page's tile which sits in a shared grid outside it. The requirement is that a failure
never renders as a zero or an empty state, and an error panel with no tiles satisfies
it — but it is a different shape from the admin section and worth knowing before
someone "fixes" the inconsistency. The Analytics tab now uses `useAuthedQuery` while
the rest of that page still uses its older hand-rolled `refresh()`; converting the page
is M13.6's job and would have made a milestone commit carry an unrelated migration.


## 2026-09-03 ~19:30 CDT — four of the five standing Evan-decisions, and a guard that now checks its own reasons

**WHAT.** Cleared the backlog of decisions that had been waiting on Evan: a LICENSE,
the real account export moved out of the repo, the consent-gate allowlist restructured
so its reasons are partly machine-checked, and `security.md` split. **378 backend tests
green** (376 + 2 new), ruff clean, frontend lint at its 0/11 baseline. The fifth —
rotating the test-mode Stripe key — needs his Stripe account and remains his.

**THE ALLOWLIST NOW CHECKS ITS OWN CLASSIFICATIONS, WHICH IS THE POINT OF THE WHOLE
ENTRY.** `test_consent_gate_coverage.py` fails the build when a new write route has no
consent decision — a real mechanism, and the reason three missed gates were caught. But
an entry was a route tuple plus a free-text comment, and **nothing checked the comment**.
The 2026-09-02 audit measured what that costs: `DELETE /apply` was exempted on the
written grounds that it reaches "no organization, no public surface", while
`_withdraw_from_rosters` says the row is KEPT precisely so "the organization retains its
record". A guardian-revoked minor could delete the org's evidence, and the guard built to
catch that class had approved it in writing.

Each entry now carries a **kind**, and the kinds that can be verified against the running
app are verified: `ORG_ONLY` must actually depend on `require_org`; `UNAUTHENTICATED`
must not require a user; `AUTHED_SELF` must require one and must not be org-gated. **No
test can make prose true.** What this does is shrink the unverifiable set to a named,
counted minority — one entry — with a second test that fails if that count grows, so
"unexamined prose" cannot quietly become the default again.

**Two things the new test found on its first run, both about my own detector before they
were about the app.** Matching dependencies by `__name__` reported *every* org route as
missing its guard: `require_org` and `require_student` are both closures returned by
`_role_guard`, so both are literally named `guard`. Identity comparison against the
imported function object fixes it and, unlike the name check, can actually tell the two
roles apart. Then five routes still failed honestly — `POST /opportunities`, the featured
and active toggles, check-in codes, and broadcast check **role and ownership together in
the handler** (`opp.org_id != current_user.id`), which a role-only dependency cannot
express; the 2026-08-31 consolidation moved the pure role gates to `require_org` and
deliberately left these. They are classified `ORG_OWNER_INLINE`, which asserts what is
true of them (a user is required) and states plainly what it cannot see.

**The guard was watched firing.** A false `ORG_ONLY` claim was planted on
`PUT /saved/{opportunity_id}` — the exact class of false claim the audit found — and the
test failed on it. Plant reverted, and `grep PLANTED` returns 0. A control never observed
firing is unverified.

**THE SPLIT IS MEASURED, NOT CLAIMED — AND IT DID NOT MEET THE CAP.** `security.md` was
316 lines against a documented ~150. Four sections moved to a new `consent.md`
**verbatim**, so the move cannot have changed what any of them says: the M5 gate,
retroactive revoke plus the guardian auth surface, read-time consent on the public
portfolio, and `min_age` on apply. Result: **security.md 316 → 227, consent.md 113.**
A line-by-line check confirms **0 of 290 non-blank lines lost**. But 227 is **still over
the cap** — the honest number, not a rounded success. Getting to ~150 means moving the
audit-log sections too, and where those belong is a judgment about bin boundaries that
should be made deliberately rather than as a side effect of this one.

`INDEX.md` now routes to the new bin and no longer advertises consent under
`security.md`. It grew 50 → 52 lines against its own ~25 cap, which was already violated
and is now violated by two more; noted rather than smoothed over.

**A LICENSE THAT WOULD NOT HAVE REACHED THE PEOPLE IT IS FOR.** Evan chose
all-rights-reserved with a portfolio notice — the right call for a real service handling
minors' data that he may want to run commercially, and the reasoning is written into the
file rather than left implicit. Then a check found the gap: `sync_portfolio.py`'s
`MANAGED_ROOT_FILES` is `["HANDOFF.md", "PRD_ROADMAP.md"]`, so **LICENSE would never have
appeared in the public mirror** — the one place its readers are. Added to that list.
Listing it publishes nothing by itself; a sync remains a separate, deliberate step that
is Evan's to run.

**The account export is out of the tree.** `docs/servelocal-data-export.json` held a real
export — email, full name, date of birth — and was protected only by a gitignore rule.
Moved to `D:\ClaudeCode\_local-exports\`, which is outside every repo in the container.
Gitignore is a good backstop and a bad primary control: `git add -f` walks straight past
it, and the file sat inside a tree whose mirror script reads `docs/`.

**VERIFY.** 378 pytest green (`50.14s`); ruff check and format clean; frontend lint 0
errors / 11 warnings, unchanged. Split content-preservation: 290 non-blank lines in,
290 accounted for, 0 lost. Planted-positive on the new classification test: fired, then
reverted to 0 occurrences. `wc -l`: security.md 227, consent.md 113, INDEX.md 52.

**STILL EVAN'S, and only his:** rotating the test-mode Stripe key that rendered into
agent output on 2026-09-02 (test-mode, gitignored, never committed). It needs his Stripe
dashboard; there is nothing here to work around.


## 2026-09-03 ~19:37 CDT — all 14 bins updated, and the map the system required and never had

**WHAT.** Swept the whole project-memory system: every codebase-memory bin brought
current, and **`DIRECTORY.md` created** — a required part of the system per the skill's
§5.1 that this project had simply never built, despite a tree far past the threshold that
calls for one. HANDOFF, the PRD and the record were already current from today's work.
**378 backend tests still green** (a docs pass should not move that number, and it did not).

**THE CADENCE HOOK WAS RIGHT ABOUT WHICH BINS AND WRONG ABOUT WHY.** It had been naming
`performance.md` (53 days), `ui.md` (49) and `architecture.md` (27) all session, ranking
by file modification time. But file age does not mean stale — the docs-audit method says
so explicitly, and `performance.md` at 53 days was *accurate*, just describing a project
that had not done perf work. What made those three genuinely stale was **new facts that
had nowhere else to go**, which is a different test and the one worth applying.

**Facts added, by bin, all from work verified this week:**

- **`data.md`** — chain 0001→**0026**, and the durable lesson from that migration: **a
  column can exist in the model and not in the database.** `Message.recipient_id` was a
  declared `ForeignKey` while migration 0016 added only the column, so the SQLite suite
  (which builds schema with `create_all`) always had the constraint and production, built
  by `alembic upgrade head`, never did. No test could fail on it by construction. Also:
  `Opportunity.views` stopped being a dead column on 2026-09-03.
- **`testing.md`** — 364→**378**, plus the two mechanisms most expensive to rediscover:
  a counter opening its own session must be redirected in `conftest.py` or it writes to
  the real database during tests **while swallowing its own errors**, so the assertions
  pass while testing nothing; and *assert a specific count, never merely that a call
  happened* — that is what caught it. Plus: plant the positive before trusting a guard.
- **`gotchas.md`** — three traps that each cost real time. Every role guard is literally
  named `guard` (they are closures from one factory), so matching dependencies by
  `__name__` reports every org route as unguarded and cannot tell roles apart. The
  browser tool's `form_input` does not fire React's `onChange`, so a controlled login form
  submits empty.
- **`conventions.md`** — analytics counters open their own session **in the service, not
  the route** (one symbol for tests to redirect, rather than one per counting route), and
  role gating has two shapes that are not interchangeable.
- **`security.md`** — the consent-gate allowlist now checks its own reasons.
- **`architecture.md`** — the two analytics mechanisms and why one cannot read the other;
  `get_current_user_optional` and its never-raise contract.
- **`performance.md`** — `GET /opportunities/{id}` now WRITES, the first write on a hot
  public read path.
- **`ui.md`** — "failed loads must never look like empty ones" as a hard rule with its
  render order, and `—` as the failed-stat glyph.
- **`dependencies.md`** — M14 added **zero** dependencies, deliberately: no charting
  library, the bars are CSS divs. A "what NOT to add" fact is worth as much as a "what we
  use" one.

**MY OWN STALENESS CHECK CAUGHT TWO OF MY OWN MISSES.** After updating, a scan comparing
each bin's `Last updated` header against the newest date inside its own body found
`features.md` carrying 2026-09-03 content under a 2026-09-02 header — a staleness I had
created hours earlier — and `disclosure.md` in the same state. Both fixed; the scan now
returns **0 stale headers across 14 bins**. This is the third time this week that check
has found something, which is the argument for running it rather than trusting a memory
of having updated things.

**DIRECTORY.md, AND THE CLAIM IT ALMOST SHIPPED.** The map derives its numbers by command,
never estimate, and records each command in the file. The unreferenced-modules pass is
where it nearly went wrong: a first attempt matching only the dotted module path reported
**14 modules as dead, twelve of them live route modules** — because `router.py` imports
them with a grouped `from app.api.routes import (analytics, applications, …)`, so the
string `app.api.routes.analytics` appears nowhere in the tree. Re-derived matching either
form, the honest answer is that **nothing is unreferenced**. The skill's own warning names
exactly this: a live module listed as dead is the worst thing such a file can say, because
it invites a deletion. Both the wrong result and the corrected command are written into
the file so the next session does not repeat it.

**TWO CAPS STILL BROKEN, STATED RATHER THAN SMOOTHED.** `security.md` is **240** against a
~150 cap — I added 13 lines to it today, so this got worse, not better. Compressing it
further means moving the audit-log sections, and the rules say not to delete a
security entry casually. `INDEX.md` is **53** against a ~25 cap, and adding the
`DIRECTORY.md` route made it one line worse. That cap may simply be unreachable: fourteen
bins need fourteen lines before a single invariant is written, so the constraint and the
bin count are in direct conflict and one of them should give.

**VERIFY.** 14 bins, 0 stale headers (scan re-run after fixing two). 378 pytest green.
`DIRECTORY.md` 140 lines, under the ~200 guidance; its own staleness command
(`git diff --name-status --diff-filter=ADR b6a57b8..HEAD -- <mapped dirs>`) returns empty
at the SHA it records; its "18 include_router" claim re-grepped as 18. Spine counts
derived by the commands printed beside them, with their undercounting stated.


## 2026-09-03 ~19:48 CDT — the two bin caps: one fixed, one shown to be unreachable

**WHAT.** Acted on the cap analysis. `security.md` 240 → **184** by extracting a new
`audit-log.md` bin and moving the client-cache rule to `conventions.md`. `INDEX.md`
53 → **49** by compressing ten cross-bin invariants to one line each. **0 stale headers
across 16 files. 378 tests still green.** Neither cap is met, and both numbers are stated
rather than rounded.

**`security.md` IS STILL OVER, AND THE REMAINING GAP HAS A NAME.** At 184 against ~150 it
needs about 34 more lines to move, and there is exactly one candidate large enough: the
`repr(Settings)` section at **47 lines** — the biggest in the bin, and a *fixed* incident
whose full narrative already lives in the 2026-09-02 record entry. Compressing it to the
three durable facts (secrets never render in a repr; three layers each cover a shape the
others cannot; re-enabling frame locals silently reopens part of it) would land ~150.
**Not done, because the rules say never delete a security entry without telling Evan, and
compressing one is close enough to that line to be his call, not mine.**

**THE SPLIT KEPT THE AUTHZ FACT WHERE AUTH TASKS WILL FIND IT.** The obvious move —
lift the whole "Audit log + admin" section — would have taken `admin = the users.is_admin
column ONLY` out of `security.md`, and that is an authorization fact: an auth task must
reach it without loading the audit bin. So the section was split at the bullet. Mechanics
and the wired-event list went to `audit-log.md`; the admin bullet stayed behind under a
renamed heading, `## Admin authorization (M9.2)`.

**That rename broke three pointers, which is the whole reason to check.** A
content-preservation pass reported exactly one "lost" line — the old heading — which was
the intended rename rather than a loss. But three documents pointed AT it or at the moved
sections: `tooling.md` cited `security.md §Audit log + admin` for why `ADMIN_EMAILS` is
inert, `INDEX.md` cited `§Audit-log retention`, and `conventions.md` pointed at
`security.md §Client-side cache` — a section that had just moved *into conventions.md
itself*, so the file now referred to another file for a section it contained. All three
repointed in the same commit. **This is the fourth time this week that moving something
left a pointer behind**, and it is why the grep runs before the commit and not after.

**THE INDEX CAP IS NOT REACHABLE, AND THAT IS THE FINDING.** Composition at 49 lines:
**15 routing lines** (one per bin — a floor set by the prescribed bin set, not by choice),
~9 of read protocol and scope, 11 of invariants, plus heading and blanks. The ~25-line cap
leaves about 4 lines for the protocol AND every invariant combined. It was written for a
smaller bin set; it cannot survive this one.

The compression was still worth doing, because the invariants had a real defect: they were
written as **mini-entries with their own reasoning**, duplicating the bins that own them —
the client-cache rule ran four lines, the audit-log rule four, `scrub_event` three. Each is
now one line ending in a pointer to its owning bin. **Every one was checked to have that
reasoning in its bin before the line was shortened**, so this is compression, not deletion.
One nearly failed that check: "Only the Stripe webhook flips `User.plan`" appeared to have
no home until a second grep found it in `security.md` §Billing — the first pattern missed
it on markdown formatting, which is the reminder that a zero-result search is a claim.

**The deviation is now written into `INDEX.md` itself** rather than left as a silently
violated standard: the file states it is over cap, why the floor exists, and that raising
the cap in the skill would change every project and is therefore Evan's call, not a local
one. **A cap that is permanently violated stops being a signal** — which is how six stale
bin headers went unnoticed for two weeks.

**VERIFY.** `security.md` 184 (was 240), `audit-log.md` 50 (new), `conventions.md` 133
(was 111), `INDEX.md` 49 (was 53). Content preservation across the three moved-between
files: 316 non-blank lines in, one deliberate heading rename, nothing else lost. Stale-header
scan across all 16 files: **0**. 378 pytest green. Three cross-references repointed and
re-grepped clean.

**OPEN, and Evan's:** compress the 47-line `repr(Settings)` section to reach ~150; and
decide whether the skill's ~25-line INDEX cap should be raised globally, given that 15
bins need 15 lines before a single invariant.


## 2026-09-03 ~20:07 CDT — HANDOFF was a changelog; it is a snapshot again, and the rewrite nearly deleted a load-bearing table

**WHAT.** Rewrote `HANDOFF.md`: **590 → 255 lines**. It is specified as "the ONLY live
snapshot" and is the first file a cold session reads, but **432 lines — 73% — were 14
dated blockquote entries**, five of them added by me earlier today. A session reading
top-to-bottom waded through seven weeks of narrative before reaching what is currently
true. 378 tests green, unmoved by a markdown change.

**THE DELETION WAS GATED ON A COVERAGE CHECK, WHICH IS THE ONLY THING THAT MAKES IT
SAFE.** `HANDOFF.md` is not append-only — the record is — so trimming it is legitimate
*if and only if* nothing in the trimmed part exists solely there. Every distinct entry
date was checked against `## <date>` headings in the record files: **9 distinct dates, 0
without coverage.** One looked orphaned at first (`2026-08-25`, zero record entries) —
that was **my check being wrong**, not a gap: the entry is dated 2026-08-31 and merely
mentions 08-25 in its body. The record carries it at `record_2026-07-16.md:2221`.

**THEN THE VERIFICATION CAUGHT WHAT THE PLAN DID NOT.** The plan's step 2 — "confirm
nothing references a section the rewrite removed" — found that `CLAUDE.md` names a
**workstream table** in `HANDOFF.md`, and I had deleted it. It sat at old line 431,
immediately after the blockquote pile and before the sections I had marked to keep, so my
"pile" range swallowed it. **Four documents require it**: `CLAUDE.md` twice (including
the definition of done — "HANDOFF workstream table updated when a milestone's status
changes"), `conventions.md`, and `PRD_ROADMAP.md` twice. It is not history; it is the
per-milestone status structure the whole doc system writes back to. Restored, with a note
saying why it is not history so the next rewrite does not repeat this.

That is twice in one session that a *structural* doc change broke something a *content*
check would never have seen. The lesson is narrow and worth keeping: **before deleting a
region of a snapshot, grep who points INTO it — not just whether its prose exists
elsewhere.**

**RE-DERIVING THE NUMBERS FOUND A STALE ONE.** The plan forbade copying figures from the
old file. Doing so caught that **SWR is at 12 of 22 pages, not 11** — `applicants/page.tsx`
gained `useAuthedQuery` as part of M14.2 earlier today, and no one updated the count. Four
places said 11; all now say 12, with the reason. Also corrected: the Goal paragraph and the
Documentation section both described `PRD_ROADMAP.md` as the "M1–M11" and "M1–M13" plan —
it is M1–M14, and M14 closed today.

**WHAT REPLACED THE PILE.** A `## Recent history` section of eleven one-line rows — date,
what changed, and the commit or record entry that holds it — under a sentence stating
plainly that this file is the snapshot and the record is the history. Older entries were
dropped outright, since they are duplicated verbatim in the record.

**BLOCKED-ON-EVAN was re-dated and pruned.** Its heading had said "as of 2026-07-16" for
seven weeks, and 13 of its lines were struck-through resolved items, which buried the live
blockers in a trail. Only open items now, grouped by what they actually are: the launch
critical path (legal review, and the two pre-mortem questions for a responsible adult),
values and accounts, operational jobs, and two small open decisions. Two items this
session produced were added — **rotate the test-mode Stripe key**, and **the legal reviewer
must see the privacy sentence added 2026-09-03**.

**AND THE FILE FINALLY HAS A HANDOFF PROMPT.** The project-memory skill's §3 step 6
requires each session to end with a fenced, paste-ready prompt for the next one.
`HANDOFF.md` contained **zero** fenced blocks. It now ends with one: read order, current
state in a paragraph, the hard constraints that actually bite here (PowerShell 5.1 has no
`&&`; `ENVIRONMENT=ci` for pytest; the frontend has no test runner so browser-verify the
failure path; run `date` in its own call; never push; never sync the mirror), and next
actions in priority order.

**VERIFY.** 590 → 255 lines. Coverage check: 9 distinct entry dates removed, **0 without
record coverage**. Section diff: 7 old headings → 9 new, the 2 "dropped" being the
deliberate re-datings of `Current state` and `BLOCKED-ON-EVAN`. Workstream table restored
at 28 rows. Byte scan: **0 CR, 0 control bytes**. 378 pytest green. Numbers re-derived from
disk, not copied: 378 tests, migrations 0001–0026, 17 route modules, 29 pages, 16
codebase-memory files, SWR 12 of 22.


## 2026-09-03 ~20:22 CDT — both cap decisions closed by Evan: one section compressed, one cap raised globally

**WHAT.** Evan answered the two questions II.41 left open, and both landed here.
`security.md` **183 → 150 lines**: the 47-line `repr(Settings)` section became 13, keeping
four durable facts and pointing at the 2026-09-02 record entry for the rest. The skill's
`INDEX.md` cap went **≤25 → ≤75** in `project-memory/SKILL.md:304` and `templates.md:201`,
with the dated reason written beside the number; `INDEX.md`'s five-line "over cap on purpose"
blockquote became three lines stating 46 against ~75.

**150 IS AT THE CAP, NOT UNDER IT — the honest number.** The plan predicted 149. Landing
under 150 with every fact intact would have meant dropping the "knowingly exposed" bullet
(`model_dump*()` returns plaintext; Sentry `context_line` shows source text), which is the
trigger condition for revisiting `SecretStr` — a live decision, not history. So the section
is one line longer than planned and the file sits exactly at ~150. Stated rather than rounded.

**WHAT THE COMPRESSION KEPT, AND WHY THOSE FOUR.** Kept: secrets must never render in a repr
(853 chars, a real `sk_test_…`); the two carriers (Sentry frame locals live, `monkeypatch`
`!r` latent at 33 sites); why three layers rather than one (an object serializes to a repr
STRING with no keys to match, a dict stays a dict with no repr to mask); that layer 3 alone
reaches the three httpx frames binding the Turnstile secret, so re-enabling frame locals
silently reopens part of the hole; and the knowingly-accepted remainder. Moved to the pointer
only: the narrative, the wrong-cause correction, per-route measurements, and the `SecretStr`
rejection rationale — all verified present in `docs/record_2026-07-16.md` @ 2026-09-02 ~16:18
CDT **before** the lines were removed, not after.

**RAISING THE CAP RELAXES THE STANDARD; IT DOES NOT RESOLVE THE CONFLICT.** II.41's finding
was that 15 prescribed bins force 15 routing lines before a single invariant — the bin set and
the cap disagree, and ≤25 was the losing side. 75 gives headroom over today's 46, but it makes
the number non-binding for every project rather than fixing the arithmetic. Evan's call, made
explicitly; recorded here so the reason survives the number. **Checked for blast radius:**
`D:\ClaudeCode\Trading`'s INDEX is **22 lines over 10 bins** — it was already under the old cap,
so no other project's memory system changes today. The conflict is specific to v2's 15-bin set.

**VERIFY.** `wc -l`: `security.md` **150** (was 183), `INDEX.md` **46** (was 48). No control
bytes in the spliced section; file still UTF-8. `grep -rn "≤25 lines"` in the skill → 0 hits.
`grep -rn "repr(Settings)"` across the repo → only `HANDOFF.md` prose (updated in the same
sitting), no pointer at the removed heading. INDEX's self-referential line count says 46 and
the file is 46 — the first draft said 48 and was wrong by two, which is exactly why that
number gets re-measured rather than carried.


## 2026-09-03 ~20:33 CDT — two SWR pages that were hiding failures, and a legal packet that answers nothing

**WHAT.** M13.6 **12 → 14 of 22**: `app/portfolio/[id]/page.tsx` and
`app/opportunities/[id]/page.tsx` moved to `usePublicQuery`. Both were converted for the same
reason and not for tidiness — each was rendering a *wrong* screen on failure. Separately,
`/legal-triage` produced `docs/LEGAL_REVIEW_PACKET.md` for M11 Phase 1: a routing decision and
a verification work list, not an answer. Evan asked for two pages in one sitting rather than
the roadmap's one; both were browser-verified individually, which is the part that matters.

**THE PORTFOLIO PAGE TOLD VISITORS A TRANSCRIPT WAS PRIVATE WHENEVER THE NETWORK FAILED.** Its
old state machine was `"loading" | "ok" | "unavailable"` with `.catch(() => setState(
"unavailable"))` — one catch for every failure, feeding the Lock empty state. A dropped
connection and a student who had never opted in were the same screen. Now a 4xx from the
server keeps the private/missing copy and anything else gets an error panel with Retry, split
on `error instanceof ApiError && status >= 400 && status < 500`. `instanceof` rather than
`error.status`, because a fetch network failure throws a raw `TypeError` with no `status` at
all — the typed cast in `use-api.ts` would have made `error.status` *typecheck* and be
`undefined` at runtime, which is the quiet version of the same bug.

**THE OPPORTUNITY PAGE HAD NO LOADING STATE AT ALL**, and one `error` string served both the
page load and the Feature toggle, so either cleared the other. Now `actionError` is local to
the write, the load state comes from the hook, and a spinner renders while the request is in
flight. `SignupSection`'s `onChange` and the post-write refresh both call `retry`, which is
the hook's `mutate`.

**THE VERIFICATION FOUND A CACHE THAT DEFEATS THE PROJECT'S OWN HIGHEST-VALUE CHECK.** The
standing check is: load a page, stop the backend, confirm the error panel. On the first
attempt the page kept rendering data with uvicorn stopped and `curl` returning exit 7 — the
browser HTTP cache was replaying the public portfolio response, which carries cache headers
from the 2026-07-13 audit work. The network log showed `GET /api/v1/portfolio/… → 200 OK`
against a server that was not running. **The check only works on a URL the browser has never
fetched.** Redone with a second, never-visited record, the panel appeared as designed. Worth
knowing before someone runs the check on a warm page and concludes the error path works.

**VERIFIED (real states, not code reading).** `portfolio/[id]`: public record renders data;
a non-public id renders the private copy off a real `404` (network log); backend stopped +
cold id renders "Couldn't load this transcript" with Retry after SWR's backoff; backend back
→ recovered. `opportunities/[id]`: listing renders; unknown id renders the API's own message
with Retry and — confirmed in the network log — SWR does **not** auto-retry a 4xx, only the
Retry click fired the second request; backend stopped + cold id renders the error panel where
the page used to render a blank shell under the back-link. **The Retry button was proven to
dispatch** (two `404`s: the load, then the click), because an earlier recovery had come from
SWR's own reconnect revalidation and would have proved nothing about the button.
Console: only the failures I induced (404s, `ERR_CONNECTION_REFUSED`) — no React errors.
`tsc` clean · `npm run lint` **0 errors / 11 warnings** (baseline held) · `npm run build`
clean · 378 pytest green.

**Fixture honesty:** no student in the dev SQLite DB had `portfolio_public=1`, so two were
flipped directly in `.dev-sqlite/dev.db` to exercise the page, then **flipped back**. Nothing
was seeded or invented; the DB is gitignored and is as it was found.

**THE LEGAL PACKET DELIBERATELY ANSWERS NOTHING.** `--canary` printed `CANARY PASS 60/60`
before any scan was trusted. `cite-scan` on Terms: 0 findings. On Privacy: 3 — `CCPA`,
`GDPR`, and a document-level missing-disclaimer. **The third is reported as a judgment call
rather than silently fixed:** that check assumes advisory text, and a published privacy policy
is the operative document rather than advice about one — but suppressing a finding to make a
report look clean is how a real one gets lost. `risk --matter minors-data` → HIGH, escalate;
`--matter contract` → MEDIUM, escalate; both forced by matter type at every score.

**The packet's real content is the claim-vs-code table**, each row checked against source
rather than memory: `MINIMUM_AGE = 12` / `ADULT_AGE = 18` (`app/core/consent.py:12-13`);
export `GET /auth/me/export` and delete `DELETE /auth/me` as **anonymize-in-place**
(`app/api/routes/auth.py:216,227`); `AUDIT_LOG_RETENTION_DAYS = 365` matching the policy's
12 months (`app/core/config.py:134`) — **but nothing schedules the purge**, so a public
promise is kept by hand, and the reviewer is told so; org analytics scoped to the caller's own
listings (`app/api/routes/analytics.py:72-127`), which is the sentence added earlier today
that the reviewer has not seen. Phase 1's one model-doable item, the decline-recourse
procedure, was already written (`docs/SUPPORT_PROCEDURES.md` §P1) — confirmed, not assumed.

**NOT FIXED, ON PURPOSE:** `use-api.ts`'s `usePublicQuery` docstring still cites "a guardian
consent link" as an example use, while HANDOFF says the two consent-token pages are
deliberately NOT converted. One of the two is wrong; it is a comment, it changes no behavior,
and picking which one to change is a decision about the consent pages, not about these two.


## 2026-09-03 ~22:14 CDT — a whole-project pre-mortem found the top risk is a contradiction between two of our own documents

**WHAT.** Ran `/pre-mortem` on the entire project rather than on M11 (Evan's
scope call), horizon 2027-01-01 = applications submitted. Output:
`docs/premortem_2026-09-03_project.md`, **14 risks — 7 Tigers (5
launch-blocking), 3 Paper Tigers, 4 Elephants, 3 auto-flagged** by the skill's
`risk_categorizer.py`. It does not supersede `premortem_2026-09-01_M11.md`; that
one's seven launch-blockers enter this register as a single row, because at
project scope they are one risk with one cause.

**THE FINDING IS THAT TWO LIVE DOCUMENTS DISAGREE ABOUT WHETHER THIS PROJECT
SHOULD BE BUILDING TOWARD A LAUNCH.** `EVAN_CONTEXT.md`, compiled **2026-07-22**
from Evan's own interview answers, says ServeLocal is *on hold until 18 for legal
reasons; pushing it public is age-gated*, and ranks it third of three projects.
`HANDOFF.md` and `PRD_ROADMAP.md` say the finish line is a real public launch
(Evan, **2026-07-08**) and that M11 is the frontier. **The context file is the
newer document and it says stop.** Roughly seven weeks of work since has assumed
the older one, and neither document references the other. Nothing about the code
resolves this; it needs one dated line saying which is true.

**SECOND ESCALATION: Evan's 18th birthday is in no project document.** Age 17 is
everywhere; the date is nowhere. It is the single fact that decides
person-versus-entity, contract capacity, and who carries the consent duty — so it
is listed as UNKNOWN rather than estimated. If the date is close, waiting is the
cheapest mitigation in the whole register.

**THIRD ESCALATION, AND IT IS ABOUT THIS SESSION'S OWN WORK.** 2026-09-01 raised
two questions for a responsible adult. 2026-09-03 produced
`LEGAL_REVIEW_PACKET.md` — a better-formed version of the same ask — and sent it
to nobody. A model can always generate the next artifact for a blocked step, each
one individually good, and the pile feels like progress while the blocked thing
does not move. Recorded against our own output deliberately.

**NONE OF THE FIVE BLOCKING TIGERS IS ABOUT THE CODE**, which is the reason to
read the document: T1 the public mirror is at `e5a7235` (**2026-08-19**, 15 days
stale) and still ships `auth-context.tsx:52`'s blanket catch that signs a user out
on ANY network failure — fixed privately at `:85-96`, so the artifact publishes a
bug the project has already closed; T2 the mirror carries **zero `backend/`
paths**, so a reader sees the Next.js frontend and prose *about* FastAPI, 378
tests and migrations 0001–0026; T3 **9,295 lines** of record (2,652 + 2,213 +
4,430) behind a **33-line README**, with no case study anywhere in the tree; T4
the seven M11 blockers, against a **$0** spend ceiling; T5 ServeLocal is priority
#3 while World Models has a ~late-September ICLR deadline, and both draw on the
same ~2-hour sitting.

**Dismissed with reasons, so they stop costing attention:** code quality (378
green, four cold audits — not the binding constraint on any goal here); a
security failure sinking the launch (already triaged 2026-09-01 — the real risks
are operational and silent); and M13.6 SWR as a gate (`PRD_ROADMAP.md:253` says
it gates nothing — it is the work that is *available* precisely because the
critical path is blocked, which is what makes it feel like the critical path).

**RECOMMENDATION RECORDED AS DISSENT-IF-OVERRULED:** stop building ServeLocal
features until the document contradiction is answered. If the answer is "launch
waits for 18," then the remaining work is T1/T2/T3 — sync the mirror, decide
whether `backend/` ships, write one case study — about a week, with no money, no
accounts and no adults in it.

**VERIFY.** Every number was read off disk during the session, not recalled:
record line counts via `wc -l`; the mirror's date via `git log` in
`D:/ClaudeCode/servelocal-portfolio`; the stale `auth-context` line by opening
both copies and diffing the catch blocks; the `backend/`-absence via
`MANAGED_DIRS` in `scripts/sync_portfolio.py` plus `disclosure.md`'s 2026-09-01
verification. Categorizer inputs preserved at `scratchpad/premortem_project.json`
(the script wants a `{"risks": [...]}` wrapper — a bare array raises
`AttributeError`).


## 2026-09-03 ~22:20 CDT — pre-mortem E2 resolved: Evan turns 18 in 5 days, and that answers half of E1, not all of it

**WHAT.** Evan stated in chat that he turns 18 in 5 days (**~2026-09-08**), resolving
`docs/premortem_2026-09-03_project.md`'s E2, which had been recorded as UNKNOWN,
deliberately not estimated, twelve minutes earlier. Updated in place: E2's status,
E1's reframe, the summary count, the action-plan row, the launch-decision paragraph,
and the follow-up line. `HANDOFF.md`'s contradiction block and next-actions item 0
updated to match; last-updated bumped to 22:20 CDT.

**WHAT THE BIRTHDAY DOES AND DOES NOT SETTLE.** It resolves Evan's own legal capacity —
person-versus-entity and contract capacity lift on ~2026-09-08 without anyone's
decision. It does **not** resolve E1's other half, now tracked as the live question
under E4: the platform holds OTHER people's minors' data (students 12–17) regardless
of the operator's own age, and COPPA, guardian consent, and the audit-retention
promise are about them. Turning 18 removes the reason Evan *couldn't* launch a real
minors platform; it does not answer whether he *wants* to take on that duty. The
launch-decision paragraph was rewritten to say this explicitly rather than let
"contradiction resolved" read as "go ahead."

**Numbers not invented:** the ~2026-09-08 date is arithmetic (today + 5, per a real
`date` call at 22:18 CDT), stated as an approximation because Evan gave "5 days," not
a calendar date. Nothing else in the register was touched — T1–T7, P1–P3, E3 and E4
stand as written twelve minutes ago.

**VERIFY.** `grep -n "E1/E2\|E1 is answered\|Unknown, deliberately"` against
`premortem_2026-09-03_project.md` and `HANDOFF.md` returns zero stale references
after the edits. No code changed; no tests affected.


## 2026-09-03 ~22:25 CDT — legal triage re-run across every legal surface, then a sourced research brief on CCPA/GDPR/COPPA/state minors' law

**WHAT.** Evan asked for "everything even remotely connected legally to the app."
Ran `/legal-triage` first (canary `CANARY PASS 60/60`), then `/research-brief`
on the one question the 2026-09-03 `LEGAL_REVIEW_PACKET.md` left open: whether
the regimes the privacy page *names* (CCPA, GDPR) actually apply, plus COPPA
and state law. Output: `docs/research/2026-09-03_ccpa-gdpr-coppa-state-minors-privacy-background.md`.

**TRIAGE.** Five matters scored (`minors-data`, `contract`, `money`, `ip`,
`publish`), every one RED by forced override. `cite-scan` over the packet,
both legal pages, both ADRs and `LICENSE`: 7 findings, all already listed in
the packet (CCPA/GDPR named without applicability shown; privacy page lacks
a not-legal-advice line — a judgment call, flagged not fixed). No new
surfaces beyond the packet's.

**THE BRIEF — what it found that nobody had written down.**
- **COPPA's line is under 13, not under 12.** `privacy/page.tsx:85` says
  "students under 12 are not permitted"; a 12-year-old is inside COPPA's
  protected class. If the signup form takes an exact DOB, a "12" gives the
  operator individualized actual knowledge of a COPPA child (FTC FAQ A.12,
  verbatim from ftc.gov). Whether COPPA is *triggered* still turns on the
  "directed to children" factors — a product fact this research cannot answer.
- **CCPA** thresholds ($26,625,000 revenue / 100,000 CA records sold or shared
  / 50% revenue from data) each sit far above current documented practice.
  "Does business in California" was not found defined in the statute —
  provisional (one search pass).
- **GDPR** Art. 3(2)(a) targeting matches the EDPB's own non-application
  examples 8 and 10; Art. 3(2)(b) monitoring is a separate prong that was
  NOT tested against M14's analytics design.
- **Texas SCOPE Act is a live open question**: no revenue threshold, a
  3-factor features test that plausibly overlaps messaging and public
  portfolios, only partially enjoined (July 24, 2026, 5th Cir.) — that
  ruling's scope is sourced to CCIA's press release, an interested litigant.
- Follow-up round (CT, CO, VA, MD, UT): they do NOT work alike. Virginia has
  no 13–17 ad opt-in at all; Maryland's AADC is in force and under an
  undismissed challenge; Utah could not be verified against primary text.

**METHOD, honestly.** Four parallel sub-agents, then compile. Stage 8.5
downgraded two claims to CLOSE-PARAPHRASE (CCPA text came through a
summarizing fetch). Stage 10.5 cold assessment earned its cost: it caught a
same-source dependency (two "CCPA doesn't apply" findings both resting on
the privacy page's own "we never sell your data"), a false "interest
concentration: none", one overclaimed sentence, and the skipped state laws.
eur-lex was WAF-blocked; not bypassed — GDPR text came from the EDPB PDF via
`pdftotext`. Nothing in the brief is a compliance verdict, by design.

**CONTEXT LANDED MID-SESSION.** The record entry twelve minutes earlier
(pre-mortem E2) says Evan turns 18 ~2026-09-08. The brief's "17-year-old
operator" framing is accurate as of its date; contract-capacity questions
lift on that date, the minors'-data questions do not. Not rewritten — dated
docs stay dated.

**OPEN.** Root whole-project record not yet appended for this (offer stands).
Evan then asked to "make sure the privacy policy and terms follow all laws" —
that is a legal conclusion, refused; `/audit-docs` runs instead as
claim-vs-code-vs-brief verification. No code changed; 378 tests unaffected.


## 2026-09-03 ~22:35 CDT — cold docs audit of the Privacy and Terms pages: honest about the product, false about themselves

**WHAT.** Evan asked to "make sure the privacy policy and terms follow all
laws." Refused as a legal conclusion (legal-triage rule 0); ran `/audit-docs`
instead, scoped to `frontend/app/privacy/page.tsx` and `terms/page.tsx`, two
cold Sonnet workers (D2/D6/D1/D3 and D5/D4/D8/D7), every high and medium
re-run by the session. Report: `docs/audit_2026-09-03_legal-pages.md`. No
file changed — findings pass only, fixes await approval.

**THE PATTERN.** Every sentence about what the code does held except two. The
false claims are the pages' claims about *themselves*:
- **F1 (high).** `auth.py:68` refuses registration without `accepted_terms`;
  `register/page.tsx:153` gates submit on the checkbox; `terms/page.tsx:32`
  says the Terms are "not yet in effect" and create no binding obligations.
  Every registrant agrees to a document that says agreeing means nothing.
  Only one `User(` path exists (`auth.py:77`). A decision, not a patch.
- **F2 (med).** "Draft dated July 16, 2026" on both pages, while `privacy:91`
  promises the date tracks material changes — 5 substantive commits since
  (`816bcf8` → `cf4e09f`), 27 lines changed, date never moved.
- **F3 (med).** `privacy:40` "website or EIN" — nothing collects either
  (0 hits in model, schema, register form).
- **F4/F5 (med).** "What we collect" omits `stripe_customer_id`
  (`user.py:67`); the vendor list omits Cloudflare Turnstile
  (`turnstile-widget.tsx:14`, IP resolved at `auth.py:49`) — off until keys
  are set, so false only at launch.
- Lows: consent gate covers ≥9 routes, prose names 4 (understates); the
  email toggle is on `/notifications`, not "account settings"; `terms:69`
  is a third `[… Evan]` placeholder the packet's "only two blanks" missed;
  cancel-at-period-end is a Stripe Dashboard setting, unenforceable here.

**VERIFIED TRUE.** Anonymize-in-place, listing-deactivation gate, 365-day
retention (unscheduled — known), no cookies (two-way), IP only on consent
routes (`applications.py` 0 by grep and rg), Sentry scrub, Argon2, 12-year
floor server-side, all links resolve, one account-creation path.

**FOR THE REVIEWER, not this audit.** `register/page.tsx:119` collects an
exact `type="date"` DOB while the floor is 12 — the brief's COPPA rows put
the line at 13. Packet §3 row 1 already carries the floor question; the
DOB-field fact is new.

**CORRECTION TO THE PACKET.** `LEGAL_REVIEW_PACKET.md:41` says grep for
`[…]` returns only two placeholders; it returns three (`terms:69, 86`).
Corrected forward here, not edited in place.

**COVERAGE.** 2/2 files, re-enumerated (190 lines). No doc-lint tool
installed — grep floor, stated. 2 workers, 0 delegates, 105 tool calls,
~341K subagent tokens, ~6.5 min. One audit defect caught by the audit: W1's
first EIN grep matched "b**ein**g"; re-run with `-w`.


## 2026-09-03 ~22:38 CDT — F2–F8 applied from the legal-pages audit; F1 (the clickwrap decision) left for Evan

**WHAT.** Applied the seven no-decision-needed fixes from
`docs/audit_2026-09-03_legal-pages.md`: both pages now read "last revised
2026-09-03" beside the July 16 draft date (F2); `privacy:40` no longer
promises a website/EIN field nothing collects, and instead discloses the
Stripe customer ID orgs on a paid plan actually get (F3, F4, one edit — the
sentence doing both jobs); the vendor list names Cloudflare Turnstile
(F5); the consent-gated-actions list on both pages now says "submitting or
appealing hours, leaving a review" instead of just the four original verbs,
matching the 9 `require_consent` call sites the audit found (F6); the
notifications-toggle sentence points at "the Notifications page" instead of
"account settings" (F7); `LEGAL_REVIEW_PACKET.md`'s "only two blanks" line
corrected forward with a dated note pointing at the third bracket at
`terms:69` (F8), not edited in place.

**NOT DONE, BY DESIGN.** F1 — registration forces "I agree" on a Terms page
whose own banner says it creates no binding obligations — is a decision
(rewrite the banner, rewrite the clickwrap copy, or gate registration behind
legal sign-off), not a text patch, and stays with Evan. F9–F12 are reviewer
notes (Stripe-portal cancel timing, the Turnstile-cookie carve-out, the
CCPA/GDPR naming, the HTTPS infra promise) and were left as written.

**VERIFY.** `npx tsc --noEmit -p frontend` clean, 0 errors, after both edits.
`git diff --stat` — 3 files, +16/-11. All seven edits are prose changes only,
no logic; no runnable check applies beyond the typecheck.

**OPEN.** M14.1 root record still not appended for the research brief or this
audit (offer stands, twice now).


## 2026-09-03 ~22:49 CDT — F1 resolved: registration now gated on a boot flag, not a checkbox

**WHAT.** Evan picked option 3: gate registration behind legal sign-off rather
than rewrite the banner or the clickwrap copy. Implemented as
`LEGAL_SIGNOFF_COMPLETE`, an 8th field on the existing `check_production_config`
boot guard (`app/core/config.py`) — same mechanism as the dev `SECRET_KEY` and
missing `SUPPORT_EMAIL` checks, default `false`, production refuses to boot
without it. Not a new gating path built parallel to the existing one.

**WHY THIS SHAPE.** The alternative was a per-route 503 on `/auth/register`
only. The boot guard already exists, already fails closed in production and
stays lenient in dev/test, and the actual problem — nothing should be
publicly reachable before sign-off, not just the register endpoint — is
better solved by refusing to boot at all than by patching one route.

**TOUCHED, because the guard has a tripwire.**
`test_boot_guard_check_count_matches_the_docs` asserts the exact count of
problems `check_production_config` reports and fails loudly on drift by
design — it did its job. Updated: `_prod()` test helper now sets
`LEGAL_SIGNOFF_COMPLETE=True`; new `test_prod_rejects_missing_legal_signoff`;
the tripwire's count 7→8 and name list gained the new field;
`test_prod_refuses_when_proxy_hops_was_never_declared`'s `kw` dict got the
field too so it still isolates one dimension. Docs updated the same session
(the tripwire's own instruction): `security.md`, `tooling.md`, `INDEX.md`,
`DEPLOY_RAILWAY.md` §check_production_config gotchas, `API_KEYS.md`,
`backend/.env.example`, `HANDOFF.md`'s BLOCKED-ON-EVAN list.

**VERIFY.** `ENVIRONMENT=ci .venv/Scripts/python.exe -m pytest -q` → 379
passed (378 + 1 new). `npx tsc --noEmit -p frontend` clean.

**Root record:** `../docs/Project Record — Full Chronological History.md`
II.46 covers this entry plus the research brief and docs audit together;
HTML twin regenerated, `broken: 0`.


## 2026-09-03 ~23:23 CDT — CI was red for three runs on a dependency nobody imports

**WHAT.** Pinned `anyio==4.14.1` in `backend/requirements.txt` (with the reason and an
upgrade trigger beside it) and recorded the general rule in
`.claude/codebase-memory/dependencies.md`. Both backend CI jobs had been failing;
the frontend job was green throughout.

**THE FAILURE WAS AT COLLECTION, NOT IN A TEST, AND THE EXIT CODE SAID SO.** GitHub
reported `exit code 4` for `Backend (pytest)` and `Backend (pytest on Postgres)`.
Pytest's 4 is a **usage error** — the suite never ran. `gh run view --log-failed`
gave the real line: `ImportError while loading conftest`, from
`tests/conftest.py:17` → `fastapi.testclient` → `starlette/testclient.py:53`, which
evaluates `anyio.abc.BlockingPortal`. anyio **4.15.0** deprecates that alias;
`pyproject.toml:9-10` sets `filterwarnings = ["error", …]`; so the DeprecationWarning
was raised as an exception during import.

**NOTHING IN THIS REPO CHANGED.** `anyio` is not in `requirements.txt` — it arrives
transitively through starlette and httpx. CI installs fresh every run and therefore
took 4.15.0, while the local venv still had **4.14.1** and stayed green. That is the
whole difference between "379 passed" here and a dead collection there.

**THE FIRST TWO GUESSES WERE WRONG, AND CHECKING WAS CHEAP.** `ruff check` and
`ruff format --check` both pass locally (exit 0, 117 files). `pytest` passes locally
(379). `pip-audit` DOES fail locally — `pip 26.1.2`, `PYSEC-2026-3721`, fixed in
26.2 — and it was a plausible culprit because it runs in both backend jobs and not
in the frontend one. It was not the cause. **The log settled in one command what
reasoning from the exit code alone would have gotten wrong**, which is the argument
for fetching the log before proposing a fix.

**NOT NEW TODAY.** Run `33823868339` (00:57 CDT) failed the same way — the
BlockingPortal warning appears twice in its failed-step log. So CI has been red
across at least three pushes, including the two this session's own work went out on,
and nobody looked until Evan pasted the annotations.

**WHY A PIN AND NOT AN IGNORE FILTER.** Adding `ignore:...BlockingPortal...` to
`filterwarnings` would also silence it locally, hiding a real upstream deprecation
that starlette will eventually have to fix. The pin matches the file's existing
precedent — `starlette==1.3.1` carries the same shape of comment — and it makes CI
install exactly what the 379-green local run proved. Bumping starlette past the
alias was rejected as the immediate fix: 1.3.1 is pinned to clear seven PYSEC
advisories, and moving it is a bigger change than a red CI justifies tonight.

**VERIFY.** `pip install -r requirements.txt --dry-run` resolves with no conflict
(every requirement already satisfied, no resolver error). `ENVIRONMENT=ci pytest -q`
→ **379 passed** after the edit. Local anyio is 4.14.1, which is what the pin now
names, so the pin describes a state that was actually tested rather than a guess.

**STILL RED, SEPARATELY:** `pip-audit` will fail the next run on `pip 26.1.2`
(PYSEC-2026-3721). That is the runner's own pip, not a project dependency, so
`requirements.txt` cannot fix it — CI needs a `python -m pip install --upgrade pip`
step before the scan. Not done here; it is a second, unrelated red.


## 2026-09-03 ~23:38 CDT — the legal pages stop claiming a safety review that never existed, and the age floor moves 12 → 13

**WHAT.** Acted on all four decisions Evan made against
`docs/premortem_2026-09-03_legal-pages.md`: rewrote the false organization-vetting
sentence (T1) and added roadmap **M15** to build the capability later; raised
`MINIMUM_AGE` 12 → 13 (T3); left the two governing-law blanks open but added a test
that refuses to let them ship (T4); gave the "no cookies" claim a Cloudflare
carve-out (T5). Also closed T2, which was not a question: the frontend now honours
the sign-off flag. **383 backend tests pass** (was 379: +3 new, and one existing
test had to move off an age that can no longer register).

**T1 — THE SENTENCE WAS FALSE ABOUT CHILD SAFETY, WHICH IS WHY IT WENT FIRST.**
`terms/page.tsx` told every reader "A ServeLocal administrator reviews organizations
before their listings go live." Confirmed three ways that nothing did:
`create_opportunity` (`app/api/routes/opportunities.py:139`) checks role and the
free-plan cap and nothing else; `Opportunity.active` defaults `True`
(`app/models/opportunity.py:52`); the public list filters on `active` alone
(`opportunities.py:70`); and a grep for any admin-approval path across routes and
services returns zero. It now says plainly that ServeLocal does **not** vet
organizations, that listings publish immediately, and that students and guardians
should confirm independently before attending anything in person. **Evan chose
"rewrite now, build later"**, so `PRD_ROADMAP.md` gained M15 (schema + gate, admin
surface, and — explicitly last — restoring a vetting sentence only after the gate
exists, because restoring it early is the exact failure this came from). M15 also
states the cost before it is chosen: every new org would then wait on one person who
is in school.

**T3 — 12 → 13 IS A ONE-CONSTANT CHANGE THAT SIDESTEPS A QUESTION RATHER THAN
ANSWERING IT.** COPPA's protected class is everyone under 13, so the old floor
admitted covered children and an exact DOB at signup handed the operator actual
knowledge at that moment. Whether an emailed guardian link meets *verifiable parental
consent* is still unexamined; 13 means we no longer need to know tonight. Blast
radius was small and checked first: one constant, two call sites in `auth.py`, two
copy lines.

**A TEST WAS RENAMED BECAUSE ITS NAME BECAME A LIE, AND THE REAL BOUNDARY GOT
COVERED.** `test_under_12_is_rejected` used age 11 — still rejected at the new floor,
so it would have passed while testing nothing new. It is now
`test_under_13_is_rejected`, plus `test_age_12_is_rejected_after_the_coppa_floor_raise`
(the boundary that actually moved, asserting the message names 13) and
`test_age_13_registers_as_a_gated_minor` (the floor is inclusive and still
consent-gated). **`test_opportunities.py::test_no_min_age_is_unaffected` then failed
with a `KeyError` on `access_token`** — it registered a 12-year-old, got a 400, and
tried to read a token out of the failed login. Fixed by moving it to 13; the age was
incidental to that test, whose point is a minor without consent hitting the consent
403.

**T2 — THE BOOT GATE HAD BEEN GUARDING THE WRONG LAYER.** `LEGAL_SIGNOFF_COMPLETE`
(backend, `config.py`) refuses to start production, so launching *requires* setting
it — and setting it changed nothing a user sees, because the draft banner is a
hard-coded `<div>` in the frontend. Launch day would have shipped a clickwrap over a
page saying "not yet in effect... does not create binding obligations." Now
`NEXT_PUBLIC_LEGAL_SIGNOFF_COMPLETE` in `lib/flags.ts` (the `BILLING_LIVE` pattern)
gates the banner and flips the subtitle to "In effect". **Verified by rebuilding with
the flag on and reading the live page**: banner gone, subtitle "In effect · last
revised 2026-09-03", on both pages; then rebuilt with it off so `.next/` is not left
claiming sign-off. `API_KEYS.md`, `DEPLOY_RAILWAY.md`, `.env.example` and
`security.md` now all say **set both or neither**.

**T4 — A BOOLEAN CANNOT SEE PROSE, SO THE GUARD READS THE PROSE.** New
`backend/tests/test_legal_pages_are_publishable.py`. Two rules: a pinned set of
known-open placeholders (so a NEW bracket cannot appear silently, and filling one
fails until the pin is updated), and a hard rule that if `LEGAL_SIGNOFF_COMPLETE` is
set, no bracket may survive at all. **Proven both directions**: passes today with the
flag off and two placeholders present; fails with `LEGAL_SIGNOFF_COMPLETE=true`,
naming both. It lives in the backend suite because the frontend has no test runner
and pytest is the only thing CI runs that can read prose — and it cannot live in the
boot guard, because api and web are separate containers, so the backend process has
no frontend tree to read in production.

**T5 + the third bracket.** "No cookies" became "no cookies of our own", with a
sentence naming Cloudflare's bot-check cookie as Cloudflare's and unread by us —
true today with Turnstile off and true the day the keys go in, so it needs no second
edit at the worst possible moment. The `[Refund terms… — Evan to confirm]` bracket at
`terms:69` was **removed as stale, not decided**: `PRD_ROADMAP.md:547` and ADR-0002
both record the refund default as Accepted in the 2026-08-31 Phase-0 batch.

**A TOOL LIED AND THE SECOND CHECK CAUGHT IT.** The browser `find` tool reported the
privacy page as reading "ServeLocal does use cookies to sign you in or to track you"
— the accessibility summary had swallowed the bolded `not`. Reading `innerText`
directly showed the correct "does **not** use cookies". Worth recording because that
inversion, if believed, is the single worst sentence that could ship in a privacy
policy, and the fix would have been to "correct" text that was already right.

**KNOWINGLY OVER CAP:** `security.md` is **157 lines** against ~150, having been
brought to exactly 150 earlier today. The four added lines are the set-both-or-neither
rule, which is the fact that stops T2 recurring. Stated rather than fixed by deleting
an unrelated security entry.

**VERIFY.** `ENVIRONMENT=ci pytest -q` → **383 passed**. `ruff check` → all checks
passed; `ruff format --check` → 118 files already formatted. `tsc --noEmit` → silent.
`npm run lint` → **0 errors / 11 warnings** (baseline held). `npm run build` →
compiled. Browser, production build, both pages: flag off → banner present, "at least
13 years old", the no-vetting sentence, no refund bracket; flag on → banner absent,
"In effect", placeholders still present (which is exactly the state the new test
fails on). LEFT OPEN, unchanged: the two governing-law blanks, HTTPS/HSTS (T6),
the Stripe-side cancellation timing (T7).


## 2026-09-03 ~23:40 CDT — CI's second red: the CVE scanner was failing on its own toolchain

**WHAT.** Added an `Upgrade pip` step to the `backend` job in `.github/workflows/ci.yml`,
before `Install dependencies` and therefore before the blocking `pip-audit` scan. This
is the second of the two independent CI failures found tonight; the first was the
`anyio` pin an entry earlier.

**THE SCANNER WAS FAILING ON A PACKAGE THE PROJECT DOES NOT DEPEND ON.** `pip-audit`
audits the whole environment, and that includes **pip itself**. The runner ships pip
**26.1.2**, which carries **PYSEC-2026-3721** (fixed in 26.2). Because `pip-audit` has
been a blocking step since 2026-08-08 and has no severity threshold, this would have
failed every push indefinitely — and `requirements.txt` cannot fix it, because pip is
not a project dependency. The gate was correct and the finding was real; the fix simply
does not live where the project's other dependency fixes live.

**VERIFIED BEFORE IT WAS WRITTEN, NOT AFTER.** Ran the upgrade in the local venv first:
`pip 26.1.2 -> 26.2.1`, then `pip-audit` went from `Found 1 known vulnerability in 1
package` to **`No known vulnerabilities found`, exit 0**. The comment in the workflow
carries those numbers so the next reader does not have to re-derive them.

**ONLY THE `backend` JOB CHANGED, ON PURPOSE.** `backend-postgres` runs no `pip-audit`
step — parsing the workflow confirms its four steps are checkout, setup-python, install,
run tests. Adding the upgrade there too would have been symmetry for its own sake with
no failure behind it. Stated because the asymmetry now looks like an oversight and is
not one.

**VERIFY.** The workflow parses as valid YAML and the step order is
`checkout · setup-python · Upgrade pip · Install dependencies · Lint (ruff) · Format
check (ruff) · Dependency CVE scan (pip-audit) · Run tests` — read back by loading the
file with `yaml.safe_load` rather than by eye. `tooling.md`'s CI table updated to match.
Not verified on the real runner: the actual proof is the next push going green, and both
of tonight's CI fixes are still uncommitted.


## 2026-09-05 ~16:27 CDT — four audit fixes, and the one where the prescribed fix does not work on the test engine

**WHAT.** Acted on the 2026-09-05 daily audit's five fixes. **Fix 5 was already done** —
its premise was a stale snapshot: those 20 files were committed as `8bfbe56` and pushed
earlier the same session, the tree was clean, both formerly-untracked files were tracked,
and `pytest --collect-only` returned 383, not the 381 the audit predicted. Nothing to do.
The other four landed. **393 passed + 1 skipped on SQLite; 394 passed on real Postgres.**

**FIX 1 — the leaderboard called organizations "Vetted Orgs" while the Terms said the
opposite.** `frontend/app/leaderboard/page.tsx:60` → `Organizations`. One word. The
2026-09-03 fix that corrected this exact false claim reached `terms/page.tsx` and missed
the leaderboard, which git blame explains: leaderboard last touched 2026-09-01, terms
2026-09-03. Browser-verified on a production build with the API up: the four stat labels
now read VERIFIED HOURS / STUDENTS / **ORGANIZATIONS** / SERVICE ENTRIES, and `/vetted/i`
matches nowhere on the page.

**FIX 2 — the guardian kill switch worked on two of three public surfaces.**
`list_reviews` is a fully public GET with no auth dependency and returned `author_name`
from a column denormalized at write time, with no live re-check — so a minor's first name
and last initial kept being served after a guardian revoked. Portfolio got this check
2026-08-11, leaderboard 2026-08-06; reviews was missed. Fixed with the leaderboard's own
SQL predicate (`adult_cutoff_date`, so the age arithmetic keeps one implementation).

**Applied to all THREE queries, not just the list the audit named.** Filtering only the
rows would have left `count` and `average_rating` computed over hidden reviews — the
revoked minor still moving the org's public star rating, and the count disagreeing with
what rendered. Three regression tests; **verified they fail without the fix** by stashing
`reviews.py` and re-running: 2 failed, 10 passed. A regression test that passes without
the fix proves nothing.

**FIX 3 — THE PRESCRIBED FIX DOES NOT CLOSE THE BUG ON SQLITE, AND THE TEST PROVED IT.**
Added `.with_for_update()` per the audit. Then the required concurrency test —
two applies to a one-spot listing — **failed, with both returning `status: "approved"`**.
Not a test bug: `SELECT ... FOR UPDATE` is a **no-op on SQLite**, which is what the default
suite runs, so the oversell is still reachable there. Re-ran the same test against real
Postgres 16 (`TEST_DATABASE_URL`, docker compose db): **3 passed**. So the fix is real
where production runs and absent where the tests run. The race test is now Postgres-gated
with that stated in the `skipif` reason rather than buried, and CI's `backend-postgres` job
is what executes it.

**One helper, not six call sites.** The audit named six unlocked read-modify-writes.
`enrollment.promote_from_waitlist` does not fetch — it receives an already-loaded `opp` —
so locking there is impossible; the lock belongs at the four FETCH sites whose request
mutates spots (apply, withdraw, org decide, guardian revoke). Those now call
`get_opportunity_for_update()` in `services/enrollment.py`, and a test pins the count at 4
so a fifth spots-touching path cannot quietly skip it. `billing.py:92`'s comment claiming
"there is no row locking anywhere else in this codebase" is now false and was corrected.

**FIX 4 — the three throttles, replaced by one atomic primitive.** New
`app/core/throttle.py`: a `Throttle` class doing check-and-record in ONE call under ONE
`threading.Lock`. A cooldown is just a window with `max_events=1`, so the two shapes that
existed collapsed into one class, and three duplicated `_sweep`/`_since_sweep` copies went
with them. **The audit's reproduction is now a permanent test**, and I re-ran the OLD shape
standalone first to confirm the test is meaningful: **50 of 50 passed a cap of 10** with
check-then-act; exactly **10 of 50** with the new class.

**A DELIBERATE SEMANTIC CHANGE, stated because it is not obvious.** Check-in reserves the
attempt at ENTRY instead of recording a failure afterwards — atomicity is impossible
otherwise, since whether to record depended on DB work done after the check. The two
post-hoc `record_fail()` calls are gone. `reset()` on a correct code still wipes the key,
so the cap remains 10 consecutive FAILED attempts for any real user.

**MY OWN CHANGE BROKE 40 TESTS AND I HAD GREPPED THE WRONG THING.** Removing the private
`_requests` / `_resend` dicts errored every test whose fixture reached into them — I had
grepped for the throttle FUNCTION names and never for the dicts. Repointed five fixtures
across four files at the public `reset()` / `reset_all()`. `ruff` then caught three more
of my own: an over-long comment, an `Opportunity` import my helper orphaned in
`consent.py`, and an import I had placed below a module constant.

**DOC CORRECTIONS (same commit).** `PRD_ROADMAP.md`'s age-floor paragraph asserted 12 and
claimed code and both drafts said 12 — all three now say 13. Per the append-only
convention the historical text is untouched and carries a dated 2026-09-03 follow-up
saying a legal reviewer should take 13 as current. "Seven boot blockers" → the boot
guard's **9 checks over 8 variables** in `HANDOFF.md` and two places in `PRD_ROADMAP.md`,
mirroring `INDEX.md`, which had it right; **third recurrence of that undercount**.
`HANDOFF.md` had 378 in one place and 383 in another; both now carry the real,
just-measured numbers.

**VERIFY.** SQLite: `393 passed, 1 skipped in 63.14s`. Postgres 16: `394 passed in 85.64s`.
`ruff check` all checks passed; `ruff format` clean (3 files reformatted, then clean).
`tsc --noEmit` silent. `npm run lint` **0 errors / 11 warnings** (baseline held).
`npm run build` compiled. Leaderboard label browser-verified on a production build.
NOT pushed, per the audit prompt.


## 2026-09-05 ~16:28 CDT — the legal-pages pre-mortem, re-run: two Tigers genuinely closed, one new one about the safety net itself

**WHAT.** Ran `/pre-mortem on the legal docs` — the re-run
`docs/premortem_2026-09-03_legal-pages.md`'s own Follow-up section asked for,
after `8bfbe56` acted on T1–T5. Output: `docs/premortem_2026-09-05_legal-pages.md`.
Read cold: current age floor, the flag-gated JSX, the CI workflow's env vars,
the M15 roadmap entry, and the placeholder set were each re-checked against
disk, not recalled from the prior document.

**RECLASSIFIED, not just re-listed.** T3 (COPPA) is genuinely closed: raising
`MINIMUM_AGE` 12→13 (`consent.py:21`) means no honestly-registered user is a
COPPA "child" (under 13) at all, so the verifiable-parental-consent question
T3 raised no longer attaches to anyone the site admits — confirmed against
FTC FAQ A.12 in the 2026-09-03 research brief, not asserted. T1 (false
vetting claim) is closed and checked for leakage: grepped the rest of the
frontend for the same claim, found none. T2 and T4's code is real, verified
by direct read of `flags.ts` and both pages' conditional JSX.

**NEW: NT1, launch-blocking.** `test_signed_off_pages_have_no_placeholders_left`
is the mechanism built to stop `LEGAL_SIGNOFF_COMPLETE=true` from shipping
with a bracket in the governing-law clause. `.github/workflows/ci.yml` never
sets that variable in either pytest step — so every CI run to date has hit
the test's early-return, never its assertion. The only thing that has ever
verified "flag true, no placeholders" is the one manual local run the
2026-09-05 commit describes. A green pipeline and a working safety net are
not the same fact, and this is the day that gap stopped being theoretical.

**NEW, minor: NT2 (fast-follow).** The subtitle's `2026-09-03` revision date
is hardcoded in both branches of the flag ternary — it will still say
"2026-09-03" the day real sign-off happens.

**NEW: NT3 (track), an Elephant carried forward as E1.** M15 (org review) has
no owner or decision date and is stated not to gate launch — on a project
that already diagnosed exactly this failure mode in itself (II.45: artifacts
instead of progress). E1 restates 2026-09-01's pre-mortem E3: two rounds of
real fixes have now landed around the missing reviewer without naming one.
`terms/page.tsx:91` still holds both placeholders, unchanged since 2026-09-03.

**NOT DONE, by design.** No code changed this session — the register ends in
an action plan and a launch decision, same as 2026-09-03's, for Evan to act
on. `risk_categorizer.py` counts: 5 risks, 3 Tigers (1/1/1) / 1 Paper Tiger /
1 Elephant, auto-flagged.

**VERIFY.** `ENVIRONMENT=ci pytest -q --collect-only` in `backend/`: 394
tests collected — higher than HANDOFF's stated 388 because the working tree
also carries an unrelated, uncommitted capacity-locking feature; noted as a
scoping caveat, not analyzed as a legal-pages finding. `gh run list`: the
push containing `8bfbe56` is CI-green (`success`, 2m25s) after four prior
reds — the anyio/pip-audit fixes are confirmed on the real runner.


## 2026-09-05 ~17:52 CDT — NT1 and NT2 fixed, and NT1's first fix was wrong

**WHAT.** Evan approved fixing both findings from
`docs/premortem_2026-09-05_legal-pages.md`.

**NT1, the honest failure first.** The obvious fix — add a CI step forcing
`LEGAL_SIGNOFF_COMPLETE=true` and re-running
`test_legal_pages_are_publishable.py` — was written, then tested locally
before being trusted: `ENVIRONMENT=ci LEGAL_SIGNOFF_COMPLETE=true pytest -q
tests/test_legal_pages_are_publishable.py` failed immediately, because the
real pages genuinely still hold both open placeholders. That fix would have
made CI permanently red for as long as legal sign-off is pending — correct
behavior from the test, useless as a standing gate, and the wrong shape for
what NT1 actually asked for. Reverted before it was committed.

**The real fix decouples the mechanism from today's content.** Extracted the
assertion into `_assert_none_survive_if_signed_off(pages, signoff_env)`
(`backend/tests/test_legal_pages_are_publishable.py`); the real test still
calls it against the real pages and the real env var, but a new
`test_placeholder_rule_fires_on_a_planted_placeholder` calls the same function
against a `tmp_path` fake file with a planted bracket, a clean fake file, and
the flag off — three assertions, all decoupled from whether the real pages
currently have zero, one, or two open brackets. This runs unconditionally,
every push, and proves the rule works regardless of the real flag's value. No
CI workflow change needed; the fix lives entirely in the existing pytest job.

**NT2.** `LEGAL_LAST_REVISED = "2026-09-03"` added to `frontend/lib/flags.ts`
as the single source for the date that was previously hardcoded in 4 places
(2 pages x 2 banner branches); both pages now interpolate it. Reduces the
"forgot to bump one of four copies" surface to one constant — does not
eliminate the human step, which no code change can.

**VERIFY.** `test_legal_pages_are_publishable.py -v`: 3 passed (was 2) under
normal flag-off conditions — the new meta-test genuinely exercises fire and
no-fire paths every run, not just once by hand. Full suite: 394 passed, 1
skipped (was 393 passed before this session's new test). `tsc --noEmit`
clean. Both pages browser-verified at `localhost:3000/terms` and `/privacy`:
"Draft dated: July 16, 2026 · last revised 2026-09-03" renders correctly from
the shared constant on both.

**NOT DONE.** E1 (no named reviewer) is unchanged — no code change closes it.


## 2026-09-05 ~18:04 CDT — verifying someone else's fixes, and the bins that three commits had skipped

**WHAT.** NT1 and NT2 were fixed and committed by a concurrent session (`b63ebba`). This
entry is the verification pass plus the codebase-memory sync that commits `b537ce5` and
`b63ebba` both skipped. **394 passed + 1 skipped on SQLite; 395 passed on Postgres 16.**
No code changed here — the bins and HANDOFF did.

**NT1 VERIFIED BY MUTATION, NOT BY READING.** The claim is that
`_assert_none_survive_if_signed_off` now runs every push against a planted placeholder in
a throwaway file, instead of only firing the one day someone sets the real flag true. I
broke the rule (`found = set()`) and re-ran: `test_placeholder_rule_fires_on_a_planted_
placeholder` FAILED, the other two passed. So the new test genuinely exercises the
mechanism and is not decorative. Restored the file afterwards and confirmed the tree clean.

**The rejected first attempt is worth keeping**, and the other session recorded it: forcing
`LEGAL_SIGNOFF_COMPLETE=true` as a CI step is the obvious fix and the wrong one — it makes
CI permanently red, because today's real pages legitimately still carry two open
placeholders. That is correct behaviour being mistaken for a gate.

**NT2 VERIFIED BY SENTINEL BUILD.** "One constant now drives four spots" is exactly the
kind of claim a grep can support and a build can refute. Set `LEGAL_LAST_REVISED` to
`SENTINEL-9999-01-01`, rebuilt, and read both live pages: **2 sentinel hits per page, and
`2026-09-03` matched nowhere** — 4 of 4 rendered spots trace to the constant. Restored,
rebuilt, `git status` clean, so the sentinel left nothing behind.

**THE BINS WERE THREE COMMITS BEHIND, WHICH IS THE FAILURE THIS PROJECT KEEPS HAVING.**
Nothing in `.claude/codebase-memory/` knew about the reviews consent leak, the capacity
row lock, or the atomic throttle — all landed in `b537ce5` without a bin update. Fixed:

- **`consent.md` gains the FOURTH public surface.** This bin already said in bold "Three
  sibling surfaces publish student data... a change to one is a change to all three." That
  sentence was WRONG, and its wrongness is what let `/orgs/{id}/reviews` go unnoticed for a
  month — the bin was actively pointing readers at an incomplete list. It now names four
  and says to ask which of the four any new public read resembles.
- **`conventions.md`**: never `db.get(Opportunity, ...)` on a spots-moving path; use
  `enrollment.get_opportunity_for_update`. Four locked fetch sites, count pinned by a test,
  and `promote_from_waitlist` explicitly excluded because it receives an already-loaded row.
- **`testing.md`**: a row-lock test proves nothing on SQLite — with the lock correctly in
  place two concurrent applies BOTH returned `approved` there, and the same test passed on
  Postgres. Gate on `TEST_DATABASE_URL`, put the reason in the `skipif`, and pair it with a
  dialect-compile assertion that survives SQLite.
- **`security.md`**: every anti-abuse throttle now goes through one `core/throttle.Throttle`.
- **`INDEX.md`** routing lines repointed, including a stale **378** test count.

**KNOWINGLY OVER CAP, worse than yesterday:** `security.md` is **160** lines against ~150
(was 157). `conventions.md` 151 and `testing.md` 152 are also a hair over. Stated rather
than fixed by deleting entries that are all load-bearing; the honest number beats a clean
one, and the same compression decision as 2026-09-03 applies whenever Evan wants it made.

**VERIFY.** SQLite `394 passed, 1 skipped in 51.23s`; Postgres 16 `395 passed in 85.62s`;
`ruff check` all checks passed. NT1 mutation → 1 failed as designed, then restored. NT2
sentinel build → 2 hits/page, 0 stale dates, then restored and rebuilt clean.


## 2026-09-05 ~18:26 CDT — bin cap raised 150 → 200, and compression found a real gap while shrinking prose

**WHAT.** Evan: "compress bins and raise limit to 200." Raised the per-bin cap in the
`project-memory` skill (`SKILL.md:365`) with a dated reason attached — ServeLocal v2 hit
160 on a bin still adding load-bearing facts weekly, and the ~150 cap was compressing real
content out faster than the project could stop generating it. Same shape as the INDEX cap
raise on 2026-09-03. Compressed the three bins that were over it: `security.md` 160 → 154,
`conventions.md` 151 → 145, `testing.md` 152 → 148. Committed `13ec7e0`, not pushed until
now.

**COMPRESSION FOUND A REAL BUG, NOT JUST VERBOSE PROSE.** Rewriting the boot-guard section
of `security.md`, its own "9 checks over 8 variables" enumeration turned out to **omit
`TRUSTED_PROXY_HOPS`** even though the count included it — a session reading the bin's list
literally would have missed one of the nine. Verified against `app/core/config.py:190-205`
before writing the fix, so the correction is sourced, not guessed. The bin had been telling
readers to trust its own enumeration over the code for at least two weeks.

**TWO SECTIONS WERE DUPLICATING SOURCES OF TRUTH THAT ALREADY EXIST ELSEWHERE.**
`security.md` stated the `is_admin`-only admin rule twice, once in its own section and
again pasted at the end of the boot-guard section. `conventions.md`'s entire "Hard rules"
section restated `CLAUDE.md` almost verbatim — definition of done, commit/push, BLOCKED-
ON-EVAN — a file that is loaded every session regardless. Both are now pointers instead of
copies: one place to keep current instead of two that can silently disagree, which is the
same failure class as the stale test counts this project keeps re-discovering.

**`testing.md` carried a dangling chain of historical test-count deltas with no actionable
content:** `329 → 330 → 334 → 341 → 349 → 354` mashed onto an unrelated "plant the
positive" bullet, followed by a second unrelated chain `277 → 285 → ... → 327`. Neither
chain is a fact a future session acts on — it is an append-only leftover nobody swept.
Deleted. The bin's own headline test count was also still **378**, stale since before the
2026-09-05 work landed 394/395 everywhere else — corrected here.

**VERIFIED NOTHING WAS LOST, not assumed:** grepped for `TRUSTED_PROXY_HOPS`, the `is_admin`
rule, the `CLAUDE.md` pointer, and the corrected count post-edit — each present exactly
once. Full suite re-run before push: **394 passed, 1 skipped** on SQLite.

**VERIFY.** `wc -l`: security.md 154, conventions.md 145, testing.md 148 — all three well
under the raised 200 cap. `git diff --stat` on the three files: 31 insertions, 47
deletions, net -16 lines with zero facts removed.


## 2026-09-05 ~18:39 CDT — the handoff prompt told the next session to refuse a push Evan asks for routinely

**WHAT.** Refreshed HANDOFF's "Next session — paste this" block. Not a rewrite for
freshness alone: its HARD CONSTRAINTS said **"Never push. Never sync the public
mirror."**, a line that had been true once and had since gone false in practice — both
have happened repeatedly this session on Evan's direct word (`737b231`, mirror `a3eb658`
among others). A fresh session reading that constraint literally would have refused a
routine, explicitly-requested push, or pushed anyway and concluded the doc lies. Replaced
with the actual rule: push and mirror-sync need Evan's instruction each time, same as
commit — not blanket-forbidden, not unprompted either.

**Also folded in three facts a fresh session would otherwise have to rediscover the hard
way**, each already paid for once this session: the cached-200-on-a-dead-server trap in
the stop-the-backend check, `FOR UPDATE`'s SQLite no-op, and the real PRD state — 18/23
success criteria (78.3%), with the 5 open ones being M11 launch itself, not stray tasks.
T1 (stale mirror) marked DONE in the next-actions list rather than left as an open item
that was already closed two syncs ago. M15 added as a buildable-now, non-gating unit.

**VERIFY.** `git diff HANDOFF.md` reviewed line by line before this entry: no unrelated
content changed, only the paste-ready block and its timestamp.


## 2026-09-05 ~18:46 CDT — three legal-page updates, and a count from our own INDEX that did not survive checking

**WHAT.** Evan asked to update the Privacy Policy and Terms of Service. No
content was specified, so the pages were diffed against what the code
actually does; three gaps were found, all the same class — **the documents
understate protections the code already provides**, the safe direction of
error, and all three created by fixes that landed 2026-08-11 → 2026-09-05
without the prose following them.

1. **`privacy/page.tsx` "Public surfaces minimize minor names"** listed the
   leaderboard and portfolio. `first_last_initial` also runs on reviews
   (`reviews.py:68` read, `:134` write), which the 2026-09-05 consent-leak
   fix made consent-filtered too. Reviews added.
2. **`privacy/page.tsx` Guardian rights** described revoke as forward-only
   ("re-blocks the student from applying, messaging..."). Revoke has been
   retroactive since 2026-08-11 and got wider on 2026-09-05: active
   applications are withdrawn and the spot released to the waitlist, and the
   student's name and reviews stop appearing publicly — **including the org's
   public review count and average rating, recalculated without them**. A
   guardian reading the old text would not have learned any of that. Written
   in, along with the honest limit: the org keeps its record of already-
   verified hours, shown as a deactivated participant without saying why.
3. **`terms/page.tsx` content-license paragraph** said the license ends on
   deletion of content or account. Guardian revocation is a second
   termination trigger for a minor's public display; added.

**A NUMBER THAT DID NOT SURVIVE.** The options presented to Evan said four
public surfaces, citing `INDEX.md`'s "read-time consent on FOUR public
surfaces — portfolio, leaderboard, org lists, and `/orgs/{id}/reviews`".
Checking before writing it into a privacy policy: `grep -rn
"first_last_initial" backend/app` returns leaderboard, portfolio, reviews and
`sender_display_name` (messages). "Org lists" are **org-facing, not public** —
per `consent.md` they deliberately still show identity because the org keeps
its record. The policy now says **three** public surfaces, named
individually rather than counted. INDEX.md's phrasing is not wrong about
consent filtering, it is wrong as a count of *public* surfaces, and this
entry is the correction rather than an edit to that bin.

**NOT WRITTEN, deliberately.** `sender_display_name` minimizes a minor's name
on messages too (`messages.py:92,123,192`). Messages are not a public
surface, so it does not belong in the sentence that was in scope; flagged
here rather than folded in silently.

**VERIFY.** `LEGAL_LAST_REVISED` bumped 2026-09-03 → 2026-09-05 — the first
real use of the constant added earlier today, and the mechanism worked: one
edit moved the date in all four rendered spots. `tsc --noEmit` clean.
`test_legal_pages_are_publishable.py`: 3 passed. Both pages browser-verified
on a fresh dev server: the date reads 2026-09-05 in both the subtitle and
the draft banner, and all three new passages render (`find` located each by
its own text). Placeholders and the sign-off flag untouched.
